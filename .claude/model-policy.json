{
  "_comment": "Single source of truth for sub-agent model assignment (AI_OPERATIONS_PLAN §2.2). `tiers` maps a capability tier to a model; `agents` maps each .claude/agents/*.md to a tier. `scripts/check-model-policy.ts` enforces this: --check (in verify.sh) fails if any agent's frontmatter `model:` drifts from policy; --write is the hygiene routine that rewrites frontmatter from here. Only /research may change a tier→model mapping, after verifying the official model catalog (stamp last_verified). No other file may hardcode a model name.",
  "agents": {
    "builder": "builder",
    "builder-strong": "reasoning",
    "db-engineer": "reasoning",
    "evaluator": "reasoning",
    "explorer": "fast",
    "security-reviewer": "reasoning"
  },
  "tiers": {
    "reasoning": {
      "model": "opus",
      "effort": "high",
      "last_verified": "2026-06-09",
      "notes": "Orchestrator, planner, evaluator, security-reviewer, db-engineer, and builder-strong (Tier C implementation — reasoning-critical/irreversible/novel work). Fable 5 (claude-fable-5) is a valid upgrade for the hardest planning/evaluation passes; usage-credit pricing applies after 2026-06-22."
    },
    "builder": {
      "model": "sonnet",
      "effort": "high",
      "last_verified": "2026-06-09",
      "notes": "Routine implementation of one well-briefed feature. Sonnet 4.6 at ~60% of Opus per-token price."
    },
    "fast": {
      "model": "haiku",
      "effort": "medium",
      "last_verified": "2026-06-09",
      "notes": "Explorer/search/triage fan-out. Returns conclusions, never file dumps."
    }
  }
}
