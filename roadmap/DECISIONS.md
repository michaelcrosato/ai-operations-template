# Decisions Log (append-only, ADR-lite)

> One entry per autonomous judgment call: context → decision → reversible? → where it lives.

- **2026-06-09** · Bootstrap branch model → initialized repo with `develop` as the root/default branch so the constitution's "never touch master/main" rule is never violated, even at bootstrap; stable branch gets created at first promotion. Reversible (branch settings). → git history, AI_OPERATIONS_PLAN §6.4.
- **2026-06-09** · Repo visibility → **private** (safe default for an unreviewed engine); marked as GitHub *template repository* for one-click reuse. Reversible in repo settings. → github.com/michaelcrosato/ai-operations-template.
- **2026-06-09** · Permission rule `"publish"` (intent unclear) → interpreted as package-publishing ban: `Bash(npm publish*)` + `Bash(pnpm publish*)`. Reversible. → .claude/settings.json.
- **2026-06-09** · `assertion-shield` bypass env var was agent-settable (self-bypass hole) → bypass now ignored when `CI=true`, and `guard-bash.sh` denies setting it in commands. Reversible. → scripts/assertion-shield.ts, .claude/hooks/guard-bash.sh.
- **2026-06-09** · features schema lacked the `dependencies` field that CLAUDE.md's session protocol and update-state validation both reference → added optional `dependencies: string[]` (feature IDs) to features.schema.json. Reversible. → roadmap/features.schema.json.
- **2026-06-09** · Engine meta-tooling language → kept existing TypeScript (ts-node) rather than rewriting to plain JS; added package.json with pinned dev-tooling so `npx ts-node` is deterministic. Reversible. → package.json.
- **2026-06-09** · GitHub Actions pinning → pinned to major release tags (v4/v5/v1), not commit SHAs: SHA freshness can't be web-verified from this session; weekly hygiene routine owns upgrading tags→SHAs. Reversible. → .github/workflows/.
- **2026-06-09** · Added `/kaizen` skill (not in original blueprint) → operator goal explicitly requires manager-style continuous improvement ("1% better daily"); blueprint's weekly hygiene was too coarse. → .claude/skills/kaizen/.
- **2026-06-09** · State-flip convention → `update-state.ts --passes/--status done` runs **inside the feature PR** (not as a post-merge push to develop): merge-on-green-CI makes "CI green" acceptance self-enforcing and keeps develop strictly PR-only. → .claude/skills/work/SKILL.md step 6.
- **2026-06-09** · Security review accepted risks (each mitigated by another layer, revisit when stakes rise): ① bash-redirection edits to features.json bypass the Edit/Write hook — CI `--validate` + evaluator backstop; ② a forged `VERIFY: PASS` log can flip `passes:true` cheaply — CI re-runs the real gate on every PR, log now carries a `VERIFY-COMMIT` signature for audit; ③ `Bash(npx *)`/`Bash(node *)` allow rules permit arbitrary code that could read `.env` — the real boundary is the environment (dev creds only, prod secrets never mounted); ④ guard-bash regexes evadable by quoting/indirection — deterrent layer, backed by branch protection once active. → security-reviewer report, PR #1.
- **2026-06-09** · `VERIFY_GATE_PARSER` env seam added to verify-gate.sh so contract tests can force the jq/node/sed branch deterministically on any platform; all branches remain fail-closed. → .claude/hooks/verify-gate.sh.
- **2026-06-09** · Operator's philosophy (F-0005) codified as README "Philosophy" + plan P6/§5.5 rather than a standalone doc → one source of truth per audience: README for principles, plan for mechanics, skills for behavior. "Keep the team busy" interpreted as *keep the system ready* (pre-briefs, verified assumptions, healthy pipelines) with a ≤30% downtime context budget — literal always-busy agents would violate the operator's own token-efficiency requirement. → README.md, AI_OPERATIONS_PLAN.md §5.5, .claude/skills/downtime/.
- **2026-06-09** · F-0005 touches `.claude/skills/**` (agent behavior, not enforcement surfaces) → evaluator review only, security-reviewer skipped; hooks/workflows/permissions untouched. → /work step 5 sensitivity rule.
- **2026-06-10** · External-review remediation (F-0006) batched into one PR rather than nine → the defects share surfaces (gates, docs) and a single CI/review cycle; one-feature-per-PR remains the rule for product features. → PR #3.
- **2026-06-10** · Plan's claude-code-action vulnerability citation corrected → the published advisory is GHSA-8q5r-mmjf-575q (<1.0.74, malicious `.mcp.json` in PRs → RCE/secret exfiltration); the previously cited "pre-v1.0.94 [bot]-actor flaw" matched no published advisory. Added Claude Code 2.1.128 `/proc/self/environ` fix (Microsoft, 2026-06-05) and Agents-Rule-of-Two guidance. Verified against the GitHub advisory API + vendor docs 2026-06-10. → AI_OPERATIONS_PLAN §7.2.
- **2026-06-10** · StopFailure registration removed from settings → hooks reference confirms StopFailure cannot block (output/exit code ignored); registration was dead config. → .claude/settings.json, plan §6.3.
- **2026-06-10** · security-patterns.json rewritten to the plugin's documented schema (`patterns[]`, rule_name/reminder/regex|substrings) → previous file used an invented schema and was silently ignored. JSON retained over native YAML deliberately: works without PyYAML. → .claude/security-patterns.json, plan §7.2.
- **2026-06-10** · Format hook and per-feature path guard formally deferred (not silently missing) → format hook activates when a product formatter exists (Phase 1); path guard tracked as F-0007 pending an "active feature" design. → plan §6.3.
- **2026-06-10** · assertion-shield switched to execFileSync with arg arrays → flagged in-session by the security-guidance plugin (env-supplied BASE_BRANCH must not pass through a shell). → scripts/assertion-shield.ts.
- **2026-06-10** · First Dependabot wave: actions/checkout v6, setup-node v6, @types/node 25 merged on green CI; TypeScript 6.0.3 initially failed the gate (TS5107: moduleResolution "node10" deprecated) → tsconfig migrated to module/moduleResolution "NodeNext" on the same PR rather than pinning back. Reversible. → tsconfig.json, PR #7.
- **2026-06-10** · **feedback-2.md verification (operator-ordered):** all 77 recommendations verified by a 14-agent fleet against primary sources + repo ground truth (52 claims in the feedback refuted, incl. a phantom "v1.0.94" advisory and an inverted AGENTS.md design). Outcome: 26 adopt-now → groomed as F-0008–F-0011; 27 sequenced; 25 reshaped; 18 rejected; 25 already done. Full report: docs/feedback-2-verification.md (+ machine-readable verdicts JSON). → PR #10.
- **2026-06-10** · Feedback §J avoid-list formally adopted as standing policy at zero always-loaded-context cost (recorded here, NOT in CLAUDE.md): no observability platforms, graph/DAG frameworks, monorepo release machinery, scheduled write-capable automation pre-protection, untrusted-PR-code execution, maintenance-mode/hype references, DSPy-style black-box prompt optimization, vector-DB RAG at this size, low-code canvases, docs sites pre-adopters, trust-widening defaults, CLA/badge noise, telemetry-by-default, auto-merge/deploy, per-agent template forks, velocity-over-rigor. → docs/feedback-2-verification.md §J verdicts.
- **2026-06-10** · Fork rulings (7): graphs/parallelism rejected; MCP migration rejected + registry-as-policy-rule until first external tool; shellcheck+actionlint now, zizmor next CI touch (its unpinned-uses default must be configured to respect our tag-pin decision), semgrep/codeql at product code; sandboxing via Claude Code's NATIVE OS-level sandbox when needed (never bespoke Docker); CODEOWNERS gated on Q-0001; src/ layout decided when product code lands; AI PR review = diff-analysis-only with claude.yml-grade gates. → report §Fork rulings.
- **2026-06-10** · AGENTS.md direction corrected from feedback: official docs say Claude Code reads CLAUDE.md, not AGENTS.md (import pattern `@AGENTS.md`; no symlinks on Windows) → we ship only a thin AGENTS.md pointer stub for other CLIs (F-0011); no divergence checker. → code.claude.com/docs/en/memory, verified 2026-06-10.
- **2026-06-10** · **Promotion #2 (operator-ordered): develop → main, tagged v0.2.0** (PR #21, merge commit, verify check server-enforced). Mechanical necessity logged: main's required-approval rule was lifted for this one merge and RESTORED immediately after — GitHub cannot let a sole author approve their own PR, and the GitHub App (which will author future promotion PRs so the operator can approve them) is not yet installed. Once the App is in (Q-0001), promotions revert to the designed App-authored + operator-approved flow and this workaround retires. → repo settings, tag v0.2.0.
- **2026-06-10** · **F-0010/11 review cycle (attempts: 1 each):** evaluator NEEDS_WORK on three mechanical findings (evidence dirs missing pre-judging; README.md edit outside F-0011's groomed scope; one seeded metrics record over-claimed F-0001's security verdict) — all fixed. README ride documented here rather than rescoped: `--paths` correctly refuses to restate F-0011's guard-surface scope (grandfathered at --add; rescope-cannot-grant is by design — factory features needing guard surfaces are re-groomed, not rescoped). Security APPROVE; took two hardenings: 500-char cap per metrics record (bounded-injection rule) and this exemption note: read-only `*_FILE` test seams (STATE_FILE/MODEL_POLICY_FILE/METRICS_FILE) are NOT "dangerous overrides" under the DANGEROUSLY_ naming rule — they forge nothing CI doesn't re-run. → PR #19.
- **2026-06-10** · **New dev-dependencies (F-0008):** @biomejs/biome 2.4.x (engine lint; `noRedundantUseStrict` disabled — repo is CommonJS and the directive is semantic, Biome's "safe" fix wrongly strips it) and shellcheck 4.x npm wrapper (cross-platform binary, no install scripts) — both registry-resolved with integrity hashes. actionlint is NOT a dependency: CI-only, version+sha256-pinned download. → package.json, biome.json, ci.yml.
- **2026-06-10** · **F-0008/9 review cycle (attempts: 1 each):** evaluator BLOCKED on the tripwire self-tripping its own introducing PR (grep pattern contained its trigger string) → fixed by key-shape anchoring (`key\s*[:=]`); security APPROVE with a MEDIUM (`wget --post-data` evaded the exfil guard despite wget being named) → flags added (+`--post-file`,`--body-data`,`-T`) with tests; MCP tripwire widened to `"mcpServers"`/`--mcp-config` per the same review; actionlint download now checksum-verified. Out-of-scope ride documented: `src/health.js` got a one-line cosmetic template-literal fix forced by the new lint gate (no behavior change, 'use strict' intact). → ci.yml, guard-bash.sh, PR #18.
- **2026-06-10** · **Subscription-first auth (operator-directed, verified 2026-06-10):** the engine requires NO paid API key from any provider in any lane. Cloud sessions/Routines = subscription login; local CLI = subscription login; @claude Actions lane = `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token` (officially supported for Pro/Max; bills the agent credit pool included with Max 20x from Jun 15). `ANTHROPIC_API_KEY` demoted to optional-alternative everywhere it was mentioned. Operator's parallel subscriptions (Gemini Ultra, ChatGPT Pro, SuperGrok) are not integrated by design (plan §2.1); verified in passing that Gemini AI Pro/Ultra does include AI-Studio API access since Apr 2026, for the heterogeneous-second-opinion lane if ever wanted. → claude.yml, plan §7.2/§11, QUESTIONS Q-0001, README, PR #17.
- **2026-06-13** · Downtime /groom judgment on ROADMAP Next bullet ("Nightly autonomous run (Routine) once a week of supervised runs is clean."): unshipped but no --add via update-state (non-code goal; current engine + F-0007 guard + F-0019/20 product slices + /status skill + downtime sentinel + /research P1 + /kaizen + 7 schedulers + --validate drift guard + model-policy fresh already enable clean scheduled runs; adding spurious F- would violate "never make-work" + downtime budget). Bullet stays in ROADMAP for operator to mark ✅ shipped when Routine is configured and runs observed clean. Pre-briefs (F-0021 F-0017 close, F-0022 Routine+auth extension) + kaizen + sentinel + STATUS refresh executed instead. Web P1 research (xAI 4/16 + Anthropic sub-agent compaction) recorded; citations in pre-briefs. → this entry + PROGRESS + pre-briefs.
- **2026-06-10** · **--paths scope hardening (security review, PR #16):** the rescope verb could grant a feature the guardrail surfaces, hollowing out the evaluator's mechanical scope check. Now: `.claude/`, `.github/`, `scripts/` globs rejected unconditionally; catch-all (`**`) and parent-traversal (`..`) globs rejected; rescope allowed only for pending/in_progress; new paths may not collide with the feature's own forbidden_paths. Strict-narrowing was rejected as the fix — the verb's legitimate purpose IS widening (F-0002 needed package.json); the denylist guards what must never widen. Coverage guard's glob shortcut also dropped (a script merely containing "*.test." skipped the guard). Five new contract tests. → scripts/update-state.ts, scripts/verify.sh.
- **2026-06-10** · **Evidence-audit marker collision (post-#14 fix):** the deep audit matched `VERIFY: PASS (exit 0)` as a substring, and its own failure message quotes that literal — so a failed run's log (captured while the audit read the half-written file it was tee'd to) self-satisfied the check on CI. Fixed: exact-trimmed-LINE match (a quotation can never be a whole line), two contract tests (quoted-marker log must REJECT; exact-line log accepts), evidence regenerated via temp-file-then-move, and the no-tee-onto-referenced-evidence rule added to /work step 4. Additionally, the commit security review flagged the pinned test file as gate-weakening (future tests silently skipped) — its suggested fixes were the two forms we proved broken (Node 20 vs 24 --test divergence), so verify.sh gained a coverage guard instead: any src test file not wired into the npm test script is a hard failure (negative-tested with a planted file). → scripts/update-state.ts, scripts/verify.sh, PR #15.
- **2026-06-10** · **Node --test portability (F-0002, attempt 1):** bare dir args fail on Windows/Node 24; quoted globs fail on Linux/Node 20 (CI: "Could not find src/**/*.test.js" — glob support landed in Node 21). Resolution: explicit file list in the test script (works everywhere); CI caught what local testing couldn't — the layering working as designed. Future: align CI node-version with local (24) during F-0008's workflow touch. → package.json, PR #14.
- **2026-06-10** · **F-0002 loop proof — three judgment calls:** ① Orchestrator-prepared test script (`node --test src/`) was defective on Windows/Node 24 (bare dir arg not expanded); the builder correctly refused to touch read-only prep, escalated with a verified fix, and the orchestrator applied `node --test "src/**/*.test.js"` — evaluator ratified as decide-and-document. ② Custom agent types (.claude/agents/*) are not loadable mid-session in the local CLI — builder/evaluator mandates were inlined into general agents verbatim; cloud sessions load the roster at start, so no change needed. ③ security-reviewer skipped: no sensitive paths (CLI demo + package.json scripts, zero new dependencies). → roadmap/briefs/F-0002.md, PR #14.
- **2026-06-10** · **Branch protection enabled BY AGENT via gh API (operator-authorized goal):** develop = PR + green "verify" check, enforce_admins; main = same + 1 human approval. The account supports protection on private repos. Q-0001 narrowed to: GitHub App install, ANTHROPIC_API_KEY secret, cloud environment. → repo settings, Q-0001.
- **2026-06-10** · **Optional-modules catalog (operator-directed):** all trigger-gated, non-core ideas from the verified review now live in docs/optional-modules.md, keyed by the repo-state condition that activates them (product code, public flip, branch protection, first external tool, other CLIs, external adopters). Zero always-loaded context; the /downtime sentinel watches the triggers and grooms modules in when one fires — never silently. Core-vs-optional-vs-product decision rule added as plan §2.3 ("Deliberately not used" renumbered to §2.4; no cross-references existed). → docs/optional-modules.md, PR #12.
- **2026-06-10** · **Operator-intent rule (operator-directed):** the operator asked that casual remarks never "mess with the guts" — codified in CLAUDE.md §8: remarks are intent to engineer through the gates, never literal edits; a literal reading that would weaken the core gets the safer shape + a DECISIONS entry. → CLAUDE.md §8.
- **2026-06-10** · **License (operator decision, Q-0002):** MIT — adoption-friendly fit for a template meant to be embedded in downstream repos; LICENSE file + package.json field added. Repo remains private; the license simply makes future sharing/template use legal. → LICENSE, PR #11.
- **2026-06-10** · Model-policy freshness: NO scheduled cron auto-PR (bills the agent-credit pool; violates the no-scheduled-write-automation-before-protection rule) → zero-token staleness check in `update-state --validate` instead (F-0009). → report item #48. `main` created at develop HEAD `2b9f643` via the GitHub API and tagged `v0.1.0`. First promotion is a branch-creation (no diff exists for a PR); future promotions are App-authored PRs develop→main with a qa-pack (plan §7.4). The API path was used because the engine's own hooks block any agent `git push` touching main — deliberate; the operator's explicit instruction is the authorization. `develop` remains the default branch (§6.4). → github.com/michaelcrosato/ai-operations-template/tree/main.
- **2026-06-11** · **F-0012 — first field defect (department-reported, adoption trial #1 / agy-software-2):** seed.ts stub exited 0 unconditionally → `verify.sh --e2e` passed unseeded in product mode in every adopter. Groomed straight into features.json (spec lives in this entry — the department brief is the source; reference fix proven green in the adopter, adapted to template idiom). One deliberate addition beyond the reference: a `SEED_SHIM_ACTIVE=1` recursion sentinel — an adopter wiring `"seed": "ts-node scripts/seed.ts"` would otherwise loop forever in CI. Sentinel ruled NOT a `DANGEROUSLY_` override (setting it can only cause failure, never weaken a gate; same reasoning as the read-only `*_FILE` test seams, 2026-06-10) — security reviewer concurred. → scripts/seed.ts, PR #24.
- **2026-06-11** · **Evidence provenance enforced against own builder:** the builder hand-transcribed seed-shim-demo.log after the sandbox blocked its redirection attempts (it disclosed this honestly in its report). Orchestrator rejected the artifact, recaptured verify.log via a live `npm run verify` and rebuilt the demo log exclusively from live-captured output, with provenance stated inline, BEFORE judging. Root cause is working conditions, not the builder — kaizen candidate: an allowlisted evidence-capture path so agents never face the choice. → roadmap/evidence/F-0012/, /work step 4.
- **2026-06-11** · **`.agents/` + `.codex/` mirrors found on disk, swept into commit 1 by `git add -A`:** generated by local cross-CLI tooling, not by this session's work; the "other CLIs" module is explicitly deferred (docs/optional-modules.md) and must not land as a side-effect of a fix PR. Untracked and gitignored in a follow-up commit on the same branch (no force-push). If the mirrors are wanted, they arrive via their own groomed feature. → .gitignore, PR #24.
- **2026-06-11** · **Backlog state corruption caught pre-CI (F-0012 session):** the real features.json was found carrying two fixture-shaped mutations from scripts/test-hooks.sh's update-state section (F-0001 evidence gained `tmp/hook-tests/verify.log`; F-0004 blocked_reason became "waiting on operator") — committed unnoticed in 2e55c08. Forensics: a full gate run post-repair produced ZERO leakage, so STATE_FILE isolation holds under normal runs; most plausible mechanism is a subagent manually replaying writer commands without the STATE_FILE export while independently verifying. Repair done WITHOUT hand-editing: `git checkout origin/develop -- roadmap/features.json` (restore known-good) + full replay of F-0012 through the writer (--add/--evidence/--passes/--status), then `--validate` green (12 features, 10 passing, evidence re-verified) and a fresh gate run. The deep evidence audit WOULD have failed CI on PR #24 — the layered gate worked; the cost was one extra commit. Prevention groomed as the kaizen item: fixture feature IDs must not collide with real backlog IDs (F-0001/F-0004 → F-9xxx) so any future leak fails loudly ("feature not found") instead of corrupting silently. → roadmap/features.json history on feat/F-0012, PR #24.
- **2026-06-11** · **Kaizen PR #25 review scoping:** fresh-context evaluator skipped — kaizen engine changes have no features.json acceptance criteria to judge against; the mandated layer for hook/gate diffs is the security-reviewer (ran: APPROVE, one low optional finding adopted — row-count snapshot in the leak tripwire), and CI re-runs the full 97-test gate as the independent check. Evaluator stays mandatory for all backlog features. → /kaizen guardrails, PR #25.
- **2026-06-11** · **F-0013 post-hoc record:** the prior session exited while watching PR #27's CI — the watcher died with the process and RECORD never ran. This session merged the green PR and reconstructed PROGRESS/metrics from commits + state (attempts 0; evaluator PASS implied by the protocol-gated state flip in 65422cc; security APPROVE with one adopted hardening, the temp-script EXIT trap named in that same commit). The reconstruction is flagged as such in the metrics note rather than presented as a live record. → PR #27.
- **2026-06-11** · **F-0014 groomed from the department brief (F-0012 precedent):** spec source = department brief + the two proven adopter fixes; ported their union (agy-software-2's broader paths filter + claude-software-3's self-trigger entry). Security review APPROVE, zero findings; its one optional hardening — single-sourcing the actionlint pin as a shared composite action — deferred as a kaizen candidate: it requires touching ci.yml (forbidden for F-0014) and the new lane-parity contract test already pins both lanes to ci.yml's values mechanically. → PR #28.
- **2026-06-11** · **Constitution edit (§9 checklist walked): session-end CI rule.** A session that opens a PR must watch CI to completion (`gh pr checks <n> --watch`) or write an explicit `HANDOFF:` line naming the PR in PROGRESS.md — 3rd portfolio occurrence of the watcher-dies-with-session failure (this repo: PR #27; claude-software-3 adopted the same rule after its own incident; encoded upstream here so every adopter inherits it). Checklist: not path-scopable (global session protocol); no duplicate/contradiction (complements the Stop hook, which sees dirty trees but not pending CI); 49/150 lines after edit; imperative wording; logged here. → CLAUDE.md §6.
- **2026-06-12** · **F-0015 — ship.sh adopted (PR #30, attempts 0):** ported from claude-software-3 (cross-fleet adoption pattern, cf. F-0012/F-0014) to mechanize the §6 watch-then-merge rule. Evaluator PASS (7 criteria); security review ran though ship.sh touches no hooks/workflows — justified because the tool can *merge PRs*, so its fail-closed paths are a guard surface. Two low-sev notes adopted pre-merge (commit 194f79b): ① the base-branch guard now refuses an empty/errored `gh pr view` result, not just literal main/master — a transient view failure can no longer slip a merge through (fail-closed on undetermined base); ② `SHIP_REGISTER_TIMEOUT/INTERVAL` sanitized to integers and interval clamped `>=1` so the register-wait can never busy-spin without advancing toward timeout. +2 contract tests (157 total). → scripts/ship.sh, CLAUDE.md §6, PR #30.
- **2026-06-12** · **F-0015 self-merge restraint:** PR #30 (ship.sh's own introducing PR) was hand-watched and merged, NOT merged by ship.sh — a brand-new merge tool should not be trusted to merge itself before one clean run exists. ship.sh's first real merge was PR #31/F-0016 (eeec562). Convention: a tool that gates merges earns its first live use on the *next* PR, not its own. → PR #30, PR #31.
- **2026-06-12** · **F-0016 — three field defects batched into one polish PR (#31, attempts 0):** (a) record-PR doctrine reworded template-neutral; (b) installer ROADMAP seed no longer duplicates `## Now`; (c) assertion-shield prints a calm "first commit" line instead of `fatal: ambiguous argument` when origin/develop doesn't exist yet. Batched (cf. F-0006) rather than one-PR-each because all three are same-day field-reported install-experience noise sharing the install/shield surface; one-feature-per-PR stays the rule for product features. Mandatory security review (touches the assertion-shield guardrail) APPROVE, zero findings — explicitly confirmed the shield's scan coverage is unchanged: the change only quiets a pre-upstream first-commit case where there is genuinely nothing to diff, it never skips a real diff. → scripts/install-into.sh, scripts/assertion-shield.ts, AI_OPERATIONS_PLAN.md §6.4, PR #31.
- **2026-06-12** · **Constitution edit (§9 checklist walked): post-merge-RECORD rule.** After a feature PR merges, its RECORD step (state flip → PROGRESS + DECISIONS + metrics) must complete in the same session, or an explicit `HANDOFF:` line in PROGRESS.md must name the merged feature. 2nd end-mid-protocol occurrence in this repo (this session reconstructed F-0015/F-0016's records; the first, F-0013, predates the rule). Checklist: not path-scopable (global session protocol); complements §6's CI-pending rule (which covers *before* merge) and the Stop hook (which sees dirty/unpushed trees but NOT a merged-yet-unrecorded feature) without duplicating either; ~50/150 lines after edit; imperative wording; logged here. → CLAUDE.md §6.
- **2026-06-12** · **Kaizen candidate (not built — brief scoped this session to RECORD only):** add a SessionStart tripwire that flags any feature `done` in features.json with evidence on disk but absent from roadmap/metrics.jsonl — the mechanical loud signal that RECORD never finished. Note: the brief's literal framing ("in_progress features whose PRs are merged") does NOT fit this repo — state flips ride the feature PR, so a merged feature is already `done`, not `in_progress`; the metrics-gap check is the shape that actually catches the F-0015/F-0016 failure mode. → backlog/kaizen, /downtime sentinel.
- **2026-06-12** · Completed path-guard feature (F-0007), resolving shellcheck warnings and standardizing shell execution of hook contract tests using "$BASH" instead of "bash" to prevent WSL Bash hijacks on Windows. Reversible. → .claude/hooks/path-guard.js, .claude/hooks/path-guard.sh, scripts/test-hooks.sh, scripts/verify.sh.
- **2026-06-13** · **Codex operations review:** current official docs confirm Codex uses `AGENTS.md` for project instructions while this repo remains Claude-first with `CLAUDE.md` as the single instruction source; the thin AGENTS pointer is still the right tracked shape. Clarified F-0004's blocked reason through `update-state.ts` to name the current `CLAUDE_CODE_OAUTH_TOKEN` setup. Ignored local `.agents/`/`.codex/` mirrors had stale `.Codex/*` path text; fixed locally for this workspace, but they remain ignored and must not become durable repo surface without a groomed cross-CLI feature. → AGENTS.md, roadmap/features.json, .gitignore.
- **2026-06-13** · **F-0018 Visual Canvas unblocked and merged:** Resolved Next.js compile errors for named exports in `@xyflow/react` and ignored TypeScript side-effect typecheck warnings for CSS style imports. This unblocked PR #40 and allowed merging F-0018 after successfully merging F-0007 and F-0020. → app/demo/page.tsx, app/layout.tsx, package.json.
- **2026-06-13** · **Reviewed + merged codex/fix-local-cli-hook-preflight (local Windows CLI preflight):** CI caught two issues a local-green run missed. ① The branch's own WSL-detection contract test inherited CI/GITHUB_ACTIONS and so tripped the preflight's `is_ci` suppression — green locally, red under CI; fixed by clearing both vars for that one invocation (the path under test is non-CI by definition). ② A pre-existing flaky race surfaced (not introduced by this branch): `node --test` ran the forge test files in parallel and `abSim.test.js` reads `roadmap/evidence/F-0020/graph.json` while `exportArtifacts.test.js` truncate-writes it → an empty copy landed in `F-0017/graph.json` and `--validate` rejected it. Fixed at the gate with `--test-concurrency=1`: the writers (promptToGraph→F-0019, exportArtifacts→F-0020) are listed before the reader (abSim), so serial execution guarantees fully-written sources with zero change to evidence content. Folded into this branch (maintenance session, no active feature) to unblock the merge; the ordering dependency is recorded here so a future test-list reorder is informed. Also split the preflight failure message: `fail_wsl` keeps the Windows/Git-Bash recovery steps for the shell-detection path, `fail_missing` gives platform-neutral install/PATH guidance for the node/git check (which also runs on Linux/macOS). Reversible. → scripts/local-cli-preflight.sh, scripts/test-hooks.sh, package.json, PR #42.
- **2026-06-14** · **Forge evidence tests made order-/concurrency-independent (root-cause follow-up to PR #42's `--test-concurrency=1`):** `abSim.test.js` copied sibling tests' evidence files (`F-0019/sample-graph.json`, `F-0020/{graph.json,Dockerfile,docker-compose.yml}`) into `F-0017/` at module load, so a parallel `node --test` could read a source mid-truncate-write and land an empty `F-0017/graph.json` (the flake masked at the gate by `--test-concurrency=1`, which only works because the writers are listed before the reader — fragile under the planned Phase-1 glob runner). Now those artifacts are derived IN-PROCESS from the same functions their own tests use — `promptToGraph('Research X and summarize')` and the side-effect-free `minimalGraph()/dockerfileContent()/dockerComposeContent()` — so the F-0017 emission depends on neither file-write order nor concurrency. Two judgment calls: ① used the PURE export functions, not `exportArtifacts()` (the literal recommendation), because `exportArtifacts()` writes F-0020 to disk and would re-introduce a write/read race against `exportArtifacts.test.js`; the pure functions return byte-identical content with zero I/O (SHA-256-verified against committed evidence: graph.json 571B, sample-graph.json 1304B, Dockerfile/compose identical). ② KEPT `--test-concurrency=1` as defense-in-depth — `abSim.js`'s CLI (run by abSim.test.js's CLI test in a child process) still calls `exportArtifacts()` and writes F-0020, a residual subprocess-level write race the in-process read fix does not cover; full parallel-safety would need atomic writes in the source emitters, out of scope here. Proven: 25× parallel `node --test` all green with `F-0017/graph.json` stable at 571B + `bash scripts/verify.sh` PASS + fresh-context evaluator PASS. Reversible. → src/forge/abSim.test.js (package.json unchanged), PR (this branch).
- **2026-06-14** · **5-source external-review remediation (operator `/goal`):** 89 consolidated review points were live-verified against the real tree (multi-agent fleet + direct inspection) and cross-checked with `feedback-2-verification.md` + fresh web research (`docs/ai-capabilities-research-2026-06.md`). ~52 confirmed, 0 false, ~14 already-fixed, rest by-design/deferred. Judgment calls: ① **safe subset implemented in one `fix/` PR**, not nine — they share surfaces and one gate cycle; large/risky items **groomed as F-0021/F-0022/F-0023** rather than done casually (constitution + verified plan both say groom). ② **Public-repo governance batched into this PR** rather than groomed separately: the public trigger has fired (`docs/optional-modules.md`), the files are pure-additive docs with zero gate impact, and grooming five doc files through the full loop is disproportionate — logged loudly here instead of "adopted silently". ③ **postcss** fixed as a direct-dep bump to `^8.5.10` + `overrides` (npm rejected an override alone — postcss is a direct devDep); `npm audit --omit=dev` → 0 vulns. Reversible. → many paths, this PR; docs/feedback-3-verification.md.
- **2026-06-14** · **Landing-page copy honesty (decide-and-document per `.claude/rules/frontend.md`):** removed fabricated proof from `app/page.tsx` — fake customer-logo wall (real company names → reputational/legal risk), "4,812 workflows shipped this week", "Facts verified live 2026-06-12", "realistic production data" → synthetic/demo wording; "Grok-powered swarms" → "multi-agent swarms"; `package.json` description de-marketed. The competitor comparison table is **kept** but re-captioned "illustrative … not independently verified" (neutralizing it entirely is left to the operator — Q-0003). Reversible. → app/page.tsx, package.json.
- **2026-06-14** · **GitHub security posture enabled out-of-tree (operator goal):** turned on vulnerability alerts, Dependabot security updates, secret scanning, secret-scanning push protection, and private vulnerability reporting (all were disabled). Pure-upside, reversible repo settings; resolves the HIGH "alerting disabled" finding. Push protection could in principle block a future push containing a secret-shaped string — acceptable (that is the point) and reversible. → GitHub repo settings, SECURITY.md.
- **2026-06-14** · **Model-currency fix split (grok-4 stale):** product code hardcodes `grok-3`/`grok-4` in ~57 places; `grok-4` is verified stale (now `grok-4.3`). The forge-logic change regenerates committed audit evidence, so it is **groomed as F-0023** (proper evaluator byte-match) rather than rushed; the *engine* `.claude/model-policy.json` is already correct (tier aliases) and deliberately left unchanged. Landing-page model copy was de-Grok'd to model-agnostic wording now to avoid a stale public claim. Reversible. → docs/feedback-3-verification.md, F-0023.
- **2026-06-14** · **F-0021 build (attempts 2) — RBAC enforcement + role model + UI proof:** ① The security-reviewer caught a real privilege-escalation in two passes — first the `editor` branch ignored the resource arg (could MUTATE org/billing that admin is denied), then a residual READ-side escalation (editor could READ org/billing because the read-allow ran before the org/billing denial). Fixed: editor branch denies ORG_BILLING_RESOURCES first (incl. read), so editor never exceeds admin; added a **monotonicity property test** (lower-role 'allow' ⟹ higher-role 'allow' across a resource×action matrix) — the test shape that catches this whole class. ② F-0021's `authorized_paths` were under-scoped at groom time (missed the engine `canEdit` predicate + its test); widened via the sanctioned `update-state --paths`. Removed the dead, unused `lib/rbac-helpers.ts`. ③ **AC3/AC4 reconciliation (decide-and-document):** the groomed AC literally said "Playwright E2E + verify.sh --e2e", but the governing rule `.claude/rules/frontend.md` accepts "the E2E run **OR** screenshots from walking it like a user." F-0021 ships screenshot + DOM-asserted walkthrough evidence (`roadmap/evidence/F-0021/`); the full Playwright `--e2e` program (incl. e2e.yml browser install) is groomed as **F-0024** rather than bundled, because standing up CI-green Playwright is a feature in its own right and `e2e.yml` (browser install) is a guard-surface change. Reversible. → src/forge/rbac.js, src/forge/rbac.test.js, app/demo/page.tsx, lib/seed.ts, F-0024.
- **2026-06-14** · **F-0027 (AST assertion-shield) attempt 1 — security-reviewer BLOCK; NOT merged, deferred with corrected design.** The build replaced the shield's regex line-diff deletion scan (for TS/JS files) with an AST *assertion-count* comparison (BASE vs HEAD). The security-reviewer proved this STRICTLY WEAKENED the guard for the most common weakening: gutting an assertion's content while keeping the call count constant (e.g. `expect(compute()).toBe(42)` → `expect(true).toBe(true)`) passed the new shield but is blocked on develop, because count-only checks never see content. The branch was discarded (not merged); develop keeps the sound regex shield. **Corrected design for the next attempt (the count approach cannot satisfy both "no rename false-positive" and "block content-gutting" for an existing file — security must win):** keep the strict regex deletion/weakening scan unchanged for every file that EXISTED on origin/develop, and relax (suppress) flagging ONLY for files that are new-in-branch (absent on BASE — removing/editing them cannot reduce pre-existing coverage). That provably fixes the real false positive that bit this session (removing an in-branch-added test file) with zero weakening, and leaves existing-file content-gutting fully caught. Lesson: never trade guard strictness for fewer false positives; an evaluator PASS is not sufficient for a guard change — the adversarial security-reviewer is the binding check. Reversible. → scripts/assertion-shield.ts (unchanged on develop), F-0027 spec.
- **2026-06-14** · **F-0024/F-0027/F-0030 shipped + F-0027 attempt-2 merged (corrected design).** F-0024 stood up the Playwright E2E program (browser-level viewer-RBAC test; e2e.yml installs browsers; CI e2e job green). F-0030 broadened the command guards to cross-shell/indirection forms (attempt 1 over-blocked nested relative paths and was BLOCKED; attempt 2 anchored the target to a path boundary like the rm -rf rule). F-0027 attempt 2 landed the corrected, non-weakening shield fix (suppress only for branch-new files; gutting an existing assertion still blocked — proven by a new contract test). Three security-reviewer BLOCKs across F-0021/F-0027/F-0030 this session were all real and all fixed before merge — the adversarial JUDGE layer is doing its job. → PRs #53/#54/#55.
- **2026-06-14** · **F-0026 (test the LLM judges) intentionally NOT built this session — deferred per optional-modules.md.** The MECHANICAL judges (assertion-shield, path-guard, update-state, guard-bash) are deterministically tested in the contract suite and were extended this session (F-0027 gutting/branch-new cases; F-0030 cross-shell evasions). The remaining gap is behavioral testing of the LLM judge PROMPTS (evaluator/security-reviewer), which is inherently non-deterministic and not gate-able in verify.sh; the lean shape is an advisory promptfoo lane needing provider setup + eval budget. optional-modules.md "Full eval suite" trigger defers this until the loop is proven AND eval budget is accepted. F-0026 stays pending (groomed) rather than shipping unprovable scaffolding. → optional-modules.md, F-0026.
- **2026-06-14** · **Kaizen candidate (recurring CI/test flake): exit-127 from `npx ts-node` in the update-state contract tests under load.** Several post-commit verify runs this session hit transient `[FAIL] ... (expected exit 1, got 127)` on the update-state.ts test block (a few re-ran green immediately). Root cause: those tests invoke `npx ts-node` which intermittently fails to resolve under concurrent/sandboxed runs; the assertion-shield tests avoid this by calling the node ts-node bin directly (`$TSNODE="$ROOT/node_modules/ts-node/dist/bin.js"`). Fix candidate: switch the update-state contract tests to the same direct-bin invocation. Not built here (out of the current features' scope). → scripts/test-hooks.sh, /kaizen.
- **2026-06-14** · **F-0031 (dependency rationalization) shipped (PR #57).** Removed 4 unused runtime deps (zod, cmdk, date-fns, react-hook-form — zero imports anywhere; supersedes "zod 3→4", an unused dep is removed not upgraded). Upgraded lucide-react ^0.511→^1.18.0 (33 used icons verified present in 1.x, no renames) and @biomejs/biome ^2.4.16→^2.5.0 (lint clean). **Deferred Next 15→16** (the one high-risk core major; security floor Next ≥15.5.18 + postcss ≥8.5.10 already met) to its own focused cycle. Security-reviewer APPROVE: removals confirmed unused, upgrades registry-verified (integrity hashes match, no typosquat/install-scripts), npm audit --omit=dev = 0, Next not downgraded. → package.json, PR #57.
- **2026-06-14** · **F-0029 (page.tsx decomposition) — incremental slice shipped (PR #58), full decomposition deferred.** Centralized the demo's permission logic into a `useDemoAuth` hook (delegates to the existing lib/seed.ts predicates — no policy change), extracted the MetricsCards + LiveTickerPanel components, and added 16 permission unit tests. Behavior preserved (F-0024 E2E viewer/owner RBAC still green; all data-testids intact). The InteractiveCanvas + graph-mutation handlers were deliberately LEFT in page.tsx: they close over a dozen state atoms with no clean seam — a full `useWorkflowCanvas`/context restructure is risky and is a follow-up. Scope-spec fix: F-0029 authorized_paths widened (`--paths`) to include `src/forge/**`+`package.json`, since all unit tests live there (the groomed paths omitted them; evaluator flagged this as a spec defect, not builder misbehavior). → app/demo/_hooks, _components, PR #58.
- **2026-06-14** · **F-0028 (src/forge CJS→TS migration) BLOCKED — structural guard constraint (real finding).** A clean migration must rename `src/forge/*.test.js` → `*.test.ts`, but that deletes BASE-existing test assertions, which the assertion-shield blocks (F-0027's suppression only relaxes for branch-NEW files). A source-only migration (keep `.test.js`, point them at `.ts`) is blocked by CJS/ESM test-runner interop (a `.test.js` CJS `require` of an ESM `.ts` is fragile under `node --test`). The builder's attempt kept both `.js` and `.ts` (duplication, not a migration) — rejected, branch discarded. This concretely vindicates the external reviews' "the shield makes refactoring hard / bypass fatigue" finding. Unblock path: extend the assertion-shield with rename-detection (a deliberate follow-up to F-0027 — match a deleted assertion against an added identical one in a renamed file), or a human-authorized one-off migration bypass. Until then F-0028 stays blocked. → src/forge (unchanged), F-0027 follow-up.

- **2026-06-14** · **F-0026 (test the judges) shipped (PR #60).** Delivered the gate-able half: a labeled "known-bad corpus" in `scripts/test-hooks.sh` proving each *mechanical* judge rejects a known-bad input (weakened-assertion→assertion-shield; out-of-scope edit→path-guard; invalid state mutation→update-state; evasion command→guard-bash) — +8 checks (248→256). The non-deterministic *LLM-judge* eval is documented as a deferred **advisory** promptfoo lane (`tests/judges/README.md`) with fixtures staged under `tests/judges/inputs/` (weakened→NEEDS_WORK, seeded-IDOR→BLOCK, irrelevant-evidence→NEEDS_WORK, clean→PASS), per optional-modules.md "Full eval suite" trigger (activates on eval budget). No promptfoo dependency; not in verify.sh; seeded-vuln fixtures are inert `.patch` data. Evaluator's sole finding (advisory doc was outside authorized_paths, and `--paths` refuses `scripts/` guard surfaces so scope couldn't cover both) resolved by relocating the doc into the in-scope `tests/judges/`. Security-reviewer APPROVE. This supersedes the earlier "F-0026 DEFERRED by design" stance: the deterministic slice was buildable now; only the LLM-eval half remains deferred. → scripts/test-hooks.sh, tests/judges/**, PR #60.
- **2026-06-14** · **Shield rename-detection shipped (PR #61) — UNBLOCKS F-0028.** Added `-M` (git rename detection) to the assertion-shield's three `git diff` invocations so a pure `.test.js`→`.test.ts` rename is recognized as a move (no `-expect` lines → PASS) instead of a wholesale assertion deletion, while a rename that *also* removes an assertion is still flagged (the `-` line surfaces under the old base-existing path). Security-reviewer APPROVE with a full adversarial laundering walk-through: confirmed a strict superset of prior behavior (only `-M`, not `-C`; parser/F-0027/F-0009/bypass logic byte-identical; no rename/split/move/copy sequence lets a base-existing assertion disappear without a surfacing `-`). +3 hook tests (248→251). This is the F-0027 follow-up the prior F-0028 block named; the TS migration can now proceed. → scripts/assertion-shield.ts, PR #61.
- **2026-06-14** · **Q-0003 resolved by decide-and-document (CLAUDE.md §4), not left waiting.** (1) **Brand voice:** neutralized the comparison table further — removed specific competitor pricing figures and softened two loaded characterizations ("retention upsell"→"Tracing-focused"; "Typical vendor lock"→"Hosted SaaS"); kept named competitors + structure + the "illustrative, not verified" caption. Removes the assert-specific-competitor-prices risk; reversible. (2) **Priority of remaining work:** recommended sequence — F-0028 TS migration (now unblocked) → full page.tsx decomposition → major dep upgrades (individual PRs) → verify.sh Node port. (3) **Major dep upgrades:** a dedicated "dependency modernization" cycle of one-major-per-PR scoped migrations, no urgency (security floor met), re-verify each via /research at start. All three are reversible operator-overridable defaults. → app/page.tsx (copy), QUESTIONS.md, DECISIONS.md.
- **2026-06-14** · **Shield module-decl false-positive fix + harness de-flake shipped (PR #63).** The CJS→ESM migration deletes `const assert = require('node:assert')` import lines, which the over-broad `'assert '` keyword mis-flagged as deleted assertions. Added a skip for pure CJS module declarations. **The first regex was correctly BLOCKED by security review**: `/^...=\s*require\(/` was unanchored at the end, so `const _ = require('x'); expect(role).toBe('admin')` matched the prefix and would let a real deleted assertion slip past — a genuine bypass. Fixed with a fully both-ends-anchored pattern (require must be the entire RHS; single bare-ident or one `{...}` destructure; optional `;`; EOL) + a negative test proving the weaponized prefix line stays BLOCKED. Re-review APPROVE (17-case adversarial set). Also de-flaked the harness: 10 `npx ts-node` call sites → the direct `$TSNODE` bin (npx intermittently returned exit 127 under concurrent load and coin-flipped the gate). Invocation-only; +4 hook checks. → scripts/assertion-shield.ts, scripts/test-hooks.sh, PR #63.
- **2026-06-14** · **F-0028 (src/forge CJS→TS migration) shipped (PR #64) — was BLOCKED, now DONE.** Unblocked by the shield rename-detection (#61) + module-decl FP fix (#63). Migrated 12 files to TypeScript (tsc --noEmit now covers the engine via `allowImportingTsExtensions`), removed the duplicate `promptToGraph` heuristic from `lib/seed.ts` so the demo uses the single forge source (thin typed adapter). Node 24 runs the `.test.ts` natively (explicit `.ts` import extensions — extensionless fails; probed before building). Security-reviewer APPROVE: rbac.ts/canEdit.ts byte-equivalent (no privilege change), no new dependency, only the pure `promptToGraph`+`models` reach the client bundle. Evaluator caught a CLI-test tautology (`const stdout='exported...'; assert.includes('exported')`) — fixed by relocating the removed CLI's stdout summary onto `exportArtifacts()`'s real return (derived from the artifact list); evaluator mutation-verified the fix is a real regression test. **Builder out-of-scope changes disentangled at orchestrator level:** the builder had bundled a shield edit (→ extracted to the reviewed #63, branch reverted to develop's shield) and hand-edited features.json; the only kept features.json change is F-0017's evidence paths `.js`→`.ts` (a forced rename consequence — those source files moved — tool-validated, no status/passes touched). → src/forge/**, lib/seed.ts, tsconfig.json, package.json, PR #64.
- **2026-06-14** · **Session cleanup: gitignore ephemeral worktrees + refresh churning evidence snapshot.** (a) Added `.claude/worktrees/` to `.gitignore` — workflow/Agent worktree isolation auto-creates/removes these, and tracking them tripped the Stop hook with transient dirty state. (b) `src/forge/abSim.test.ts` re-emits F-0019/F-0020 evidence each run; the F-0028 migration shifted a sample prompt so the committed `roadmap/evidence/F-0019/sample-graph.json` no longer matched the deterministic output → churn on every gate run. Refreshed the committed snapshot once to match (it's "never read back" per the test's own comment), making it stable. → .gitignore, roadmap/evidence/F-0019/.
- **2026-06-14** · **Kaizen candidates logged (not built this session, out of scope).** (1) The exit-127 `npx ts-node` flake is now fixed in test-hooks.sh (#63), but a *residual* transient remains: ts-node compiles on each invocation, so under very heavy concurrent load (e.g. `verify.sh --e2e` alongside parallel agents) an update-state test can still rarely report 127 — candidate fix: precompile or transpile-only the state scripts. (2) Forge tests emit into committed evidence dirs (F-0017/F-0019/F-0020) as a side-effect — candidate fix: make emission idempotent/skip-if-present or write to a non-committed scratch path, so gate runs never dirty the tree. → /kaizen, scripts/test-hooks.sh, src/forge/*.test.ts.
- **2026-06-14** · **RBAC input contract hardened to fail-CLOSED (operator-directed defensive hardening).** A prior adversarial review confirmed `check()`'s old behavior was fail-SAFE (empty action `''` granted READ only, never mutation; org/billing denied first; padded principals default-denied) — NOT an escalation. This is optional hardening, NOT a vuln fix: (a) added a `KNOWN_ACTIONS` allowlist (folds in `RUN_MANAGE_ACTIONS` so the sets can't drift) and made the bounded roles editor/viewer `deny` on empty/unknown actions instead of read-defaulting — an unknown verb can no longer slip through as a graph/template mutation for editor; (b) `.trim()` on principal/res/act so a padded-but-valid principal resolves to its intended (still gated) role and — the one real fail-open this closes — a padded resource `'billing '` can no longer miss the exact-match org/billing denial (previously gave admin-allow / editor-read). **Judgment calls:** ① Scoped the action-allowlist to editor/viewer per the request; owner/admin keep their documented allow-all (resource-gated) contract — security-reviewer confirmed no material residual fail-open. ② Implemented directly + fresh-context security-reviewer rather than the full builder/evaluator F-XXXX feature cycle: this is an operator-directed `fix/` with an exact spec, and the binding gates for a guard change (full `verify.sh` + security-reviewer) were both honored. ③ Evidence stored under `roadmap/evidence/fix-rbac-failclosed/` (no F-id). Monotonicity (lower allow ⟹ higher allow) preserved — proven by an added property test over empty/unknown/padded inputs and an exhaustive security-reviewer sweep (0 violations). All existing rbac.test.ts assertions byte-unchanged; +5 additive tests. Reversible. → src/forge/rbac.ts, src/forge/rbac.test.ts, fix/rbac-failclosed-input-contract.
- **2026-06-14** · **F-0032 (complete app/demo/page.tsx canvas decomposition) shipped (PR #68).** Finished the decomposition F-0029 deferred: moved `GraphViz` + `InteractiveCanvas` (≈340 lines) out of the 1742-line page.tsx into `app/demo/_components/InteractiveCanvas.tsx` → page.tsx **1742→1382 (−360)**. Pure mechanical, behavior-preserving move (evaluator confirmed byte-equivalent, call-site props identical, every E2E data-testid + the viewer-RBAC gating preserved 1:1, no test touched, no new dep). Frontend-only refactor → evaluator JUDGE only (no security surface). E2E + 50 unit tests green. → app/demo/page.tsx, app/demo/_components/InteractiveCanvas.tsx, PR #68.
- **2026-06-14** · **F-0033 (Next.js 15→16 major upgrade) shipped (PR #69) — last deferred backlog item, now DONE.** Bumped next ^15→^16.2.9, react/react-dom→19.2.7, @types latest. Next 16 makes **Turbopack the default bundler**; researched the official upgrade guide live (freshness rule §5) and audited our exposure as NARROW (no middleware, no async-request-APIs, no next/image, biome not eslint, no AMP/getConfig/sitemap/opengraph/parallel-routes). Two required fixes: removed the no-op `webpack()` from next.config.ts (a custom webpack config breaks the Turbopack-default build) and made lib/seed.ts's forge import extensionless (Turbopack-safe; intra-forge `.test.ts` imports keep `.ts` for node --test). Security-reviewer APPROVE: only new transitive dep is `baseline-browser-mapping` (next@16-mandated, registry-clean, no install scripts), `npm audit --omit=dev`=0; evaluator PASS (Turbopack build + E2E green). **Bonus kaizen:** root-caused the residual exit-127 flake — Next 16 rewrites tsconfig `jsx: preserve→react-jsx`, whose slower per-invocation compile triggered a Windows libuv `UV_HANDLE_CLOSING` race under concurrent ts-node load — and fixed it by pre-committing the tsconfig values + `ts-node: { transpileOnly: true }` (runtime type-check skipped for scripts; `tsc --noEmit` still type-checks them in the gate, so no safety loss). Supersedes the Q-0003 "Next 16 = greenlight-gated, no urgency" deferral. → package.json, next.config.ts, lib/seed.ts, tsconfig.json, PR #69.
- **2026-06-14** · **npm audit: 5 moderate `file-type` advisories cleared (dev-tooling only).** GHSA-5v7r-6r5c-r473 (ASF parser infinite loop) + GHSA-j47w-4g3g-c36v (ZIP decompression-bomb DoS) reached the tree as `file-type@20.5.0`, pulled transitively by the `shellcheck` wrapper's modern `@xhmikosr/decompress-*` extraction chain. Prod was already clean (`npm audit --omit=dev` = 0); only `shellcheck` (a devDependency) was affected. Two fixes weighed: **(a)** force `file-type` to a patched `>21.3.1` via `overrides` — rejected: no patched 20.x line exists, so this forces an *untested major* (20→21/22) onto the wrapper's extraction lib, and the shellcheck binary download is 403-blocked in this container so the override couldn't be runtime-verified; **(b)** npm's blessed `fixAvailable` — pin the wrapper to `shellcheck@^3.1.0`, which uses the legacy `decompress@4.2.1` chain (old file-type@3/5/6, below the vulnerable floor → unflagged). Chose **(b)**. Critical finding: both wrapper 4.1.0 and 3.1.0 default `SHELLCHECKJS_RELEASE` to the **same `latest` shellcheck binary**, so the "downgrade" changes only the npm-side archive-extraction tooling, not the analysis binary — lint behavior is identical (verified: every non-environmental verify.sh stage stayed green, and a clean-tree baseline run produced byte-identical pass/fail sets). Result: `npm audit` 0/0. Reversible — bump back to 4.x once a shellcheck 4.x release ships a patched file-type. → package.json, package-lock.json, roadmap/evidence/security-shellcheck-audit/.
- **2026-06-14** · **Branch-naming reconciliation for this fix.** Constitution §6 prefers `fix/...` feature branches; the web-session harness mandates developing on its designated branch (`claude/adoring-lovelace-2kx8l8`) and forbids pushing elsewhere without explicit permission. Resolved by honoring the harness branch (the more specific, operator-set constraint) while still opening the PR **into `develop`** as a draft per §6 — the `fix:` nature lives in the PR/commit title, not the branch name. Reversible. → this PR.

- **2026-06-14** · **F-0034: path-authorization consolidation (3 audit holes closed; security-reviewer APPROVE).** The v2 forensic audit found three real authz holes: (1) verify-gate.sh's matcher never resolved `..`, so `src/../package.json` escaped an `src/**` scope; (2) path-guard.js failed OPEN on an unknown active-feature id (set CLAUDE_ACTIVE_FEATURE to any unknown id → every edit waved through) and on env-set-but-missing-state; (3) path-guard.js's matchGlob corrupted `**`→`.[^/]*` (the `*`→`[^/]*` pass mangled the `.*`), so `src/**` never matched a nested file like src/forge/abSim.ts. Fixes: path-guard.js rewritten — NUL-sentinel glob so `**` crosses `/`, and run() FAILS CLOSED on unknown id, duplicate id, asserted-but-missing/unparseable state, and empty authorized_paths (was fail-open); verify-gate.sh canonicalizes the file path via path.posix.normalize (resolves `..`) before matching. An opus security-reviewer empirically fuzzed 20+ traversal/encoding variants (backslash, absolute, leading ../, src/./../) — zero false-allows; APPROVE with one MEDIUM fixed in the same cycle: verify-gate's matches_any over-matched a dir/** scope by segment-containment (an `src/**` scope wrongly allowed foo/src/secret.ts) — tightened to anchored matching mirroring path-guard.js. +15 contract tests (264→279) proving traversal blocked, nested-`**` allowed, and fail-closed parity in BOTH guards. Driven from the department-manager session as a fix/ PR (not the /work loop) under the operator /goal hardening pass. DEFERRED (logged, not built): the jq-only/no-node bash fallback in matches_any still over-blocks nested paths (fail-safe; node is a hard dep) — drop or anchor it; collapse the two matchers into one shared module. Reversible. → .claude/hooks/path-guard.js, .claude/hooks/verify-gate.sh, scripts/test-hooks.sh, this PR.
- **2026-06-14** · **F-DM1: evidence hermeticity (kills the documented flake class).** Every flake in this log (the F-0017 graph.json truncate-write race, the empty-file --validate failure, the --test-concurrency=1 band-aid, per-run dirty-tree churn fighting the Stop hook) traced to ONE root cause: src/forge product + test code wrote into git-tracked roadmap/evidence/ at runtime — evidence regenerated by the very run it attests. Fix: src/forge/{exportArtifacts,abSim}.ts made PURE (return artifacts, zero disk I/O); the three forge test files' evidence-write side-effects removed (assertions untouched — exportArtifacts.test.ts now reads the committed golden artifacts read-only); --test-concurrency=1 removed from package.json (explicit file list kept so src/health.test.js is not dropped); a mechanism-agnostic regression guard added to test-hooks.sh asserting `git status --porcelain roadmap/evidence` is empty after the unit tests. Committed F-0017/F-0019/F-0020 goldens unchanged (no stranded evidence). Proven: verify.sh PASS; npm test 5x at default concurrency all green with ZERO evidence churn; fresh-context evaluator PASS (empirically confirmed the guard FAILS on an injected write and skips outside a git tree); assertion-shield green (no test assertion deleted — counts byte-identical 16/16, 12/12, 23/23). Driven as a fix/ PR from the department session under the operator /goal hardening pass. Reversible. → src/forge/exportArtifacts.ts, src/forge/abSim.ts, src/forge/{promptToGraph,exportArtifacts,abSim}.test.ts, package.json, scripts/test-hooks.sh, this PR.
- **2026-06-14** · **F-SM2: state-writer closed shape + type guards + non-clearable forbidden_paths (security-reviewer APPROVE).** Audit state-model finding: features.schema.json was decorative (nothing loaded it; AI_OPERATIONS_PLAN claimed "validated in CI" — false), and --add spread `...incoming` LAST so a caller could clear the guardrail forbidden_paths defaults AND inject arbitrary keys (a typo like `passe:true` or junk survived every gate; validate() only checked required-key PRESENCE). Fixes in update-state.ts: (1) KNOWN_KEYS allow-list — validate() rejects any field outside the schema's 15 keys (closed shape); (2) per-field type guards (string/array-of-strings/integer/boolean/null) with continue-past-value-checks on fundamental type breakage so a malformed hand-edit reports cleanly instead of crashing CI; both dependency loops guarded against a non-array dependencies; (3) --add unions forbidden_paths with SAFETY_FORBIDDEN (.claude/**, .github/workflows/**) AFTER ...incoming, so caller input can only ADD, never clear. features.schema.json gains additionalProperties:false; a cross-check contract test binds KNOWN_KEYS<->schema (15-key set) so the schema is no longer decorative; AI_OPERATIONS_PLAN line 87 claim corrected. +7 contract tests (279->286). Opus security-reviewer APPROVE after 30+ adversarial probes: __proto__/constructor smuggling rejected (JSON.parse makes them own-enumerable -> Object.keys sweep catches them; no real prototype pollution), every prior invariant still fires, type guards fail-safe without throwing, the union is truly non-clearable, the continue is not a bypass (errors already pushed -> exit 1; cross-feature checks run in later loops). Real 33-feature backlog still validates (exit 0). LOW non-blocking: a continue'd malformed row's id is not dup-tracked (harmless — it already fails hard); REQUIRED + the TS Feature interface remain hand-maintained (KNOWN_KEYS<->schema is the security-relevant pair, now test-bound). Driven as a fix/ PR under the operator /goal hardening pass. Reversible. → scripts/update-state.ts, roadmap/features.schema.json, scripts/test-hooks.sh, AI_OPERATIONS_PLAN.md, this PR.
- **2026-06-15** · **F-EC1: sandbox-safe evidence-capture tool + closed a laundering hole the adversarial review caught.** DECISIONS 2026-06-11 logged a builder HAND-TRANSCRIBING a log because the sandbox blocked `> file` — the exact integrity failure the gate exists to prevent. Shipped scripts/capture.mjs (+ thin capture.sh wrapper): runs a command via a node child process (shell:false, NO redirection), tees stdout+stderr to roadmap/evidence/<F-XXXX>/<name>.log with a provenance header (CAPTURED-BY/CAPTURE-FEATURE/CAPTURE-CMD/CAPTURE-COMMIT/CAPTURE-TS/CAPTURE-EXIT), and propagates the child's real exit code. Plain ESM (no ts-node) for sandbox robustness; EVIDENCE_ROOT is a test seam (cannot satisfy the real gate — verified). **The first security-reviewer BLOCKED on a real HIGH it proved end-to-end:** collectEvidenceErrors counted "green" purely on the VERIFY: PASS (exit 0) marker line and NEVER read CAPTURE-EXIT, so a FAILING command echoing that marker laundered a red run into passes:true. Fixed two-sided (defense-in-depth): (1) GATE — a CAPTURED-BY:capture.mjs log now counts green ONLY with CAPTURE-EXIT: 0; (2) CAPTURE — a nonzero-exit capture neutralizes any echoed marker line (byte-identical predicate to the gate, no asymmetry). A FRESH security-reviewer re-reviewed and APPROVED: laundering closed against every evasion (trailing spaces / CRLF / stderr / a child forging CAPTURE-EXIT:0 into its own stdout), the two halves independent, the normal raw-verify-log path preserved (37 goldens + real 33-feature backlog validate exit 0), no new bypass (the only residual — a hand-edited log stripping CAPTURED-BY — is the pre-existing CI-backstopped hand-edit threat, not worsened). +9 contract tests (286->295: 6 capture-tool + 3 laundering). FOLLOW-UP (T-EC2, deferred): make capture the SOLE writer into roadmap/evidence (PreToolUse guard) + require CAPTURED-BY at --passes. feat/ PR under the operator /goal pass. Reversible. → scripts/capture.mjs, scripts/capture.sh, scripts/update-state.ts, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **F-TC4: mutation-smoke gate — proves the safety-critical tests have teeth ("green != correct"); security-reviewer BLOCK→fix→verified.** Critic flagged the engine's weakest oracle: no mutation/anti-tautology testing, so a vacuous test passes against broken code undetected. Shipped scripts/mutation-smoke.sh + a verify.sh stage: applies 4 known mutations IN-TREE to rbac.ts (the privilege-escalation guard — strongest tests incl. a monotonicity property test), runs the guarding test, requires each mutant KILLED; a survivor fails the gate. In-tree is mandatory (critic's T-TC4): tests import their SUT by literal relative path, so a tmp-copy mutant loads the original and always "survives." Safety: cp-backup per mutation + a cleanup trap (EXIT/INT/TERM) + a pre-run-baseline cmp backstop + a baseline-green check + a SUT-exists fail-closed guard. A --selftest runs a known survivor through the real mutate() and is contract-tested so the meta-gate cannot go blind. **Security-reviewer BLOCKED (HIGH):** the original backstop compared to git HEAD, so an agent with legitimate uncommitted WIP on rbac.ts was FALSE-FAILED (WIP differs from HEAD even though restoration was byte-perfect) — breaking the "runs constantly with WIP" case. Fixed: backstop now compares against a PRE-RUN cp-backup (working-tree, WIP-inclusive), not HEAD; plus the baseline-green + SUT-exists guards (reviewer #3/#5); also fixed a self-inflicted bug where the cleanup logic deleted the comparison baseline mid-run. Verified the reviewer's exact scenario: WIP-dirty rbac.ts -> gate PASSES, WIP preserved byte-for-byte; selftest + normal + shellcheck all green; integrity preserved (cmp still catches an un-restored mutation). Accepted LOW residuals (documented): in-tree mutation is non-atomic under SIGKILL (trap covers normal signals); eval on the hardcoded test string has no injection surface. Minor real finding surfaced (logged, not fixed — rbac is F-0021-done): rbac lacks an arbitrary-principal default-deny property test. FOLLOW-UP: extend mutants to update-state/assertion-shield; add held-out tests + anti-tautology. feat/ PR under the operator /goal pass. Reversible. → scripts/mutation-smoke.sh, scripts/verify.sh, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **CI de-flake: pre-fetch shellcheck binary with retries (transient GitHub-CDN 504s).** PR #78's CI failed twice on `shellcheck (hooks + gate scripts): FAILED` — NOT a script finding but a 504 Gateway Timeout while the `shellcheck` npm wrapper lazily downloaded its binary at verify time (a GitHub release-asset CDN blip); mutation-smoke + all tests passed, only the binary fetch flaked, and it would block ANY PR during the outage. Fix: a ci.yml step pre-fetches the binary (`npx --no-install shellcheck --version`) with 5 retries before verify.sh, so the shellcheck stage uses the cached binary — same pinned version (no skew vs. a system shellcheck), just resilient to transient CDN timeouts. No verify.sh/guard-logic change (the trust-widening tripwire + actionlint validate the workflow edit; categorically not a guard-logic change so no dedicated security-review spawn). chore PR. Reversible. → .github/workflows/ci.yml, this PR.
- **2026-06-15** · **F-LP1: task-autonomy tier field (A/B/C) — the adaptive layer's data model.** First step of the adaptive layer (operator's "loop switching / make it adaptable"). Added an OPTIONAL `tier` field to features.json on the F-SM2 closed shape — a deliberate, schema-bound addition (the F-SM2 comment anticipated exactly this): KNOWN_KEYS gains 'tier'; features.schema.json gains a `tier` enum property (A/B/C); update-state.ts validate() rejects a tier outside A/B/C when present; the Feature interface + the schema-cross-check test updated (16-key set). Tier is OPTIONAL so the 33 legacy features (and any ungroomed row) keep validating. Tier SEMANTICS — loop-switching: A=evaluator spot-check / B=mandatory evaluator / C=+security-reviewer +human-approval gate — land in the next PRs (groom assigns the tier; /work routes review depth by it). +2 contract tests (296->298). No security semantics yet (inert validated data); the routing PR that gives tier behavior gets the full security review — this is a small additive enum on the already-reviewed closed shape. feat/ PR under the operator /goal pass. Reversible. → scripts/update-state.ts, roadmap/features.schema.json, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **F-LP1b: tier system wired into the loop — triage rubric + groom assignment + ambiguity gate (loop-switching behavior).** Re-introduced TASK_AUTONOMY_TRIAGE.md (the dead doc from session start, now PROPERLY wired): the tier A/B/C rubric, the gate-on-consequences principle (irreversibility × blast radius), the two hard rules (trust-boundary → human security review; ambiguity → human intent), and a failure-mode→counter-gate map referencing the engine's existing gates. Wired: CLAUDE.md pointer-map references it; /groom step 3 assigns `tier` + enforces the AMBIGUITY GATE (don't groom a feature whose "done" isn't a one-sentence verifiable criterion → QUESTIONS.md instead); install-into.sh now copies it to adopters (fixing the dangling-pointer the audit flagged). CRITICAL alignment with the critic's hard ruling: the fresh-context evaluator is MANDATORY on every tier (never sampled) — tier modulates the security-reviewer (mandatory on C), the human-approval merge gate (C only), and model selection (next PR), NOT the evaluator. Caught + fixed a "Tier A = evaluator spot-check" wording slip that would have encoded that violation into the engine's own instructions. Docs/skills + a trivial install-into copy-list line — no guard-logic change; the /work routing PR that gives tier its mechanical behavior gets the full security review. feat/ PR under the operator /goal pass. Reversible. → TASK_AUTONOMY_TRIAGE.md, CLAUDE.md, .claude/skills/groom/SKILL.md, scripts/install-into.sh, this PR.
- **2026-06-15** · **F-LP2 (PR #82): /work loop de-fluffed + tier-routed JUDGE — the mechanical routing behavior.** The PR that turns the tier *field* (F-LP1) + *rubric* (F-LP1b) into actual loop behavior. Two intents in `.claude/skills/work/SKILL.md`: (1) removed 11 jargon/telemetry tokens (xAI 4/16, 0e, 6x RL, Heavy 16-style, TELEMETRY 16/0+173/0, velocity, TOOL_CALLS_APPROX, 4x cost) that encoded no enforceable rule — a fresh-context evaluator verified every removed line's real rule still survives (explorer-first, Grep/Glob-before-edit, report-don't-dump w/ approximate tool-call count + errors, BUILD-end git-hygiene contract, temp-then-move evidence rule, state-drift guard, VERIFY: PASS marker, all 8 loop steps, manager mindset); (2) step-5 JUDGE now routes by tier per TASK_AUTONOMY_TRIAGE.md §7 — the fresh-context evaluator is MANDATORY on every feature/tier (never sampled; the core invariant, now stated loud), the security-reviewer is ADDITIONALLY spawned on Tier C OR a sensitive-path diff (auth/API/data/workflows/hooks/deps) and is mandatory regardless of path on Tier C, BLOCK=NEEDS_WORK; SHIP notes the Tier-C REQUIRE_APPROVAL merge gate lands with the `awaiting_approval` status (next PR). **Reviewed as a skill (behavior, not an enforcement surface) → fresh-context evaluator, not security-reviewer**, per DECISIONS 2026-06-09 (F-0005 precedent); evaluator PASS on all 4 acceptance criteria (no rule lost, only jargon removed, routing matches §7 + groom, no contradiction). verify.sh PASS, 298 contract tests, mutants killed; CI green, merged. **Debt surfaced (NEXT PR):** CLAUDE.md §3 still carries the same jargon AND the pre-tier JUDGE line ("Sensitive paths also get security-reviewer") — the constitution now contradicts the cleaned /work; de-fluff §3 + align its JUDGE routing under the §9 checklist (≤150 lines). feat/ PR under the operator /goal pass. Reversible. → .claude/skills/work/SKILL.md, this PR.
- **2026-06-15** · **CLAUDE.md §3 de-fluff + JUDGE-routing alignment (PR #84): closed the contradiction #82 surfaced.** After #82 cleaned /work, the constitution's §3 ("session protocol") was a jargon-laden prose DUPLICATE of /work whose JUDGE line ("Sensitive paths also get security-reviewer") now CONTRADICTED the tier-routed JUDGE in /work + TASK_AUTONOMY_TRIAGE §7. Rebuilt §3 as a tight 8-step skeleton mirroring /work (SELECT..MANAGE), stripped of jargon (xAI 4/16, TELEMETRY 16/0+173/0, 0e/velocity, 6x RL, Heavy 16, TOOL_CALLS_APPROX, 4x cost, the 1min/5min eval snippet, the prompt-bloat snippet, worktree-isolation aside). Every enforceable rule preserved (evaluator confirmed each): explorer-first, Grep/Glob-before-edit, CLAUDE_ACTIVE_FEATURE, the BUILD-end git-hygiene contract, capture.sh evidence, the cross-platform/Windows rule, the state-drift guard, the manager doctrine. §3 step-5 JUDGE now states the evaluator is mandatory on every tier (never sampled) + the security-reviewer fires on Tier C OR a sensitive path (mandatory regardless of path on C) — STRICTLY TIGHTER than the old line (which only triggered on sensitive paths, no Tier-C-mandatory) and word-identical to /work + the triage rubric. Also fixed a stale "/work step 6" → "step 5" cross-ref in triage §7 (JUDGE is step 5 in the cleaned /work). §9 constitution-edit checklist walked: 59→52 lines (≤150), no duplicate/contradiction left, enforceable wording, logged here. Reviewed as instructions (not an enforcement surface) → fresh-context evaluator PASS on all 4 criteria (no rule lost, only jargon removed, routing matches /work + §7, no new contradiction, tightening-not-loosening confirmed). verify.sh PASS, CI green, merged. NEXT: `awaiting_approval` status + the Tier-C REQUIRE_APPROVAL merge gate (state-machine change), then per-tier model agents (model switching). refactor/ PR under the operator /goal pass. Reversible. → CLAUDE.md, TASK_AUTONOMY_TRIAGE.md, this PR.
- **2026-06-15** · **F-MP1 (PR #86): model-policy.json made load-bearing — model-switching foundation; security-reviewer APPROVE.** The v2 audit flagged "model-policy single source of truth is false — agents hardcode `model:`, sync is vaporware"; the model-switching work confirmed it: `AI_OPERATIONS_PLAN §2.2` claimed a hygiene routine syncs frontmatter from policy, but none existed (update-state.ts only WARNS on `last_verified` staleness). Agent frontmatter models matched policy by luck, unenforced — a `/research` tier→model bump would silently drift every agent. Same dead-config class as F-SM2's decorative schema. Fix: (1) `.claude/model-policy.json` gains a machine-readable `agents` map (agent→capability tier) beside the existing `tiers` map (tier→model); (2) `scripts/check-model-policy.ts` — `--check` (wired into verify.sh as a gate step) parses each agent's frontmatter `model:` (anchored `^model:\s*(\S+)\s*$`, first `---`…`---` block only) and FAILS on any drift from policy; `--write` is the real hygiene routine that rewrites the single `model:` line from policy. FAIL-CLOSED on: missing/invalid-JSON policy, missing/non-object `tiers` or `agents`, agent→unknown-tier, tier with no string model, a mapped agent whose `.md` is absent, an agent .md declaring a `model:` but ABSENT from the map (no unmanaged/hardcoded models), and an unsafe agent name (`SAFE_NAME=/^[a-z0-9][a-z0-9-]*$/` validated BEFORE any path construction — preempts a `--write` path-traversal via a compromised policy key, added before the review). MODEL_POLICY_ROOT env seam for fixture tests. (3) §2.2 reworded from the vaporware claim to the enforced mechanism. **Security-reviewer APPROVE** (adversarial pass on drift-evasion, --write traversal/clobber, fail-closed completeness, verify.sh wiring, empty-map vacuity, ReDoS): drift reliably detected, --write cannot escape the agents dir, all failure paths fail-closed, gate unconditional; sole finding a cosmetic TOCTOU (readFileSync can throw uncaught between existsSync and read) that is STILL fail-closed (non-zero exit) — accepted as **LOW, deferred** (wrap in try/catch for a clean message; not an exploit). +8 contract tests (298→306): match / drift / --write-resync / file-content / unmanaged-agent / unsafe-name-traversal / missing-map / real-config-no-drift. The shipped config already had zero drift so no agent model changed (behavior-neutral today). FOUNDATION for F-MP2 (per-tier builder agents + /work tier→model routing = the actual model switch). feat/ PR under the operator /goal pass. Reversible. → .claude/model-policy.json, scripts/check-model-policy.ts, scripts/verify.sh, scripts/test-hooks.sh, AI_OPERATIONS_PLAN.md, this PR.
- **2026-06-15** · **F-MP2 (PR #87): per-tier builder agents — the model switch (Tier C → builder-strong/opus); evaluator PASS.** On the F-MP1 foundation, this is the operator's "model switching" headline + triage §7's promised "(later) model selection", made real. Because per-invocation model override is SDK-only (fixed by frontmatter in the CLI/Actions lanes, per [[claude-code-subagent-model-routing]]), switching = choosing WHICH builder the orchestrator spawns. Added `.claude/agents/builder-strong.md` (frontmatter `model: opus`, mapped to the `reasoning` tier in model-policy.json) for Tier C work (security/crypto/auth, irreversible, novel architecture, concurrency); kept `builder` (sonnet) for Tier A/B and de-fluffed it (dropped 0e / TOOL_CALLS_APPROX / "xAI 2026 guides" / marker+counts / worktree aside — every enforceable rule kept, evaluator confirmed). `/work` BUILD (step 3) routes Tier C → builder-strong, A/B → builder; triage §2 (Tier C line) + §7 (new BUILD bullet, JUDGE/cost bullets) + AI_OPERATIONS_PLAN §2.2 table all updated to match. builder-strong is strictly STRONGER, not an escape hatch: stop-don't-guess on ambiguous security/correctness, mandatory abuse/negative tests, and an explicit "green verify is necessary-not-sufficient — the mandatory security-reviewer + operator approval gate still run before merge." **Invariants preserved:** evaluator mandatory every tier (never sampled); Tier C still gets the mandatory security-reviewer + REQUIRE_APPROVAL gate; builder-strong cannot self-certify. The F-MP1 drift gate now governs 6 bindings and ENFORCES builder-strong's frontmatter==opus (zero drift). +2 model-agnostic contract tests (306→308): builder-strong resolves to the reasoning tier; the Tier-C builder resolves to a DIFFERENT model than the default (guards the switch INTENT, which the drift gate alone does not — a mis-map to a weaker tier would be caught here). Behavior/config + docs, no enforcement-surface change → fresh-context **evaluator PASS** on all 5 criteria (routing consistent across all docs, no rule lost in de-fluff, builder-strong sound+stronger, switch real+coherent, no invariant weakened). The shipped config had zero drift so no existing agent's model changed. NEXT: `awaiting_approval` status + Tier-C merge gate, then advisory cost. feat/ PR under the operator /goal pass. Reversible. → .claude/agents/builder-strong.md, .claude/agents/builder.md, .claude/model-policy.json, .claude/skills/work/SKILL.md, TASK_AUTONOMY_TRIAGE.md, AI_OPERATIONS_PLAN.md, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **F-AP1 (PR #88): awaiting_approval status — the human-approval hold; security-reviewer BLOCK→fix→APPROVE.** The human-in-the-loop gate, built status-first per the critic ruling (build the status FULLY before wiring the merge gate). `awaiting_approval` = a built+verified+reviewed feature whose irreversible/operator-visible MERGE is held for human sign-off (Tier C / REQUIRE_APPROVAL, TASK_AUTONOMY_TRIAGE §3); forward-referenced by /work SHIP + CLAUDE.md §3 + the triage doc — this makes it real. State machine (scripts/update-state.ts): STATUSES + features.schema.json status enum gain `awaiting_approval` (a contract test cross-checks enum↔STATUSES, mirroring the F-SM2 key guard); validate() requires evidence for awaiting_approval (no parking an UNBUILT feature in limbo); the `--status` transition guard permits the forward move only from an `in_progress` feature that already has evidence (rejects pending→awaiting_approval and in_progress-without-evidence); awaiting_approval is PARKED — excluded from the single-in_progress invariant so the loop keeps moving (the gate never blocks). **Security-reviewer BLOCKED on a real HIGH (F1):** `--add` spread `...incoming` over the `status:'pending'` default, so a caller could birth a feature directly in `awaiting_approval` (evidence-array non-empty but never built) — or in_progress/blocked/done — skipping the entire `--status` lifecycle and its transition guards (validate didn't catch awaiting_approval+evidence). Fixed: `--add` now fails unless `status==='pending'` (the lifecycle runs only through `--status`) — one guard closing the awaiting_approval birth vector AND the broader born-in-any-status hole; placed after the reserved-range + passes:true checks, before push/save. A FRESH security-reviewer re-reviewed and APPROVED: F1 closed (all non-pending births rejected, no smuggling past the guard), no regression (status-less --add still defaults pending; F-SM2 forbidden_paths union intact), every awaiting_approval invariant holds, and the residual F2 (evidence-array-non-empty-but-files-may-not-exist at awaiting_approval) is acceptable LOW because the real evidence-on-disk check is at `--passes` which still runs before `done` (done still requires passes:true requires evidence). +10 contract tests (308→318): validate with/without evidence, the three --status transition guards, parked-doesn't-block-a-new-in_progress, the three --add born-status rejections, the schema↔STATUSES drift guard. Touches the state writer (guard surface). NEXT (F-AP2): wire /work SHIP to set awaiting_approval + a QUESTIONS.md sign-off request for Tier C instead of merging; operator approval → merge → --passes → done. feat/ PR under the operator /goal pass. Reversible. → scripts/update-state.ts, roadmap/features.schema.json, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **F-AP2 (PR #89): Tier-C human-approval merge gate wired into /work; evaluator PASS.** Completes the human-in-the-loop gate on the F-AP1 status. `/work` SHIP (step 6) now branches on tier: Tier A/B (and any non-REQUIRE_APPROVAL surface) merge on green → --passes → done (unchanged); Tier C / REQUIRE_APPROVAL — after green CI + a passing evaluator + the mandatory security-reviewer APPROVE — does NOT merge: it records evidence (`--evidence`), parks the feature (`--status awaiting_approval`), writes a plain-English sign-off request to QUESTIONS.md (8th-grade, click-by-click QA + the gated irreversible action + the PR link), leaves the PR open, and KEEPS THE LOOP MOVING (takes the next feature; P2 — never stop and wait). On operator approval (later turn): merge → --passes true → done. TASK_AUTONOMY_TRIAGE §3 REQUIRE_APPROVAL updated to reference the park (and that it doesn't stop the loop). Behavior/docs only — the enforcing state machine shipped + was security-reviewed in #88 → fresh-context **evaluator PASS** on all 5 criteria: step ordering matches the state machine (--evidence BEFORE --status awaiting_approval, else the transition guard rejects it; --passes/done correctly deferred to post-approval); the loop never blocks (SELECT picks only `pending`, so a parked awaiting_approval feature is never re-selected or re-worked); the evaluator + Tier-C security-reviewer stay mandatory (the gate fires AFTER them, never instead); operator-facing is plain English; and /work step 6, triage §2/§3/§7, and CLAUDE.md §3 step 6 are all mutually consistent. verify.sh PASS, CI green. **Adaptive-layer milestone:** "where a human is required" is now real end-to-end — groom sets tier; /work routes review depth (loop-switching #82/#84), builder model (model-switching #86/#87), and the Tier-C merge gate (human approval #88/#89); the three things triage §7 says tier modulates (security-reviewer, model, human-approval gate) are all wired, with the evaluator mandatory+unsampled throughout. NEXT: advisory cost governor (last planned piece). feat/ PR under the operator /goal pass. Reversible. → .claude/skills/work/SKILL.md, TASK_AUTONOMY_TRIAGE.md, this PR.
- **2026-06-15** · **F-CG1 (PR #90): advisory cost/quality governor — observability, not a gate; security-reviewer APPROVE.** The last planned adaptive-layer piece, built honestly around a hard harness constraint: there is NO token telemetry at SubagentStop (per [[claude-code-subagent-model-routing]]), so cost CANNOT be a mechanical gate, and the critic's hard ruling forbids ever skipping/down-tiering a feature to save cost. So this is pure observability + manager steering. roadmap/metrics.jsonl gains OPTIONAL `tier` (A/B/C) + `builder` (builder|builder-strong) fields; update-state.ts --validate enforces them WHEN PRESENT (legacy records predate them and stay valid) plus an int-check on `attempts`, so /kaizen's cost scan reads trustworthy data, not free-text (new `const BUILDERS=['builder','builder-strong']` kept-in-sync-with model-policy.agents by comment). /work RECORD (step 7) emits tier+builder; /kaizen step 1 gains a cost/quality scan — per-tier first-attempt rate (thin-brief signal), builder-strong/opus usage share (over-tiering signal), attempts clustering — that proposes re-tiering / sharper briefs / cheaper-agent defaults but EXPLICITLY forbids skipping/gating/down-tiering a feature or economizing the mandatory evaluator / Tier-C security-reviewer (the advisory-only invariant, mirroring triage §7's cost rule). **Security-reviewer APPROVE:** validation sound + fail-closed (typeof-string guard before TIERS/BUILDERS.includes → no type-confusion throw on object/array/number values; `!== undefined` correctly separates absent from present-but-wrong; absent fields still pass), the 500-char bounded-injection rule + date/feature requirements intact, no features.json/closed-shape/--add/--status regression, and the /kaizen advisory language unambiguous (no cost-based gate authorized). BUILDERS-hardcoded-vs-policy drift is acceptable (fail-closed: an unknown builder is rejected until the const is updated; greppable comment). +4 contract tests (318→322): valid cost fields accepted; invalid tier / unknown builder / non-int attempts rejected; legacy no-cost-field record still accepted. Touches the state writer's metrics validation (bounded-injection channel = guard surface). **ADAPTIVE LAYER COMPLETE** (loop-switching #82/#84 + model-switching #86/#87 + human-gate #88/#89 + advisory cost #90). feat/ PR under the operator /goal pass. Reversible. → scripts/update-state.ts, .claude/skills/work/SKILL.md, .claude/skills/kaizen/SKILL.md, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **F-TH1 (PR #91): tier-system mechanical hardening from the holistic audit; security-reviewer APPROVE.** Ran a 2-agent adversarial audit of the whole adaptive layer (operator's "constantly evaluate/steer"). It confirmed the state machine, model-policy drift gate, evidence/default-FAIL gates, path guards, and the now-word-identical cross-doc JUDGE routing are SOUND, and the jargon sweep came back clean + install-into.sh complete. It surfaced two REAL gaps where tier safety relied on orchestrator PROSE rather than the state machine — both closed mechanically in update-state.ts: (1) an untiered feature (`tier===undefined` from a groom slip) would route to /work's Tier-A/B path → the weak builder + NO Tier-C gate even on security-critical work → `--add` now defaults a missing tier to `B` (the safe supervised floor; placed BEFORE `...incoming` so an explicit tier still overrides; `{"tier":null}` is rejected by the F-LP1 validate check, not silently untiered; groom still assigns tier deliberately); (2) a Tier-C feature could be marked `done` directly from `in_progress`, skipping the awaiting_approval operator-sign-off park (the F-AP2 gate was prose, not state) → `--status done` on a `tier==='C'` feature now FAILS unless the prior status is `awaiting_approval`, turning the merge gate into a state-machine gate (the state record cannot show a Tier-C feature done without the human hold having happened). **Security-reviewer APPROVE:** every Tier-C done bypass closed (blocked/pending/in_progress→done all rejected; tier is not settable via --status; born-awaiting_approval already blocked by the F-AP1 pending-only --add guard; lowercase 'c' rejected by validate's uppercase TIERS); the new gate works WITH, not instead of, done-requires-passes:true-requires-evidence; tier A/B + the legacy 33 null-tier (all done) features are unaffected (guard fires only on `tier==='C'`); no stuck state (awaiting_approval→done reachable); no uncaught throw. +4 contract tests (322→326): tier-default-on-add, Tier-C in_progress→done REJECTED, Tier-C awaiting_approval→done ALLOWED, Tier-B in_progress→done ALLOWED (C-only). Touches the state writer (guard surface). Audit also flagged DOC-accuracy debt for a follow-up PR: AI_OPERATIONS_PLAN calls the path-guard "planned, not yet shipped" (FALSE — F-0007 done + guard live), a Fable-5 transition-date inconsistency, and minor §-ref nits. feat/ PR under the operator /goal pass. Reversible. → scripts/update-state.ts, scripts/test-hooks.sh, this PR.
- **2026-06-15** · **PR #92: doc-accuracy fixes (holistic-audit residue) — killed a false "unshipped path-guard" claim.** The audit's debt-sweep found AI_OPERATIONS_PLAN's hook-mechanics section claiming the PreToolUse path-authorization guard is "planned, not yet shipped (backlog F-0007)" with an unresolved "which feature is active" design question — FALSE: F-0007 is done and `.claude/hooks/path-guard.js` is live (fails closed on unknown/duplicate active-feature id, canonicalizes `..`; the active feature = the single in_progress row (F-0025) or an explicit CLAUDE_ACTIVE_FEATURE (F-0022); hardened by F-0034). A false "a security mechanism is unshipped" claim is the exact dead-config class this program targets, so it's worth fixing even though doc-only. Rewrote it to the accurate, test-backed shipped state (verified path-guard.js's fail-closed + canonicalization behaviors against the F-0034 contract corpus that runs in verify.sh before editing). Also standardized one Fable-5 credit-transition phrasing ("from June 23, 2026") to match model-policy.json + §5.2. Doc-only, verify.sh PASS → shipped on the test-backed verification (the F-0034 corpus IS the proof the claim is now true); no separate evaluator for a factual doc correction already proven by passing tests. Deliberately left unchanged (audit over-flagged): the `(F-0034)` reference in triage §6 is a correct PR-id reference (F-0034 = the path-authz consolidation, not a features.json row); the `[[…]]` links in DECISIONS are the memory-link convention of this append-only log. Open low-pri kaizen: add mutation-smoke.sh to the plan's verify.sh step list. Reversible. → AI_OPERATIONS_PLAN.md, this PR.
- **2026-06-15** · **F-TC4b (PR #93): mutation-smoke extended to update-state.ts + assertion-shield.ts — test-teeth backlog item #1; security-reviewer APPROVE.** Under the operator's new /goal ("grind the test-teeth backlog"). mutation-smoke (F-TC4) only proved rbac.ts's tests had teeth; the two guard surfaces this program hardened most (the state writer + the assertion shield) had no vacuous-test detection. Added 5 in-tree mutants whose existing contract test must KILL them: update-state — Tier-C done-gate (`f.tier === 'C'` → `'C_MUT'`), single-in_progress (`wip.length > 1` → `> 99`), --add born-pending (`feature.status !== 'pending'` → `false`), awaiting_approval-requires-evidence (the validate condition → `false`); assertion-shield — deletion-detection gate (`violations.length > 0` → `> 99`). Killers are FAST + TARGETED with INVERTED polarity (`us_must_reject`/`shield_must_block` exit 0 when the guard rejects = green on clean, non-zero when the mutation removes it = killed) — one ts-node / shield run each (~7s total; the full contract suite is ~86s, far too slow per-mutation). Fixtures crafted so ONLY the targeted guard rejects each input (e.g. the Tier-C fixture has passes:true+evidence so done-requires-passes can't mask the missing tier gate). A baseline-green precheck requires every killer to pass on UNMUTATED code (so a broken/typo'd killer or a missing fixture fails the gate rather than reporting spurious kills). Restoration generalized to a multi-SUT backstop (cp-backup baselines, NOT git HEAD, so uncommitted WIP can't false-fail) + the EXIT/INT/TERM trap restores the in-flight file; verified the tree is byte-identical after a run. rbac coverage + the --selftest meta-gate unchanged. **Security-reviewer APPROVE** after a 26-point adversarial pass: no path leaves a guard surface mutated on disk; kill-soundness holds in both directions (a survived mutant FAILS the gate; a falsely-green harness is caught by the baseline precheck); fixtures genuinely isolated (the only exit-changing factor per mutation is the targeted guard); no rbac/selftest weakening; no command-injection via the eval'd function-name testcmds or the sed exprs (no `$`/backtick); set -u safe; fixtures cleaned via trap; perf acceptable. Sole non-critical note (fixture printf lacks `|| exit` if mktemp fails) is still fail-closed via the baseline precheck — left as-is. verify.sh PASS (9/9 mutants killed). Touches a gate script that mutates two guard surfaces in-tree (guard surface). Reversible. → scripts/mutation-smoke.sh, this PR.
- **2026-06-15** · **F-TC4c (PR #94): rbac arbitrary-principal default-deny property test (held-out + anti-tautology); evaluator PASS — test-teeth backlog CLEARED.** rbac.ts is the engine's most security-critical module; it had monotonicity property tests but NO held-out default-deny property — it asserted specific known principals, never the SPEC invariant that ANY non-role principal is denied on EVERY resource/action (the audit + the F-TC4 follow-up both flagged this). Added two held-out property tests to rbac.test.ts (they encode the rbac.ts docstring spec, not the impl's branches, over a broad DETERMINISTIC seeded space — a fixed-seed Math.imul LCG, no Math.random/Date.now so the gate can't flake): (1) ~230 arbitrary principals (curated privilege-flavored + attack-y + 200 seeded-random, filtered with the SAME lowercase+trim normalization as check() to exclude anything resolving to owner/admin/editor/viewer) × 10 resources × 13 actions all return 'deny' — 30,160 assertions, guarded by `assert.ok(checked > 5000)` so a filter bug that empties the input set fails LOUDLY instead of passing vacuously; (2) org/billing-type resources (org/billing/subscription/seat/plan) are owner-only — every non-owner denied on every action, with the owner-IS-allowed positive side so the deny assertions are meaningful. Proved their teeth by adding a mutation-smoke mutant that flips the fall-through default-deny `^  return 'deny';$` (uniquely line 78 — 2-space indent, no comment; the other return 'deny' lines are 4-space/inline) to 'allow'; the arbitrary-principal test KILLS it → 10/10 mutants now killed. **Evaluator PASS** on all 5 criteria: the test filter's normalization is byte-identical to check()'s (so they agree — only 'owner\n' is correctly dropped); non-vacuous (30,160 checks ≫ 5,000 guard); org/billing list matches ORG_BILLING_RESOURCES exactly and the positive owner side is real; deterministic + CI-stable; the sed matches ONLY line 78 and is lethal + baseline-green. Caught + fixed a biome `noAssignInExpressions` on the LCG (rewrote the arrow to a statement body) before the review. Touches rbac.test.ts (tests) + the (already-#93-reviewed) mutation-smoke gate → evaluator review. verify.sh PASS, rbac 17/17. **TEST-TEETH BACKLOG CLEARED** (#93 + #94): the safety-critical tests across rbac, the state writer, and the assertion shield are now proven non-vacuous; "held-out + anti-tautology tests" is satisfied by the held-out property tests + the anti-vacuity guards. Reversible. → src/forge/rbac.test.ts, scripts/mutation-smoke.sh, this PR.
- **2026-06-15** · **Doctor's-report review pass (PR #95): web-verified the engine (RULE 4), corrected a freshness violation in the F-MP2 rationale, rewrote README.** Operator /goal demanded a brutal senior review grounded in CURRENT web reality, not training data. Verified live (code.claude.com/docs + active sources): model lineup current (Fable 5 / Opus 4.8 / Sonnet 4.6 / Haiku 4.5 map to the real `model-policy.json` tiers); the architecture matches the 2026 autonomous-coding best-practice frontier (evidence outside self-assessment, scoped HITL, deterministic hooks, auditable state); Ralph loop mainstreamed (OpenAI `/goal`); SubagentStop carries no token telemetry (F-CG1 advisory design confirmed correct). **CRITICAL finding:** F-MP2 (PR #87) justified the two-builder design on the FALSE premise "no per-invocation subagent model override in the CLI/Actions" — the official sub-agents doc states the opposite (resolution order: CLAUDE_CODE_SUBAGENT_MODEL env → per-invocation `model` param → frontmatter → main convo). The engine's own §5 freshness rule should have caught it (the premise came from an un-reverified memory). The MECHANISM is fine (builder-strong's `model: opus` frontmatter is honored at resolution #3); only the RATIONALE was rotten. Corrected: `/work` step 3 + TASK_AUTONOMY_TRIAGE §7 now justify the two-builder design honestly — `builder-strong` is a separate agent for its **stricter Tier-C system prompt** (stop-don't-guess, abuse-case tests), NOT a routing workaround; per-invocation override is acknowledged as available. The [[claude-code-subagent-model-routing]] memory was corrected (it was the source of the false premise). **Report card** written to the operator's Desktop (overall B; core A-; demo B+, honest mockup not a liability). **Logged as tracked debt (NOT fixed — redesign or security-surface, deferred to /kaizen or operator call):** (1) two-builder ~65% duplication — Option-A alternative recorded (one `builder` + per-invocation `model` + Tier-C strictness in the brief); (2) BUILDERS list hardcoded in update-state.ts AND model-policy.json (derive from policy to make "single source of truth" fully true); (3) no parity contract test between path-guard.js and verify-gate.sh glob matchers; (4) test-hooks.sh is a 1,400-line monolith (extract path-guard / mutation-smoke-setup / install-into tests); (5) no e2e test that the orchestrator actually spawns the tier-routed builder. README rewritten to the current engine + the freshness lapse recorded as a scar. Tests: verify.sh PASS, 326 contract tests, mutation-smoke 10/10, rbac 17/17. Docs/skills only → evaluator review. Reversible. → .claude/skills/work/SKILL.md, TASK_AUTONOMY_TRIAGE.md, README.md, this PR (+ Desktop report, memory correction).
- **2026-06-15** · **Benchmark/effect-measurement suite (PR #96, bench/): a golden-task harness to measure if engine changes have an effect (quality/tokens/cost/speed), NOT a code review.** Operator /goal via /deep-research. Web-verified the field (RULE 4 + the deep-research workflow against active repos): the measurement bridge is `claude -p --output-format json` → `usage` (in/out/cache tokens) + `total_cost_usd` (CAVEAT: client-side ESTIMATE; authoritative cost = Anthropic Usage/Cost API) + `duration_ms`; `--json-schema` forces structured output for deterministic grading; `--bare` gives hermetic runs but needs an API key (subscription login can't use it). Live-loop telemetry = built-in OpenTelemetry via `CLAUDE_CODE_ENABLE_TELEMETRY=1`. SubagentStop still carries NO token telemetry (re-confirmed) — so hook-based cost stays advisory; `claude -p` JSON / OTel are the bridges. Field tooling (active, verified): **promptfoo** (22.2k★, MIT, Claude Agent SDK integration 2026-06-13 — wraps `claude -p` via exec provider, tracks latency/tokens/cost, A/B variants, CI gating = the recommended durable framework layer), **Inspect AI** (UK AISI, model_graded_qa LLM-judge), **DeepEval** (pytest-native agent metrics); task-perf benchmarks **SWE-bench/SWE-bench Verified, Terminal-Bench, Aider polyglot, LiveCodeBench** (objective, test-execution-scored, Docker-local). Built `bench/`: `run.mjs` (runner+graders+compare; `--ctx clean|engine`, `--baseline`, `--dry-run`; graders: exact/contains±negate/word_max/set_equals/**codegen-by-execution**), `micro.mjs` (local no-API throughput+gate-latency, real numbers: rbac.check 33ns/op, promptToGraph 120ns/op, update-state --validate 214ms), and 7 self-contained deterministically-graded golden tasks (haiku/sonnet/opus tiers). Live-proven: `extract-roles` ran end-to-end (✓, 383 out-tok, $0.0327, 7.4s). **Finding:** the engine's project context (CLAUDE.md+hooks) costs ≈14k tokens/~$0.01 per call (clean ~21k vs engine ~35k cached) → a dollar-denominated test of de-fluff changes (the `--ctx` delta). bench/ is isolated dev-tooling (NOT in verify.sh / hooks / the engine runtime; results+sandbox gitignored). The codegen grader executes model output via `node -e` (timeout 10s) — standard benchmark practice (SWE-bench does the same), tasks are author-trusted. Two intermediate commits on the branch came from the repo's commit-on-stop hook firing mid-build (harmless; final tree is clean: harness + 7 tasks, no artifacts). Report on Desktop with the NEEDS-HELP/access/MCP table (API key, Docker, Playwright MCP, OTel collector, Usage API, promptfoo, labeled judge sets). Behavior/tooling, no guard surface → evaluator review. verify.sh PASS (bench/ outside gated paths). Reversible. → bench/run.mjs, bench/micro.mjs, bench/README.md, bench/tasks/*.json, .gitignore, this PR (+ Desktop report).
- **2026-06-15** · **Bench loop run + measured context tax + bench wired into /kaizen (PR #97).** Operator follow-up to PR #96: run the measure-loop for real, fold discoveries into docs/README, write findings, push to develop, and make the engine's automation loop trigger the measure→improve cycle. Ran the full 7-task suite live (develop @ 8fa66bc) in both context modes: `--ctx clean` 7/7 PASS / 1028 out-tok / $0.4523; `--ctx engine` 7/7 PASS / 993 out-tok / $0.5179. **Measured engine context tax = +$0.0656 (+14.5%, ≈$0.0094/agent-call) + 0.3–4.3s/task latency, with NO quality change** — a concrete dollar-denominated payoff target that validates the session's de-fluff work and gives /kaizen a number to chase (out-token delta −3.4% was within the harness's stated noise band). All engine-component probes passed including judge-good (PASS), judge-bad (correctly caught the fail-open auth → NEEDS_WORK), injection-resist (resisted), codegen-slugify (generated code passed hidden assertions by execution). **Wired bench into /kaizen** so the daily improvement loop measures its own effect: step 3 now requires a before/after bench reading for any change that could move quality/tokens/cost/speed (micro.mjs free; run.mjs --compare; --ctx engine for context changes) — "a change that doesn't move a measured number (and isn't a pure correctness fix) is churn"; quality regressions (✓→✗) veto regardless of token/speed wins. Folded the discovery into README (a "Measuring whether a change helps" section + baseline numbers), AI_OPERATIONS_PLAN (bench/ tree entry + self-measurement paragraph: authoritative cost via Anthropic Usage/Cost API, live-loop telemetry via CLAUDE_CODE_ENABLE_TELEMETRY=1 OTel), and bench/README (measured-baseline table — committed so it's readable from GitHub). Desktop findings report updated with full-suite numbers. Docs/skill, no guard surface → evaluator review. verify.sh PASS. Reversible. → README.md, AI_OPERATIONS_PLAN.md, bench/README.md, .claude/skills/kaizen/SKILL.md, this PR (+ Desktop report).
- **2026-06-15** · **Testing-suite PLAN (PR #98, bench/testing-suite-plan.md): oracle-first, layered end-to-end benchmark — plan only.** Operator /goal (/deep-research + brutal-senior persona): v0's 7 atomic probes are a smoke test, not a benchmark; need real end-to-end build tasks (build-a-CRM, build-an-MCP-server) laddered easy→very-hard with OBJECTIVE oracles so engine changes are verifiable. Deep-research (3-vote-verified vs active repos) established: the oracle is the entire ballgame and homegrown ones are usually broken (ABC arXiv 2507.02825 — flawed oracles distort up to 100%; tau-bench empty-response scored 38%, SWE-Lancer assert-1==1 scored 100%, UTBoost found 15.7% SWE-bench-Verified false positives flipping 24% of top-50). Catalogued the production oracle patterns: hidden FAIL_TO_PASS/PASS_TO_PASS (SWE-bench Verified, Docker-per-instance), triple-verified E2E acceptance tests pass-on-fixed/fail-on-broken (SWE-Lancer), own-test-suite reconstruction (Commit0), the description+env+test+reference quadruple (Terminal-Bench), final-state-hash reward + pass^k reliability + compositional generator (tau-bench/τ²-bench), and protocol-conformance via the official MCP conformance suite + inspector CLI (the cited build-an-MCP-server oracle). Wrote the plan: the oracle doctrine (dual validity = task-validity + outcome-validity; isolate agent from ground truth = tests outside writable space injected post-run; sufficient coverage + adversarial augmentation; resist degenerate/do-nothing solutions); a 7-type oracle taxonomy mapped to tiers; the L0→L5 difficulty ladder, each with what-it-tests + prompt-mode + a concrete example prompt + the cited oracle + partial-credit + budget + expected frontier pass-rate + cadence (L0 micro probes=today's 7; L1 single-file module vs hidden tests; L2 ADOPT SWE-bench-Verified slice; L3 whole-component-from-spec incl. the MCP server; L4 the CRM with provided acceptance suite; L5 reliability/concurrency/security-abuse/the ambiguity TRAP task); thin-vs-thick prompt modes; the Terminal-Bench authoring template + two validity gates; the engine-effect protocol (pin the model, N seeds, pass^k, compare); build-vs-adopt; failure modes; phased build + needs-help (Docker, ANTHROPIC_API_KEY, budget). **Adversarially reviewed by a skeptical staff-eng critic (verdict NEEDS REVISION) and hardened in-place:** added GATING criteria (a broken-RBAC CRM caps at FAIL/insecure, not an averaged-away 65%); added a §6.5 statistical-reality + "what this benchmark CANNOT measure" section (N≥5 end-to-end can't detect small continuous deltas above loop noise — measure those on the cheap deterministic tiers at high N; the decision rule keys on categorical regressions ✓→✗, not fragile continuous deltas; keep a human gold-reference score); stated the honest authoring cost (days per L4 oracle, ongoing maintenance, oracle-author≠reference-author independence) and made Phase 1 "prove the machinery on ONE oracle (MCP, cheapest) before scaling" with a ≥10× flaky-check; added MCP-harness realities (install Node+MCP CLIs, launch+health-check the server, crashed-server=0, corpus fixture), trap-task pass^k-as-escalation-rate semantics, and reproducibility hardening (pin Node/SQLite/TZ/locale+lockfile, normalized DB hashing not raw SELECT *, fresh agent session per task, flaky detection). Plan only — implementation deferred to operator approval. Docs-only, no guard surface. verify.sh PASS. Reversible. → bench/testing-suite-plan.md, this PR.
- **2026-06-15** · **README "Direction" section (PR #99).** Operator asked if the README reflects current state + direction. It was accurate on STATE (rewritten #95, bench section #97) but had no roadmap and didn't reference the testing-suite plan (the actual trajectory). Added a concise "Direction (where this is headed)" section: the next major effort is the real oracle-first, layered end-to-end benchmark (`bench/testing-suite-plan.md`) — because without a measurable suite no change can be verified — with the roadmap (prove one MCP-server oracle → the CRM flagship → reliability/concurrency/security/trap tiers), kept distinct from the product fork (harden-the-factory vs build-ForgeOps). Docs-only, no guard surface. verify.sh PASS. Reversible. → README.md, this PR.
- **2026-06-15** · **Phase-1 BUILT (PR #100): first MCP-server benchmark task + the project-blueprint verification step, proven end-to-end.** Operator /goal: build the first MCP-server (no approval needed) + integrate the Desktop project-blueprint.md (telemetry-first, uncheatable local evaluation). Built `bench/suite/L3-mcp-calc-search/`: task.md (build-an-MCP-server-with-add/search/get_record prompt), a reference MCP stdio server (solution/server.mjs — minimal newline-delimited JSON-RPC 2.0: initialize/tools/list/tools/call), and the ORACLE (oracle/verify.mjs) — a dependency-free MCP-over-stdio client that drives the candidate server and scores it: PRIMARY checks (handshake protocolVersion, tools/list exposes the 3 tools+schemas, add(2,3)=5, search/get_record behaviors), a HELD-OUT suite (altered params the agent never saw — add(-4,100)=96, search('billing'), bad-args graceful isError, unknown-method JSON-RPC error — kills hardcoded/overfit servers), and an ANTI-CHEAT corpus-hash diff (DQ → score 0 if the agent altered the fixture to game search/get_record). Weighted per the blueprint MCP category: handshake .20 / discovery .25 / primary .30 / held-out+errors .25. **Oracle validity PROVEN** (oracle/validate.mjs, the mutation-smoke pattern applied to the oracle): the oracle scores the reference exactly 1.0 AND catches off-by-one add, a hardcoded/overfit server (caught by held-out), a missing tool (discovery), a crashing server (low score), and a tampered corpus (DQ) — the admission gate, because a broken oracle is worse than no benchmark (ABC: flawed oracles distort up to 100%). **End-to-end PROVEN** (bench/suite/run-suite.mjs): a real Sonnet build via `claude -p` (--allowedTools Write/Edit/Read/Bash, acceptEdits, max-turns 30) in an isolated temp workdir built a working server that the oracle scored 1.0 (held-out 4/4, built+finished) in 3 turns / $0.13 / 31s — capturing the blueprint dashboard (oracle score+groups, in/out/cache tokens, cost, wall-clock, iterations=num_turns). **Blueprint folded into testing-suite-plan.md §0.5:** held-out altered-param suite + anti-cheat-DQ (both now implemented in task #1), the iteration/loop-count + human-intervention telemetry axes, the task-category weighting matrix (mcp/greenfield/refactor/infra), the 4-layer stress gauntlet (L1 rigid-format · L2 long-context · L3 6-8-step tool-chaining · L4 TORTURE = broken env where the win is the engine hitting its two-strike circuit-breaker and exiting cleanly, not a runaway token-burn loop), and the daily human 1.0/0.0 multiplier (HITL batch gate = the engine's awaiting_approval applied to the benchmark); Phase 1 marked DONE. bench/ is isolated dev-tooling (NOT wired into verify.sh/hooks; results+temp-run-dirs gitignored); the oracle's `node -e`-free client is dependency-free. FOLLOW-UP (logged, not built): the L4 CRM flagship; the L4-torture/circuit-breaker task; the official @modelcontextprotocol/conformance suite can be layered onto verify.mjs; wire each task's validity gate into CI as the suite grows; human-intervention-score capture with the HITL queue. verify.sh PASS; validity gate green. Reversible. → bench/suite/** (task+oracle+runner+README), bench/testing-suite-plan.md, .gitignore, this PR.
- **2026-06-15** · **Review pass (PR #101): CRLF root-cause fix + README currency.** Operator: review the repo, fix anything, confirm the README reflects current state + direction. (1) FIX: the bench suite validity gate (oracle/validate.mjs) failed locally but passed in CI — root cause: .gitattributes pinned LF for .ts/.js/.json/.sh etc. but had NO `*.mjs` rule, so .mjs checked out CRLF on Windows, and validate.mjs's "missing-tool" mutation regex `/...\},\n/` silently failed to match `\},\r\n` → the mutant kept the tool → the "missing tool is caught" check false-failed. Fixed two ways: added `*.mjs text eol=lf` to .gitattributes (also normalizes scripts/capture.mjs, which had the same latent CRLF) and made the regex CRLF-robust (`\},\r?\n`). git ls-files --eol confirmed the INDEX was always LF (i/lf w/crlf) so CI/clones were never affected — this was purely a Windows local-checkout flake, now closed at the root; validity gate green. (2) README: the Direction section (#99) predated #100 which built the suite, so it still called bench/ "a smoke test (planned)". Rewrote it to reflect reality — bench/suite/ exists, Phase 1 (L3-mcp-calc-search) is built + validity-gated + proven (live Sonnet build scored 1.0, held-out 4/4), with the roadmap (CRM flagship with gating criteria → reliability/pass^k/concurrency/security-abuse/trap/torture tiers). README now current on both state and direction. Note: operator referenced suggestedtests.md on the Desktop — not present (only project-blueprint.md + the benchmark report), likely unsaved; proceeded on the plan's roadmap. Docs + cross-platform fix, no guard surface (the .mjs eol normalization is the engine's own cross-platform discipline, mirroring the existing .sh/.ts LF pins). verify.sh PASS; bench validity gate green. Reversible. → .gitattributes, bench/suite/L3-mcp-calc-search/oracle/validate.mjs, README.md, this PR.
- **2026-06-15** · **L4 CRM flagship benchmark task built + dogfooded (PR #102).** Operator: keep working on the test suite + use the engine's own loop to test it on itself. Built `bench/suite/L4-crm-api/` — the "build a CRM REST API" task: task.md (a thick spec: Contact/Deal entities, CRUD, RBAC via X-Role owner/editor/viewer, input validation, referential integrity), a zero-dependency reference CRM (solution/server.mjs — node:http, in-memory store, RBAC, 400/403/404/409 semantics), and an HTTP acceptance ORACLE (oracle/verify.mjs) with GATING criteria per the hardened plan: PRIMARY CRUD+filter round-trips (.35, partial-credit), GATING-RBAC viewer-cannot-write (.30, PASS-REQUIRED), GATING-INTEGRITY 409-on-dangling-reference (.20, PASS-REQUIRED), VALIDATION/held-out 400s+conditional-delete (.15, partial). A failed gating group CAPS the score at 0 (a broken-RBAC CRM is NOT partially shippable). **Validity gate PROVEN** (oracle/validate.mjs): the reference scores exactly 1.0; a broken-RBAC build (canWrite→true) scores 0 with gated_fail=['rbac'] (THE key security-gating proof — not a deceptive 0.65); broken integrity → 0; a non-gating miss (no-op filter / removed email-validation, gates intact) → partial credit between 0.5 and 1 (proves gating is not all-or-nothing); a crashing server → low. **Dogfooded end-to-end** (run-suite.mjs): a live Sonnet agent build produced a working CRM the oracle scored 0.93 (primary 4/5, rbac 4/4, integrity 2/2 = gates passed, validation 4/4) in 2 turns / $0.11 / 28s — a real partial-credit measurement with the security gates holding. **Two oracle bugs were found by RUNNING the oracle, not reading it (proving the plan's thesis that authoring the oracle is the hard part):** (1) a Windows libuv teardown assertion (exit 127) from fetch/undici keep-alive sockets lingering at process exit → switched the oracle's HTTP client to raw node:http with agent:false (no socket pool) + a child-close-then-exit shutdown; (2) a launch-detection brittleness — scraping an exact `LISTENING <port>` stdout line false-failed a correct server that logged differently (an ABC task-validity defect: it scored a real build 0) → switched to assigning a known PORT and HEALTH-POLLING /health until ready (stdio ignored, robust to the agent's output format). Both fixes re-validated (reference still 1.0, cheats still caught). Updated bench/suite/README.md (CRM row + both measured results + the authoring note) and testing-suite-plan.md §0.5 (CRM marked built). bench/ is isolated dev-tooling (no verify.sh/hooks/runtime coupling; results+temp-run-dirs gitignored). NEXT: the L1 rung; the L5 hard tier (L4-torture/circuit-breaker testing the engine's two-strike limit under a broken env, security-by-abuse-tests, the ambiguity trap). verify.sh PASS; both task validity gates green. Reversible. → bench/suite/L4-crm-api/** (task+reference+oracle+validate), bench/suite/README.md, bench/testing-suite-plan.md, this PR.
- **2026-06-16** · **L4 PUT task-validity fix (caught by `--repeat`).** Three independent L4 builds scored a deterministic 0.93 with the IDENTICAL group breakdown (primary 4/5). A stable miss across independent runs is a task-validity signal, not model noise: the prompt's "PUT … same validation" is reasonably read as full-replace (email required), so the oracle's name-only PUT correctly returned 400. This is the ABC dual-validity defect (the oracle, not the agent, was wrong). Fix: oracle now sends the full `{name,email}` on the PUT check (passes under BOTH full-replace and partial-update readings) + task.md clarified to "send both name and email". Reference still 1.0; validity gate still green; builds now score 1.0/pass^2. Chose the both-readings-valid fix over forcing partial-update semantics (canonical PUT is a full replace; don't fight the convention). Reversible. → bench/suite/L4-crm-api/{oracle/verify.mjs,task.md}, this PR.
- **2026-06-16** · **`run-suite.mjs` made reliability-capable (`--repeat N`).** The runner used `writeFileSync` (each run overwrote the prior record) → every "proven 1.0" was n=1, an anecdote (plan §6.5). Switched to `appendFileSync` + a `--repeat N` loop that reports pass-rate, score/cost/turn spread, and (for gated tasks) gate-hold-rate — tau-bench's `pass^k`. Also set L4 `pass_threshold` 1.0→0.85 (a perfect score is an unrealistic pass-bar for a partial-credit oracle; the binary gates carry the safety signal independently). Reversible. → bench/suite/run-suite.mjs, bench/suite/L4-crm-api/meta.json, this PR.
- **2026-06-16** · **Operational incident + rule: no in-repo headless builds.** An ad-hoc L4 diagnostic ran `claude -p` (Bash + acceptEdits) from the repo working tree to inspect a build; that agent wrote `server.mjs` to the repo root and ran `git commit`+`push`, sweeping uncommitted harness edits + its artifact into a stray commit on `feat/bench-reliability` and fabricating a PROGRESS entry. No PR opened; develop untouched. Recovered with `git reset --mixed` (dropped the commit, kept the real edits), `git push --delete` of the stray remote branch, and `git checkout --` of the fabricated PROGRESS entry. Root cause: a headless agent with write+shell reach, run inside the live working tree, will commit. `run-suite.mjs` already isolates every build to an `os.tmpdir()`/`repo/tmp` workdir for exactly this reason — the diagnostic bypassed it. Rule: never run an in-repo headless build with commit reach; always isolate (temp workdir) or restrict tools. Reversible (incident closed). → this entry; run-suite.mjs isolation already enforces it for the harness path.
- **2026-06-16** · **Harness-property gauntlet (G1–G4) BUILT + design grounded (single PR).** Operator /goal (crash-recovery): reproduce the crashed session's loop from the two Desktop files (`project-blueprint.md` + `suggestedtests.md`) — the four-assistant convergence on a four-layer **engine-effect** gauntlet. Built the three missing tiers (`G1-strict-json` output-discipline, `G2-context-trace` long-context cross-cutting trace, `G3-dag-pipeline` mandated DAG) and confirmed `G4-circuit-breaker` (the crash interrupted its dogfood run 2). Each = task + fixtures + reference solution + uncheatable oracle + validity gate, matching L1/L4/G4 conventions; all four **validity-gated and dogfooded `pass^2` (1/1/1, every group full marks)**. `run-suite.mjs` gained backward-compatible nested-fixture support (mkdir -p) for G2's monorepo. **DECISION (deviation from one-PR-per-feature):** shipped the whole gauntlet as ONE PR, not four — the tiers are a single cohesive deliverable sharing the run-suite change + README/plan updates, and (a department-rooted session doing engine-dev) one outward-facing merge is the more conservative shape. **DECISION (grounded sources over the desktop notes' citations):** the desktop files were riddled with unverified/fabricated-looking citations; a background research workflow live-verified the design against real 2026 eval literature (Harness-Bench arXiv 2605.27922, "Stop Comparing LLM Agents Without Disclosing the Harness" 2605.23950, ImpossibleBench's 54%→9% abort-affordance result, EvilGenie, tau-bench pass^k) plus a citation-forensics pass → `bench/HARNESS-RESEARCH.md` (§3 lists the refuted/misattributed/provisional claims kept OUT of the repo: teamai.com/attainmentlabs.com misattributions, the Opus 4.8=88.6%/Fable 5=95% single-aggregator numbers). Adopted the *designs*, grounded with *verified* sources (P1). verify.sh PASS (326 hook tests, mutation smoke all-killed); all 7 suite validity gates green. bench/ remains isolated dev-tooling. Reversible. → bench/suite/{G1-strict-json,G2-context-trace,G3-dag-pipeline}/**, bench/suite/G4-circuit-breaker/**, bench/suite/run-suite.mjs, bench/HARNESS-RESEARCH.md, bench/suite/README.md, bench/testing-suite-plan.md, this PR.
- **2026-06-16** · **Repo review + README rewrite; STATUS regen; metrics-gap documented (not fabricated).** Operator /goal: review repo, rewrite README, fix pressing, test, push. A read-only audit workflow (README accuracy / pressing issues / bench state) found the engine mechanically healthy (`--validate` green, 326 hook tests, 10/10 mutants) but two operator-facing artifacts had drifted. FIXES: (1) README "Measuring" + "Direction" sections rewritten — they framed the benchmark as unbuilt future work; it is now built across both axes (capability ladder L1/L3/L4 + harness-property gauntlet G1–G4, all validity-gated + dogfooded), with the engine-effect comparison named as the next step; reliability stated precisely; Node floor clarified (CI runs 24). (2) STATUS.md regenerated (stale at 06-14, contradicting 10+ newer PROGRESS entries). (3) bench/suite/README G4 turns ~8.5→~9.5 (actual recorded mean — honest-measurement hygiene). **DECISION (metrics):** 15 done features lack `metrics.jsonl` records; the records are hand-curated review verdicts, not reconstructable without fabrication, and the data is advisory (no per-subagent token telemetry exists). Chose to DOCUMENT the gap as a known state + flag the top `/kaizen` candidate (backfill-from-git OR add a completeness cross-check to `update-state --validate`, which currently validates only record *format*) rather than invent 15 verdict records — fabricating "looks-complete" data is exactly what this engine's evidence discipline forbids. **NOT fixed (deliberate, recorded):** CLAUDE.md's `<REPO_NAME>` title placeholder while OPERATOR_GUIDE has "ForgeOps" — a known template-vs-product seam, left as status-quo template-mode (no code impact). Docs only, no guard surface; bench/ remains isolated dev-tooling. verify.sh PASS; 7/7 validity gates green. Reversible. → README.md, roadmap/STATUS.md, bench/suite/README.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Guardrail hardening: assertion-shield diff-header fail-opens closed (BLOCK→fix→re-review).** Operator /goal: several self-improvement cycles. An explore workflow ranked a backlog; cycle 1 = the highest-value pick. `assertion-shield.ts` keyed on literal `--- a/`/`+++ b/` diff prefixes; ANY header-shape-mutating gitconfig/attribute blanked `currentFile` so a gutted test passed (silent fail-open of verify.sh's primary anti-coverage-erosion gate). Closed SEVEN config-reachable mutations — `diff.noprefix`, `diff.mnemonicPrefix`, `diff.srcPrefix`/`dstPrefix`, `core.quotepath`(non-ASCII), `color.ui=always`(ANSI), `diff.external`, `.gitattributes -diff`/binary — via `git -c core.quotepath=false diff -M --no-ext-diff --no-color --text --src-prefix=a/ --dst-prefix=b/` plus stripping git's trailing TAB on space-paths during extraction; each with a teeth-test (proven fail-open pre-fix). **Process (dogfooded the engine's JUDGE step):** the first fix closed only 4/7; a fresh-context security review BLOCKED it (empirically found color.ui/space-path/-diff); fixed comprehensively; a second fresh review APPROVED (reproduced all closures, tested ~25 configs, no regressions to -M rename / first-commit / --cached / existedOnBase). 326→333 hook contract tests. **Accepted residual (deliberate):** a filename with a literal tab/newline/quote/backslash makes git double-quote the whole header (`--- "a/…"`) → fail-open; reachable only via a pathological filename (not a one-line config) and illegal on Windows NTFS → out of the stated threat model; optional POSIX-only follow-up = also parse the `diff --git "a/…"` header form. Security-sensitive guard change. verify.sh PASS; 7/7 validity gates green. Reversible. → scripts/assertion-shield.ts, scripts/test-hooks.sh, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Guardrail hardening: two path-authz bypasses closed (NotebookEdit + ./scripts rescope); BLOCK→fix→re-review.** Operator /goal: self-improvement cycles (cycle 2). (1) Both PreToolUse guards (path-guard.js, verify-gate.sh) wired to NotebookEdit read only tool_input.file_path; a NotebookEdit's notebook_path was unread → path-guard exited 0 (out-of-scope .ipynb writes bypassed per-feature authz) and verify-gate false-blocked authorized notebooks. Fix: both read file_path||notebook_path (all three verify-gate extractors: jq/node/sed). (2) update-state.ts --paths GUARD_SURFACES was start-anchored; `./scripts/**` (+ `/scripts/**`, `\scripts\**`, drive-letter forms) slipped, then a guard stripped the prefix and authorized gate-script edits. Fix: normalize (backslash/drive/`//`/leading-`./`/leading-`/`) before the checks; reject empty-normalizing globs. **Process — engine caught my own regression:** the first cut also unioned `scripts/**` into SAFETY_FORBIDDEN (every --add'd feature); a fresh security review BLOCKED it because --add has no guard-surface check on authorized_paths and 17/33 historical features were scoped to scripts/* → forbidden-wins would brick them. Reverted scripts/** from SAFETY_FORBIDDEN; the --paths normalization is the actual fix. A second fresh review APPROVED (verified an --add script-scoped feature can edit its script; .claude/.github still force-forbidden; all bypass spellings rejected; no regression). **Accepted residual (deliberate):** a --paths glob normalizing to a bare `.`/`./.`/`/./` is accepted but grants nothing (fail-closed) → harmless no-op scope; optional follow-up to reject. **Pre-existing, noted not fixed:** `.github/` beyond `/workflows/**` (e.g. dependabot.yml/CODEOWNERS) isn't in the forbidden union (protected only by GUARD_SURFACES + a weak-glob divergence); the verify-gate-vs-path-guard wildcard glob-engine divergence (composite fail-closed). Security-sensitive guard change; 333→347 hook contract tests (teeth-tested). verify.sh PASS; 7/7 validity gates green. Reversible. → .claude/hooks/path-guard.js, .claude/hooks/verify-gate.sh, scripts/update-state.ts, scripts/test-hooks.sh, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Metrics-completeness WARN in --validate (pre-groomed kaizen, now built).** Operator /goal: self-improvement cycles (cycle 3). Closes the blind spot logged in DECISIONS 2026-06-12 + the phase-3 README pass: `update-state.ts --validate` validated metrics RECORD FORMAT only, so 15/33 done features silently lacked metrics.jsonl records and /kaizen + /status sampled a subset. `--validate` now builds a set of feature ids seen in metrics and `console.warn`s (NON-FATAL, mirroring the model-policy-staleness WARN precedent at the same site) listing every status:done feature absent from it. Chose WARN over hard FAIL: a hard gate would block on the pre-existing legacy gap (and the prior decision was NOT to fabricate the 15 historical verdict records). The WARN now fires on every verify.sh run against the real backlog, making the gap visible until backfilled — no fabrication, no silent drift. 3 contract tests (STATE_FILE+METRICS_FILE fixtures; WARN fires / non-fatal / silent-when-complete). 347→350 hook contract tests. Additive gate change, no guard weakened. verify.sh PASS; 7/7 validity gates green. Reversible. → scripts/update-state.ts, scripts/test-hooks.sh, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Reviewer-feedback response (cycle 1): CI hardening — E2E on UI paths + npm-audit gate.** Operator /goal: triage the 3-reviewer feedback (Desktop feedback-20260616.md), fix meaningful items 3-source-first. A verification workflow (4 lanes + web research) classified each item vs the actual repo. Two real-actionable CI gaps fixed: (1) [2 sources] E2E (`e2e.yml`) only triggered on `src/**` — added `app/**`+`components/**` so a pure demo-UI change auto-runs E2E (kept `verify.sh --e2e` as local opt-in; the changed set belongs in CI path filters, not verify.sh). (2) [1 source] no dependency scanning → added `npm audit --omit=dev --audit-level=high` to ci.yml; baseline 0 vulns so it is gating. **Verified-but-NOT-changed (already-mitigated / out-of-scope; recorded so they read as deliberate, not oversight):** atomic features.json write already done (temp-then-rename); single in_progress is an intentional load-bearing invariant (path-authz scope), not a limitation; path-authz duplication already parity-tested (test-hooks.sh F-0034); assertion-shield "can't prove coverage" is outdated (mutation-smoke wired into the gate); "unverifiable benchmark artifacts" is false (rerun cmds + per-task cost/model/turns + commit SHAs already in bench READMEs + result filenames); guardrails-not-a-sandbox + the broad allowlist/Trusted networkPolicy already disclosed (a real OS sandbox is backlog #42, XL); SBOM deferred (no deployable artifact yet); physical engine/demo split = XL (the install-into allowlist that excludes app/, src/forge/, components/, e2e/ is the proportionate mitigation). Doc-honesty additions + CHANGELOG land in cycle 2. CI-config change, no guard weakened. verify.sh PASS; 7/7 validity gates green. Reversible. → .github/workflows/e2e.yml, .github/workflows/ci.yml, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Reviewer-feedback response (cycle 2): demo honesty labeling + README + CHANGELOG.** Operator /goal: fix meaningful feedback 3-source-first, update README, add patch notes. (1) [3-source] ForgeOps landing page (`app/page.tsx`) now self-labels as simulated: above-the-fold banner + "INTERACTIVE DEMO (SIMULATED)" badge + hero subhead reworded to name the mocked surfaces (removed the false "real-time monitoring"/"production-grade export" specifics). Kept the aspirational h1 ("operating system for AI agent teams") as a clearly-labeled vision tagline rather than deleting it — BRANDING-TONE change, flagged for operator veto per frontend.md (the banner makes the demo nature unmissable, which is the reviewers' actual concern). (2) README: "Why a harness, not a framework" section (the 2-source "reinventing the wheel"/LangGraph-CrewAI critique answered as a deliberate harness-engineering bet, citing the 9.5pt harness-swing evidence in HARNESS-RESEARCH.md); formatter-not-enforced honesty note; demo-limitation updated. (3) SECURITY.md safeguards now list the npm-audit gate. (4) Created CHANGELOG.md (we had none; the operator asked, and optional-modules.md already contemplated it as the upgrade-tooling cheap slice) in Keep-a-Changelog [Unreleased] form. (5) optional-modules.md: SBOM recorded as a deliberate trigger-gated deferral (no deployable artifact yet); zizmor-workflow-audit trigger marked fired by cycle-1's CI change. All feedback verified item-by-item first (4-lane workflow + web research) — most was praise / already-mitigated (atomic writes, mutation-smoke, documented benchmark artifacts, install-into demo exclusion) / out-of-scope (real OS sandbox #42, physical engine-demo split, provider adapters), recorded so each reads as deliberate. Docs + demo-copy only, no guard/engine code. verify.sh PASS; 7/7 validity gates green; E2E green in CI. Reversible. → app/page.tsx, README.md, SECURITY.md, CHANGELOG.md, docs/optional-modules.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Bench measurement integrity: gate `pass` on `finished` (a timed-out/errored build is no longer a pass).** Operator /goal: keep working on meaningful things. `run-suite.mjs`'s `isPass` was `¬dq ∧ score ≥ threshold` — it ignored the recorded `finished` flag, so a build that errored or hit the `claude -p` timeout but left a scoreable entrypoint was counted as a clean pass, inflating `pass^k` (a broken measurement is worse than none — the suite's own thesis applied to the suite). Fix: `isPass = finished === true ∧ ¬dq ∧ score ≥ pass_threshold`. Extracted the pure logic to `bench/suite/lib/reliability.mjs` (run-suite executes a suite on import, so its internals couldn't be imported for a unit test) + added `lib/reliability.test.mjs` (6-case node --test: the finished-gating fix + DQ / threshold / missing-score / missing-finished). Verified end-to-end with a live G1 dogfood ×2 (finished builds still ✓pass → the gating doesn't break legit passes); no prior recorded dogfood result changes (all were finished:true). The self-test follows the bench's own validate.mjs pattern — manual/standalone, NOT wired into verify.sh (bench stays isolated dev-tooling per the standing decision). bench-only change; no guard/core code. Reversible. → bench/suite/run-suite.mjs, bench/suite/lib/reliability.mjs, bench/suite/lib/reliability.test.mjs, bench/suite/README.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Engine-effect comparison: scoped (ENGINE-EFFECT-PLAN.md) + the model-pinned harness/A1 context arm built.** Operator /goal: take on the highest-value XL item + scope it properly. (1) SCOPE: `bench/ENGINE-EFFECT-PLAN.md` defines the model-pinned comparison with arms isolating each engine dimension — A0 baseline · A1 context (distilled discipline prompt) · A2 gate-retry · A3 review-fix (the engine's signature fresh-context adversarial-review loop) · A4 full `/work` loop — plus methodology, the §6.5 statistical reality (categorical signals only at affordable N), the ceiling problem (all current tasks baseline ~1.0 → a headroom task where a bare build categorically fails is the prerequisite for A2–A4), validity threats + a phased plan. KEY DESIGN CALL: A1 injects a DISTILLED discipline (`engine-context.md`), not raw builder.md/CLAUDE.md — the raw artifacts' repo-plumbing (features.json/branches/evidence) would handicap a temp-dir build with confounding noise; distillation tests the transferable instruction layer fairly (version-controlled + inspectable). (2) FEASIBILITY verified skeptically: a claude-code-guide agent erred (claimed `--output-format json` lacks `num_turns`; run-suite reads it → false), so I confirmed flags via `claude --help` (`--append-system-prompt`, `--bare` real). Clean baseline needs no `--bare` — run-suite's os.tmpdir is already context-free; both arms now run there so the only delta is the injected prompt. (3) BUILT: `--ctx engine` (was a no-op repo/tmp workdir toggle) now injects `engine-context.md` via `--append-system-prompt`. Smoke A/B on G1: A0 1.0/$0.19 vs A1 1.0/$0.20 — identical at the ceiling, exactly as the §6.5/ceiling analysis predicts; the harness works, the signal awaits a Phase-B headroom task (honest sequencing over a premature %-better claim). A2–A4 + the headroom task groomed in the plan as the next builds. bench dev-tooling; verify.sh untouched; 7/7 validity gates green. Reversible. → bench/ENGINE-EFFECT-PLAN.md, bench/suite/lib/engine-context.md, bench/suite/run-suite.mjs, README.md, bench/testing-suite-plan.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Engine-effect Phase B+C built; first signal attempt → baseline at ceiling (honest negative result).** Operator /goal: build Phase B+C to get the first real engine-effect signal. Phase B: `H1-notes-ownership` — an in-memory notes module, owner-only update/delete (IDOR crux) as a GATING oracle group, validity-gated (ref 1.0; IDOR build gated 0; over-block caught), worked examples all owner-acting to bait the cross-user omission. Phase C: `run-effect.mjs` — paired A0-vs-A3 (build once → score A0 → independent spec-review → fix → score A3), model PINNED (added `--model` to run-suite + run-effect; also fixed run-suite's build-log to show the override). **FINDING:** the headroom check failed — a bare one-shot scored 3/3=1.0 with sonnet AND 3/3=1.0 with haiku (weak-baseline amplifier). Current models reliably implement a clearly-stated rule one-shot, so "stated-rule-omission" yields NO headroom and a weaker model doesn't help. DECISION: did NOT keep iterating tasks / burning dogfood to manufacture a failure — a real headroom task needs a hard-correctness target (idempotency / a subtle stateful invariant / a larger multi-file task), which is genuine research (scoped, ENGINE-EFFECT-PLAN §8). Validating run-effect caught + fixed a real pass-eval bug (`score` vs `oracle_score` field); on H1 (ceiling) it correctly reports A0 2/2 / A3 2/2 / 0 flips / "ceiling — no signal." The engine-effect MECHANISM is built + proven; the SIGNAL awaits a hard headroom task. bench dev-tooling; verify.sh untouched; 7/7 + H1 validity gates green. Reversible. → bench/suite/H1-notes-ownership/**, bench/suite/run-effect.mjs, bench/suite/run-suite.mjs, bench/ENGINE-EFFECT-PLAN.md, bench/suite/README.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Larger headroom task (H2-shared-docs): also baseline-ceiling → engine-effect signal redirected off greenfield.** Operator /goal: build one larger headroom task to surface a real signal. Built H2-shared-docs (6 functions / ~8 rules; 4 GATING access cruxes: view≠edit, owner-only-delete, owner-only-share, listDocs-no-leak; validity-gated: ref 1.0, each dropped crux → gated 0, primary-miss partial). A0-vs-A3 (run-effect ×4, sonnet pinned): **A0 4/4=1.0, A3 4/4, 0 flips** — even the larger multi-rule task is ceiling; the review correctly found nothing to fix. FINDING (now 3 configs: H1/sonnet, H1/haiku, H2/sonnet): strong models are at the ceiling on greenfield build-to-spec at this scale → no attention-lapse misses for the review-fix loop to recover → the categorical engine-effect signal does NOT appear on greenfield build tasks of this size. DECISION: stop enlarging greenfield tasks (diminishing odds + edges toward manufacturing); redirect (PLAN §9) to (a) refactoring/regression tasks [cheapest, most likely to flip — strong models DO break subtle behaviour mid-refactor, a behaviour-diff review catches it], (b) SWE-bench-scale multi-file [Docker], (c) process/behaviour-flag metrics not oracle score [tiering/security-review/awaiting_approval — a greenfield oracle can't see them]. H2 remains a valid validity-gated access-control task (capability/regression guard), just not a headroom task; run-effect harness proven, awaiting a route-(a) task. bench dev-tooling; verify.sh untouched; all suite validity gates green. Reversible. → bench/suite/H2-shared-docs/**, bench/ENGINE-EFFECT-PLAN.md, bench/suite/README.md, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-16** · **Purged the ForgeOps demo — repo is now an engine-only template.** Operator intent (plain English): "gutting all of this … this is an engine, as lean and lightweight as possible, maximum power-to-weight … purge every last bit of it." Confirmed two scope calls before deleting: (a) purge ALL of `src/forge` (demo feature logic, not engine) and rewire the safety gate; (b) remove ALL demo-subject backlog rows, not just the 5 obvious ones. **Decisions & judgment calls:** ① Deleted demo code/config/deps (`app/ components/ lib/ e2e/ src/forge/`, Next/Playwright/Tailwind config, Dockerfile/compose, e2e.yml; all 9 runtime deps + UI devDeps). ② **Kept** `scripts/seed.ts` and the `verify.sh --e2e` contract as *generic* engine infra (not ForgeOps) — purge the demo, not the engine's general muscle. ③ Backlog 33→21: removed F-0017–0021 + F-0023 (forgeops epic) + F-0024/0028/0029/0031/0032/0033 (hardening/deps rows whose entire subject was now-deleted demo code); kept F-0002 (engine health-check) and F-0016 (adoption fixes) which only matched on generic words. ④ Added `update-state.ts --remove` (the sanctioned writer; hand-edit is hook-blocked) with **cascade dependency cleanup** — it strips removed ids from remaining rows' `dependencies` (F-0024/F-0029 had dangled on F-0021) and re-validates; covered by a new `test-hooks.sh` contract test (unknown-id rejected, cascade proven by clean exit). ⑤ **Safety gate preserved, not weakened:** removed the rbac mutation block (rbac.ts is gone) but mutation-smoke now runs *unconditionally* and still proves teeth on the **state writer + assertion shield** (the engine-critical targets); `--selftest` made **hermetic** (own throwaway fixture). ⑥ **KEY CALL — the assertion-shield is left UNTOUCHED.** Purging the demo deletes 231 test assertions that existed on `develop`, which the shield correctly flags; the bypass is refused in CI and prohibited for agents (CLAUDE.md §7). Rather than weaken a core guardrail, the operator **admin-merges** past the (correctly-firing) shield — the shield's own documented human-override for *genuine feature removal*. Every other verify step is green; the shield being the sole red check is expected and called out in the PR. Considered & rejected: enhancing the shield to auto-exempt "test deleted because its SUT is also deleted" — principled but security-sensitive and fiddly for e2e specs (no source sibling); deferred as a possible future kaizen rather than rushed under this purge. Reversible (git history retains all deleted code + rows). → app/**, components/**, lib/**, e2e/**, src/forge/**, scripts/update-state.ts, scripts/mutation-smoke.sh, scripts/verify.sh, scripts/test-hooks.sh, bench/micro.mjs, package.json, tsconfig.json, roadmap/features.json, README.md + docs, roadmap/PROGRESS.md, this entry, this PR.
- **2026-06-17** · **assertion-shield chai/should + must.js coverage (fix, attempts 0).** Pre-existing fail-open found by a 3-reviewer pass on PR #119 (byte-identical on develop, NOT introduced by the purge): a test asserting ONLY via a chai `.should`/`.must` chain (`result.should.equal(42)`, `x.must.be.true`) carries no `expect(`, so `assertionKeywords` saw no assertion in it — its assertion line could be deleted while the `it()`/`test()` wrapper stayed and the shield PASSED. Added `'.should'`, `'.must'` (leading dot = precise — matches the method-call accessor, never the bare English word "should"/"must"; comment-only lines already skipped) and normalized the spaced `'expect '` spelling. Adding keywords to the OR-of-`includes` only ever WIDENS blocking, never narrows it — fail-SAFE: the residual cost is over-blocking (`'.must'` also substring-matches `.mustache`; a deleted prose string containing "expect "), clearable by a human via the existing local-only `ASSERTION_SHIELD_BYPASS`, never a fail-open. Two hook-contract tests added (`ASCH` fixture in test-hooks.sh): deleting a `.should` assertion line from a base-committed test is BLOCKED (exit 1), and a legitimate non-assertion deletion still PASSES (exit 0). **Routing (decide-and-document):** implemented at the orchestrator level rather than via a builder — the operator's task fully specified the surgical change, and the constitutional guarantee for a safety-hook touch is the *independent fresh-context* JUDGE gates, which were applied in full: evaluator **PASS** (6/6 criteria; empirically confirmed the BLOCKED test fails against the OLD keyword list → non-vacuous) and security-reviewer **APPROVE** (no fail-open; over-blocking fail-safe; leading-dot precision verified `.must` ∌ `muster()`; diff limited to the two files + evidence). **verify.sh:** every gate this change touches is green (assertion shield, 352/0 hook contracts incl. the 2 new, mutation smoke kills the shield mutant, biome, shellcheck, model-policy, features.json); `typecheck`/`build` are RED locally ONLY because this branch carries develop's full ForgeOps code while local node_modules is the post-purge slimmed set (missing react/next/@playwright/test) — every error is in untouched files (components/, e2e/, *.config.ts); CI installs develop's full manifest and runs them green. Cut off `origin/develop` because the >1MB fail-open fix (PR #119, different function) is not yet on develop; no overlap. Reversible. → scripts/assertion-shield.ts, scripts/test-hooks.sh, roadmap/evidence/fix-shield-chai-assertions/, this entry, this PR.
- **2026-06-17** · **Power-to-weight audit + lean cuts (operator /goal); evidence-"dead-weight" finding REVERSED.** A brutal whole-repo review (4 explorer scouts + live web verification) audited every file for power-to-weight. **Verified-good (credit, not cut):** the always-loaded tax is small (CLAUDE.md 52 lines vs its ≤150 budget); the guardrail layer earns its 4.6:1 test:code ratio; the load-bearing external claims are REAL (arXiv 2605.23950's 9.5pt SWE-bench Pro harness swing quoted accurately; the Fable 5 / Opus 4.8 / Sonnet 4.6 / Haiku 4.5 lineup + pricing; GHSA-8q5r-mmjf-575q dated 2026-05-20 — all confirmed against primary sources). **Cuts shipped (PR #124):** orphan evidence F-0027b/F-0027c (purged sub-features, cited by no feature); dead `scripts/orchestrator-status.ps1` (0 refs repo-wide); `db-engineer` agent + `database.md`/`frontend.md` rules relocated to docs/optional-modules.md (path globs schema/migrations/app/components never match the engine-only tree; synced model-policy.json agents map + PLAN §3 tree/§5.3 roster); PROGRESS archived 1213→509 lines per §4.3 (the engine was 2.4× over its own cap). **CORRECTION (so it doesn't recur):** the audit's headline "evidence logs = 39% dead receipts, archive ~8,300 lines" was WRONG on inspection — `update-state.ts --validate` (run in verify.sh + CI) re-verifies every passing feature's evidence on disk (`collectEvidenceErrors`), and the untethered `fix-*` evidence dirs are cited in DECISIONS/PROGRESS as the audit trail for security judgment calls. Evidence is load-bearing dogfooding, NOT fat; only the 2 orphaned deleted-feature dirs were truly dead. Reversible (git history). → PR #124, roadmap/PROGRESS.md, this entry.
- **2026-06-17** · **bench/ kept (not extracted/deleted) + README oversell fixed + PLAN e2e drift fixed.** Audit flagged bench/ (4,164 lines, 18% of the repo) as un-wired to verify.sh/CI/the loop with its engine-effect delta producing no signal (ceiling effect). DECISION: KEEP it — it is a working measurement tool (atomic probes 7/7; oracle suite dogfooded 1.0, validity-gated) and the engine's falsifiability apparatus; deleting it would gut the README harness-engineering thesis and discard good validity-gated work, and extracting to a long-lived branch fights the branch-consolidation goal. Instead fixed two real docs-vs-reality gaps: README claimed the "benchmark that makes engine changes falsifiable" shipped while admitting elsewhere the delta is "no signal yet" (reconciled to "benchmark harness that measures engine changes … delta not yet obtainable"); PLAN §3 tree + §7.2 still listed `.github/workflows/e2e.yml` (deleted in the ForgeOps purge) — re-marked optional/adopter-added (already the "E2E / staging lane" in optional-modules.md). Reversible. → README.md, AI_OPERATIONS_PLAN.md, this entry.
- **2026-06-17** · **Branch consolidation (operator /goal): all non-main branches merged/closed into develop.** Merged PR #124 (lean cuts) + Dependabot #121 (claude-code-action 1.0.142→1.0.150 — the §7.2 SHA-pin roll, keeps the action ≥1.0.74 per the GHSA hardening) + #122 (@types/node 25.9.3). #123 (shellcheck 3.1.0→**4.1.0**, MAJOR) conflicted with #122 on package-lock after #122 merged; rather than wait on async `@dependabot recreate`, folded the shellcheck bump into this consolidation PR (regenerated package-lock.json clean against current develop) and closed #123. shellcheck 4.1 pulls dev-only transitive deps carrying 5 moderate advisories — NON-gating: CI audit is `npm audit --omit=dev --audit-level=high` (prod + high/critical only), matching #123's own green CI. End state: only protected `main` + `develop`. Reversible. → package.json, package-lock.json, .github/workflows/claude.yml (#121), this entry.
- **2026-06-18** · **Two-products strategy: bounded "LLM-wheelhouse" harness vs the AFK engine (operator /goal; design record — DOCUMENTED, not executed).** Operator pivot note + the *AI Operations Loop* blueprint reflect two genuinely different approaches; the operator asked whether to "branch or fork." Ruling, grounded by a source-verified `/deep-research` run (107 agents / 25 sources / 16 claims confirmed, 9 **killed**) + Anthropic's *2026 Agentic Coding Trends Report*: these are **two products**, not one product with a setting — **2 of the 6 README principles invert** (full-AFK #2, non-technical-operator #3); the admission gate additionally flips from *risk tier* (a `TASK_AUTONOMY_TRIAGE` mechanism, not a principle) to *one-shot solvability*, so one ≤150-line `CLAUDE.md` cannot hold both. **Structural decision (the refined answer to "branch or fork"): fork at the constitution/loop/admission layer, but SHARE a *narrow, product-neutral* guardrail core** (guard-bash, verify-gate, assertion-shield, mutation gate, evidence-on-disk audit, the fresh-context evaluator + security-reviewer) — duplicating the *safety* code is the dangerous divergence (research: both products need the same referees). **Corrected by the red-team panel:** the shareable surface is narrower than the whole guardrail layer — `update-state.ts`/`verify.sh` hardwire B's tier / `awaiting_approval` / model-policy machinery and `path-guard` assumes the single-`in_progress` backlog model, so decoupling those from the state writer is **net-new work, not free inheritance**; and `install-into.sh` is a one-way bundle copier, *not* an existing core-vs-shell architecture. A one-repo / two-profile shape is also on the table (escalated below). The shared core's price: a cross-product regression gate (a core change must pass *both* suites) or a B-edit can silently regress A. **Load-bearing correction recorded so it doesn't get lost:** "stay inside the LLM wheelhouse and we're good" is **necessary but not sufficient** — bounded scope removes the *error-compounding / self-conditioning* failure mode (Sinha 2509.09677; METR 2503.14499) but does **nothing** for the *verification gap*, which persists single-shot (agents overconfident up to 55pp, arXiv 2602.06948; 2026 models reward-hack 49–54% of *impossible* tasks, ImpossibleBench 2510.20270). So the bounded product still needs deterministic referees (hidden/read-only tests near-zero cheating; abort signal 54%→9%) + strict context engineering (>85%→1% cheating). The reliable working set is a *fraction* of the nominal window (Chroma context-rot; NoLiMa 2502.05167), and the trend is toward **more** operator skill, not less. **Do-not-rely list (adversarially killed):** clean failure-vs-duration curve, a single "task half-life", exponential per-step decay, multiplicative subtask compounding, "<1K-token only" reliability, vendor +29/+39% context-editing figures. **Escalated (irreversible/operator-visible):** whether to actually stand up the bounded product now, naming/positioning, and pause-or-parallel the AFK backlog — `QUESTIONS.md` Q-0003. **Conservative default while unanswered:** document-only — no fork created, the engine's constitution untouched, the 21-feature backlog untouched. Reversible (a design record). → docs/bounded-vs-afk-strategy.md, roadmap/QUESTIONS.md Q-0003, this entry.
- **2026-06-18** · **One-shot tool green-lit + reduced-scope groom (operator answered Q-0003).** Operator: "build the new one-shot tool now; reduce the scope, be precise about what we can do; if it works here, that is the start; keep power-to-weight high, all substance not fluff." Folded Q-0003 (answered): Q1 build now → YES, but as a deliberately minimal seed built **here** (not a fork — the lighter reversible start; full fork stays deferred until the seed proves out, overriding the strategy record's *unanswered*-state document-only hold); Q3 pause/parallel → moot (AFK backlog already 21/21 done); Q2 name/positioning → still operator's open call, deferred (builds under the neutral dir `src/oneshot/`, no brand needed yet). **Scope decision (decide-and-document):** a 2-feature minimal loop — F-0040 one-shot **solvability admission gate** (single machine-checkable acceptance + working-set token budget) and F-0041 **evidence-gated verdict** (PASS only with exit-0 proof on disk; ignores the agent's self-claim) — the smallest "gate what you attempt → prove the result" loop. Built in isolated `src/oneshot/` (reuses the existing `verify.sh` src test-coverage guard for free → no touching the shared safety core), Tier B (reversible, non-security, real test oracle, novel glue → not A/C), **no new dependencies** (node builtins only → `package.json` touch is test-wiring, not a deps change → no mandatory security-reviewer). The richer human-referee / calibrated-doubt UX (strategy §5.4) is deliberately deferred (reduce-scope). **Fresh-context evaluator** reviewed the groom pre-add: F-0040 PASS, F-0041 NEEDS_WORK → fixed (tightened the negative-test mechanism so it can't pass vacuously; bound the F-0040→F-0041 dependency to a real shared task-descriptor contract; told the builder to mirror the engine's existing capture semantics, not diverge). Reversible (`update-state.ts --remove`). → roadmap/features.json (F-0040, F-0041), roadmap/QUESTIONS.md Q-0003, docs/bounded-vs-afk-strategy.md §5, this entry.
- **2026-06-18** · **F-0040 shipped (one-shot admission gate) + 2 engine bugs found & groomed.** Built the first one-shot-tool feature (`src/oneshot/admit.js`) via the /work loop; fresh-context evaluator PASS (5/5, 16/16 non-vacuous tests, no new deps, scope clean), merged PR #129. Two REAL engine defects surfaced and were groomed Tier C (priority 1), not patched inline — guard-surface fixes get a dedicated security-reviewed cycle (the security-reviewer is the binding check on guard changes), not a tired end-of-session edit: **F-0042** verify-gate.sh mis-normalizes absolute Windows paths to a parent-prefixed `dev/<repo>/src/…` (not repo-relative), blocking in-scope writes and forcing a non-scoped Bash bypass that defeats the path-guard on Windows; **F-0043** six test-hooks.sh contract tests read the live features.json and fail whenever a feature is in_progress, so verify.sh fails for the duration of every build. The builder's two workarounds (write via Bash; capture evidence while F-0040 pending) were judged sound — they did not affect F-0040's code correctness or the genuineness of the verify PASS (evaluator confirmed the diff is the 4 authorized files, no new deps, CAPTURE-EXIT 0 + VERIFY: PASS real). F-0040 ships as done because at CI time it is `done` (not in_progress), so the 6 tests pass — the bug only bites during the in_progress build window. Reversible. → src/oneshot/, PR #129, roadmap/evidence/F-0040/, features.json (F-0042/F-0043), this entry.
- **2026-06-18** · **Windows npx-argv mangling gotcha (update-state.ts --add).** Passing a JSON blob as `update-state.ts --add "<json>"` on Windows routes through the npx→cmd/PowerShell wrapper, which re-parses shell metacharacters in the text: a `;` inside `()` broke F-0043's add ("Missing closing ')'"), and other `;`/`(`/`)` combos silently no-op'd. Workaround used: sanitize feature text to plain prose (no parens/semicolons) — structural JSON chars `{}[]":,` are unaffected. Proper fix is a kaizen candidate: add an `--add-file <path>` mode to update-state.ts so the feature JSON is read from disk and never appears on a command line. → DECISIONS, kaizen backlog (informal).
- **2026-06-18** · **F-0041 shipped (evidence-gated verdict) — one-shot tool MVP complete.** Second/final MVP feature (`src/oneshot/verdict.js`): PASS only on a captured exit-0 on disk, the agent's self-claim ignored; NOT-DONE reports claim-vs-evidence. Fresh-context evaluator PASS — the no-false-PASS invariant was adversarially probed (PASS iff persisted exit 0 across 0/1/2/42/127/255; evidence write fail-closed) and held; 5/5 non-vacuous tests, no new deps, scope clean, consumes F-0040's descriptor shape (admit→verdict round-trip tested). Security note (evaluator): verdict.js runs the operator-supplied acceptanceCommand via child_process — same trust level as the operator typing it; no overreach, injection out of scope for this single-operator MVP → Tier B evaluator-only, no security-reviewer trigger. Interim engine-bug workaround (clean): F-0041 stayed `pending` during the build (builder scoped via CLAUDE_ACTIVE_FEATURE env), so verify.sh ran green without the F-0043 in_progress-test failures — better than F-0040's revert dance, and the pattern to use until F-0043 lands. Merged PR #131. Reversible. → src/oneshot/verdict.js, roadmap/evidence/F-0041/, this entry.
- **2026-06-18** · **F-0042 shipped (verify-gate.sh Windows abs-path fix, Tier C).** verify-gate.sh was mis-normalizing absolute Windows paths to a parent-prefixed `dev/<repo>/src/...` instead of repo-relative `src/...`, wrongly blocking in-scope edits and forcing a non-scoped Bash bypass that defeated path-guard on Windows. Fixed by canonicalizing via `path.relative` mirroring path-guard.js; path-guard.js root aligned to `CLAUDE_PROJECT_DIR||cwd`. Evaluator PASS; security-reviewer APPROVE (no fail-open). Operator pre-authorized merge in /goal. PR #133.
- **2026-06-18** · **F-0043 shipped (hook-contract STATE_FILE isolation + path-guard root align, Tier C).** Six test-hooks.sh permissive-mode tests read the live features.json and failed whenever a feature was in_progress, breaking verify.sh during every build. Fixed by pinning a zero-in_progress STATE_FILE fixture. Folded from F-0042 security-review LOW finding. Evaluator PASS; security-reviewer APPROVE. No coverage hole introduced. Hook-contract tests: 367. PR #134.
- **2026-07-01** · **"Killer Foundation" deep-research pass (operator /goal → /deep-research) → `roadmap/KILLER_FOUNDATION_ROADMAP.md`.** Ran a 56-agent workflow (6 repo-audit + 7 web-research + adversarial hype-vs-proven verification + synthesis; ~2.4M tokens) to find the highest-leverage, lowest-ceremony changes to make this repo a lean AI-built-for-AI foundation. **Key judgment (decide-and-document):** the machine audit ran on a fast model and OVERSTATED several "free prunes" — a hand ground-truth pass caught them before writing the roadmap: `.agents/` (36K), `tmp/` (284M), and `tmp-feedback-2.md` are **already untracked + gitignored** (zero repo impact, not tracked bloat); `model-policy.json` **already** uses the `effort` dial (no thinking-budget knobs to delete); and the audit's "rewrite AGENTS.md as `@CLAUDE.md`" is a Claude-only syntax that would **regress** the deliberate cross-tool thin-pointer shape (DECISIONS 2026-06-13). Net finding: the foundation is already lean and on the frontier pattern; genuine backlog is a handful of Tier-C guard de-duplications/hardening (unify the duplicated glob logic in verify-gate.sh + path-guard.js; delete the dead bash fallback; harden the gate vs untracked-test-file survival) + surgical doc de-dup + test-hooks modularization. Verification honesty: a batch of refutation votes hit server-side rate-limiting, so some external claims "survived" un-refuted — all of them endorse the status quo (single-writer, fresh-context judge, strict assertion-shield), so no risky action rides on them; the one change they motivate (gate hardening) is Tier-C and gets a mandatory security review at build time. Roadmap items are NOT yet groomed into features.json — they enter via the operator's ROADMAP.md prioritization + /groom, respecting operator ownership of that file. Reversible (delete the doc). → roadmap/KILLER_FOUNDATION_ROADMAP.md, this entry.
- **2026-07-01** · **F-0045 shipped (OPERATOR_GUIDE trim, Tier A) + F-0044 DEFERRED (blocked).** F-0045 rewrote OPERATOR_GUIDE.md 79→40 lines as an operator cheat sheet pointing to PLAN §8, dropping only a maintainer-only Windows-CLI troubleshooting section (jargon, not an operator action). Fresh-context evaluator PASS (4/4). PR #143. **F-0044 (doctrine de-dup) deferred by engineering judgment (less-is-more):** ground-truth showed CLAUDE.md §3 is already a terse skeleton pointing to the /work skill; the only overlap is with PLAN §5.1 at a different altitude (blueprint detail vs always-loaded skeleton). Trimming CLAUDE.md loses the skeleton; trimming PLAN saves no per-session tokens (not always loaded). A constitution edit for ~nil token gain fails the highest-leverage-only bar. Reversible (`--status F-0044 pending`); revisit only if CLAUDE.md nears the 150-line cap. → roadmap/features.json (F-0044 blocked), OPERATOR_GUIDE.md, roadmap/evidence/F-0045/.
- **2026-07-01** · **Build-conditions kaizen (PR #144): capture.sh mandatory, worktree-isolated builds banned.** Three agent-runs were wasted while building the foundation work on two avoidable traps, now fixed at the source (manager's-conditions doctrine): (1) capturing evidence by `tee`/redirect into `roadmap/evidence/` modifies the file mid-run and trips the **F-DM1 hermeticity** test → a false red that one builder misread as a "broken develop baseline" (with invented SHAs) — both builder cards now mandate `bash scripts/capture.sh <id> verify -- bash scripts/verify.sh`, forbid tee/redirect, and state the develop baseline is green (confirm before ever claiming otherwise); (2) git-worktree isolation gives builders no `node_modules`, so verify.sh can't run there — the /work BUILD step now forbids worktree-isolated builds. `.claude/agents/*` + `.claude/skills/**` are behavior guidance, not enforcement surfaces (DECISIONS 2026-06-09), so evaluator-class, no security-reviewer; verify.sh green. Also logged to auto-memory. → .claude/agents/builder.md, .claude/agents/builder-strong.md, .claude/skills/work/SKILL.md.
- **2026-07-02** · **Build-vs-adopt research verdict (operator /deep-research) — KEEP the differentiated core; prune toward native via gated cycles; do NOT adopt wholesale.** 105-agent deep-research + 2 local repo audits. CONFIRMED (3-vote adversarial): Anthropic's long-running-agent guidance recommends this repo's exact state-control pattern (JSON feature list, passes-flip only, strongly-worded no-test-editing) — the repo is the mechanically-enforced version; Claude Code 2.1.154–2.1.183 natively absorbed loop orchestration (dynamic workflows), generic diff review (/code-review), destructive-git blocking, and credential-read sandboxing; Anthropic doctrine: every harness component encodes a model-limitation assumption that goes stale (their 4.5→4.6 observation: the evaluator boundary moved outward). CCPM = closest OSS overlap but lacks the evidence gate / mutation-smoke / assertion-shield / tier machinery entirely. REFUTED in verification: the "$200-harness-vs-$9-solo 20x experiment" claim (1-2, mischaracterized — never cite) and the "official two-part initializer-harness architecture" claim (0-3). UNVERIFIED (rate-limited, re-verify before relying): Codex-cloud subscription bundling/pricing; the self-orchestration-beats-LangGraph paper (explicitly scoped to conversations, not coding harnesses, regardless). RULING: no existing OSS/commercial system replaces the differentiated core (evidence-gated state machine, mutation-smoke, assertion-shield, tier triage + operator gates, plain-English operator layer); the depreciating layers (loop plumbing, generic review, destructive-cmd rules that native auto-mode now blocks) should be pruned toward native — each through its own gated Tier-C cycle, never casually, since our hooks also protect CI + non-Claude harnesses where native settings don't apply. The unmeasured engine-effect delta is now the repo's central open claim → bench refactor-task headroom slice is the priority research item. Full cited report delivered in-chat (12 confirmed claims with sources). → this entry.
- **2026-07-02** · **Goal-session judgment calls (operator /goal: "verify what works, good tests, less is more, prune dead weight").** ① F-0046 (pending) deliberately NOT selected — the /goal directed verification+tests+pruning; F-0046's fresh-session deferral rationale (2026-07-01) stands. ② Dead agent-cleaner report deleted rather than archived — 0 references hand-verified (Grep, whole repo); git history is the archive; an archive/ dir would add structure, not remove it. ③ CRLF/unicode token-count characterization tests dropped from F-0047 scope — the exact budget-boundary test pins the token math; encoding characterization is padding (less is more). ④ npm-audit local-soft parity in verify.sh (test-gap sweep finding: CI-only today) NOT shipped this cycle — a network-dependent step in the deterministic local gate needs the actionlint-style local-soft/CI-hard pattern designed deliberately, not appended in a records session; queued as kaizen candidate; CI already enforces it hard so there is no unguarded surface. ⑤ F-0047's quoted-ampersand false positive (an `&` inside a quoted argument is now rejected) accepted by design — consistent with the gate's documented quoted-pipe treatment; no existing caller affected (evaluator-verified). → this entry.
- **2026-07-02** · **CLAUDE_ACTIVE_FEATURE-vs-verify leak (builder finding, A/B-proven) → kaizen PR #152.** Exporting/prefixing CLAUDE_ACTIVE_FEATURE around `scripts/verify.sh` (as F-0047's brief DoD literally instructed — an orchestrator briefing defect) leaks into test-hooks.sh's unset-env permissive-fallback fixtures and deterministically flips ~20 hook contracts to a false red (fail-closed on an id unknown to the F-9xxx fixtures). The builder isolated it with a controlled A/B (stash diff out, re-run suite) instead of misreading it as a broken baseline — exactly the behavior the 2026-07-01 build-conditions kaizen trained. Rule shipped: scope the var to EDIT commands only; verify runs env-clean. → .claude/agents/builder.md, .claude/agents/builder-strong.md, .claude/skills/work/SKILL.md, PR #152, this entry.

- **2026-07-10** · **Operator-authorized branch consolidation → `main` becomes the default and only long-lived branch.** Fresh fetch + GitHub API inventory found seven live branches. `develop` was the real canonical line (`028da48e`): 423 commits ahead of `main`; `main`'s sole unique promotion commit had the exact same tree as the merge base, so it carried no unique content. Decision: merge the reviewed candidate into `main` through a normal green PR (never reset/force), change every active base/CI/ship/installer reference to `main`, use tags + GitHub Releases for accepted milestones, then delete `develop` and every short-lived branch. Direct/force pushes to `main` remain blocked; future work uses short-lived PR branches deleted after merge. Reversible from retained git objects/commit SHAs, but intentionally changes GitHub defaults and protection. → active agent cards/skills/hooks, workflows, scripts, docs, repo settings.
- **2026-07-10** · **Unmerged branch rulings after full diff/CI review.** Drop `feat/refactor-benchmark` whole: it had no PR/check/evidence, its wrapped runner diffed nonexistent `HEAD~1` so both safety checks silently passed, its FIFO implementation passed the claimed LRU oracle, rejection could evict/delete valid entries, and its README/VISION claims had no evidence and were already stale. Drop TypeScript 7.0.2 (#156): its native compiler intentionally ships without the programmatic API until a later release, so `ts-node` crashed; CI produced 42 hook failures plus surviving safety mutations. Integrate the green compatible updates from #155/#157 together (`@types/node` 26.1.1, Biome 2.5.3), and supersede #154's v1.0.168 pin with current v1.0.170 commit `536f2c32a39763739000b0e1ac69ca2647d97ce9`. Verified 2026-07-10 against primary sources: https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/ · https://github.com/biomejs/biome/releases/tag/%40biomejs%2Fbiome%402.5.3 · https://www.npmjs.com/package/@types/node/v/26.1.1 · https://github.com/anthropics/claude-code-action/releases/tag/v1.0.170. → package manifests, claude workflow, GitHub PR/branch cleanup.
- **2026-07-10** · **Critical dev-tool extraction advisory eliminated rather than dismissed.** Promotion surfaced Dependabot alert GHSA-mp2f-45pm-3cg9 (CVSS 9.1): `shellcheck@4.1.0` used unmaintained `decompress@4.2.1` to extract a downloaded archive, so “dev-only” did not mean unreachable. A patched npm override was proved as immediate containment, then rejected as the durable shape: the wrapper's source repository now returns 404, it downloads without a checksum, and its default binary release moves independently of the package lock. `shellcheck@1.1.0` was also rejected (install-time `download | tar`, mutable release, no checksum), and no official/npm ShellCheck WASM distribution exists. Removed the npm wrapper entirely. `scripts/shellcheck.sh` now downloads official ShellCheck v0.11.0 for Windows Git Bash or Linux x86_64, pins and verifies both the release archive and extracted binary SHA-256, validates the reported version, and caches it under `.cache/`; CI and init pre-provision through the same launcher. The adopter installer removes only the exact engine-owned wrapper specs (`4.1.0`/`^4.1.0`) and preserves custom project declarations with a warning. CI's audit now includes dev dependencies so this class cannot hide behind `--omit=dev`. Sources verified 2026-07-10: https://github.com/advisories/GHSA-mp2f-45pm-3cg9 · https://github.com/koalaman/shellcheck/releases/tag/v0.11.0 · https://www.npmjs.com/package/shellcheck. Reversible by changing the pinned version plus reviewed checksums. → scripts/shellcheck.sh, package manifests, verify.sh, init.sh, install-into.sh, ci.yml.
- **2026-07-10** · **Fresh consolidation reviews changed the candidate before merge.** The first evaluator/security passes caught four real gaps: locked adopters would retain a stale vulnerable lockfile; privileged workflows still used mutable action tags; implicit/wrapped `git push` forms could inherit `main`; and a PR could be retargeted while checks ran. Fixes: reconcile only the retired engine-owned dependency through npm's lock writer with lifecycle scripts disabled; pin checkout/setup-node/Claude action to reviewed commit SHAs; replace the growing push parser with a strict fail-closed allowlist for one canonical literal short-lived-branch/tag command; and re-read the PR base immediately before merge. Review then adversarially expanded through option arguments, quotes, escapes, newlines, redirections, comments, executable paths, substitutions, and IFS construction until the security reviewer returned APPROVE. Final executable evidence: 31 product tests, 464/464 hook contracts, zero dependency vulnerabilities, actionlint/ShellCheck clean, and every mutation killed. → workflows, guard-bash.sh, ship.sh, install-into.sh, test-hooks.sh, STATUS.md.
- **2026-07-16** · **Frontier research refresh (operator /goal + /deep-research, 148 Sonnet agents / 12 lanes / 332 claims / 44 adversarially verified): model facts were stale because a model shipped after our last stamp.** `sonnet` no longer means Sonnet 4.6 — **Claude Sonnet 5 (`claude-sonnet-5`) released 2026-06-30**, three weeks after `model-policy.json`'s `last_verified: 2026-06-09`, and the Anthropic-API family alias moved with it. The engine has therefore been running Sonnet 5 builders while both `model-policy.json` and PLAN §2.2 documented Sonnet 4.6 — the *mechanism* was fine (aliases auto-track; `check-model-policy --check` was green throughout), only the *documentation* was rotten. This is the §5 freshness rule catching itself: the repo's own `--validate` had been emitting stale-stamp warnings on all three tiers and nobody had run /research. Corrections, verified against primary sources 2026-07-16: builder tier = Sonnet 5, **two-phase pricing $2/$10 per MTok introductory through 2026-08-31 (40% of Opus 4.8), then $3/$15 from 2026-09-01 (60%)** — so PLAN's flat "exactly 60% of Opus ($3/$15 vs $5/$25)" was wrong on both the model and today's price; reasoning tier = Opus 4.8 ($5/$25) with Fable 5 ($10/$50, 2× Opus) still the valid hard-pass upgrade; fast tier = Haiku 4.5 ($1/$5). **Tier→model mappings deliberately NOT changed** — the bare aliases resolve correctly on this backend, and pinning exact IDs (4.6+-generation IDs are fixed snapshots, not evergreen pointers) is a real trade-off (predictability vs. auto-tracking) that deserves its own groomed feature, not a side-effect of a docs refresh. **New `_provider` field in `model-policy.json`:** aliases resolve PER PROVIDER (`sonnet` = Sonnet 5 on Anthropic API, Sonnet 4.6 on Claude Platform on AWS, Sonnet 4.5 on Bedrock/GCP; `opus` = Opus 4.6 on Microsoft Foundry) and the repo had never recorded which backend it targets — that single unrecorded fact silently determined what every tier meant. Also removed: PLAN §2.2's `reasoning` row still listed **db-engineer**, an agent cut in the 2026-06-17 lean pass (absent from `.claude/agents/` and the policy's agents map). Carried forward as explicitly UNVERIFIED rather than asserted (outside the fetched scope): the "Fable 5 included on subscription through Jun 22, 2026, then usage credits" terms. Sources: https://platform.claude.com/docs/en/about-claude/models/overview · https://platform.claude.com/docs/en/about-claude/pricing · https://code.claude.com/docs/en/model-config · https://www.anthropic.com/news/claude-sonnet-5. Reversible. → .claude/model-policy.json, AI_OPERATIONS_PLAN.md §2.2, this entry.
- **2026-07-16** · **Dependabot wave resolved (3/3 merged green); provenance comments fixed ON the bump PRs rather than after.** #162 Biome 2.5.3→2.5.4 (lockfile verified clean: zero new transitive packages, no install scripts, all resolved URLs on registry.npmjs.org), #160 actions/setup-node v6.4.0→**v7.0.0** (benign major — the bump is an internal ESM migration; its only behavioral change, removing a dummy `NODE_AUTH_TOKEN` export, is a no-op here since the repo never sets `registry-url` and bans publishing), #161 claude-code-action v1.0.170→**v1.0.174** (security-*positive*: adds `ghu_` token redaction and makes a success check fail-closed; bundles Claude Code 2.1.210). All four action SHAs independently re-resolved to their claimed tags before merge (both claude-code-action tags are *annotated*, so the ref SHA is the tag object — it must be dereferenced to the commit; a naive comparison would have falsely flagged both as mismatched). **Judgment call:** Dependabot rolls the pinned SHA but cannot update the provenance comment on the *preceding* line, so each merge would have left a comment certifying "tag + commit verified 2026-07-10" above a SHA that did not exist on that date. Ruled NOT cosmetic and fixed pre-merge on each branch: in a SHA-pinning scheme the comment is the human-readable half of the control, a false attestation suppresses the re-verification it exists to prompt, and a reviewer trusting it over the SHA would reconcile *backwards* — rolling claude.yml back to v1.0.170 and silently undoing v1.0.174's redaction fix. Security review: APPROVE ×3, no high/critical. Reversible. → .github/workflows/ci.yml, .github/workflows/claude.yml, package.json, PRs #160/#161/#162.
- **2026-07-16** · **Audit finding, operator action required — `main`'s human-approval guardrail is OFF and the log says it was restored.** Branch protection on `main` currently reads `required_approving_review_count: 0`; the 2026-06-10 entry records `main = PR + green verify + 1 human approval` and states the rule "was lifted for this one merge and RESTORED immediately after." It is not on now (PR-only and the required `verify` check ARE enforced, and `enforce_admins`/`strict` are both true). Not changed by this session: modifying repo access controls is reserved to the operator. The gap means CLAUDE.md §6's "Tier-C / REQUIRE_APPROVAL surface holds for operator sign-off before the irreversible merge" currently rests on agent compliance alone, with no mechanical backstop. → repo settings (operator), STATUS.md, this entry.
- **2026-07-16** · **Evaluator NEEDS_WORK on the research-refresh PR (#163) — and it caught the refresh committing the exact sin the refresh was fixing.** Fresh-context evaluator verified every number as correct (all four ratios on input AND output, mappings unchanged, PROGRESS→archive dropped 0 of 83 lines, CI green on head) and explicitly ratified the "was the old note wrong?" framing as fair rather than overstated — Sonnet 4.6 at $3/$15 genuinely IS 60% of Opus 4.8, but the *tier row* was wrong on both the model and today's price, and the note's own "only becomes true **again** on Sept 1" concedes the ratio was once true. Two blockers, both fixed: ① **the PLAN blockquote asserted `opus`/`sonnet`/`haiku` resolve per-provider and stamped it "verified 2026-07-16", but the cited page tabulates `opus` and `sonnet` ONLY** — a claim attributed to a source that does not make it, carrying a verification stamp, in the very PR whose thesis is that unchecked claims rot. Re-verified 3-0 by an independent adversarial pass: the table's exact columns are `Provider | opus | sonnet`, introduced by "The version that the `opus` and `sonnet` aliases resolve to depends on the provider:". Corrected to tabulated-for-opus/sonnet-only, with haiku recorded as provider-dependent *in principle* (the page does say aliases "such as fable, opus, sonnet, and haiku" resolve to a per-provider default) and *unverified in specifics*. ② **F-0048's `authorized_paths` listed `.github/workflows/*.yml` while `forbidden_paths` had `.github/workflows/**`; forbidden wins in BOTH guards**, so the briefed builder would have been hard-blocked on paths its own brief authorized. Root cause: path-guard's matcher is `Edit|Write|NotebookEdit` — it gates WRITES only, so the pin-checker needs no authorization to READ workflows and must never edit them. Re-groomed via `--remove` + `--add` (not `--paths`: `GUARD_SURFACES = /^(\.claude|\.github|scripts)(\/|$)/` rejects any scripts/ path, which is the documented "re-groomed, not rescoped" rule working as designed). The same re-groom resolved the evaluator's hermetic-vs-network tension by specifying `scripts/action-pins.json` as a checked-in tag→commit manifest, keeping the gate offline with live re-resolution behind an opt-in `--online` flag (adds verification, cannot open a bypass → no DANGEROUSLY_ prefix, same rationale as SHIP_REGISTER_TIMEOUT). → AI_OPERATIONS_PLAN.md §2.2, .claude/model-policy.json, roadmap/features.json, PR #163.
- **2026-07-16** · **New material fact: per-token prices only compare WITHIN a tokenizer generation.** Surfaced by the evaluator as a nuance, then verified verbatim against two primary pages (pricing + token-counting, corroborated by the Opus 4.8 migration guide): Opus 4.7-and-later (so Opus 4.8), Fable 5, Mythos 5 and **Sonnet 5** use a newer tokenizer producing **~30% more tokens for the same text**; **Sonnet 4.6 and earlier — and Haiku 4.5 — use the previous one**. Consequences recorded in `model-policy.json._tokenizer` and PLAN §2.2: the builder-vs-reasoning ratios (40%/60%) ARE apples-to-apples because Sonnet 5 and Opus 4.8 share the new tokenizer, but **the `fast` tier's $1/$5 is NOT per-token comparable to the other two** — Haiku 4.5 bills fewer tokens for the same text, so naive per-token tier arithmetic understates its advantage. Magnitude carried in-file as **~30% typical, ~35% ceiling**: pricing/token-counting state "approximately 30%" (a central estimate with no ceiling given) and the Opus 4.8 migration guide gives the range "roughly 1x to 1.35x ... (up to ~35% more, varying by content)" — a ~30% typical value sits *inside* that range, so the pages are **consistent, not contradictory**. (An earlier draft of this entry said they "disagree" — an overstatement, caught on re-judge, and precisely the error this entry exists to prevent. ~30% remains a planning estimate, not a constant.) **Process note worth keeping:** the adversarial harness scored this claim "REFUTED 2/3" while all three refuters had independently confirmed the quotes verbatim — they were objecting to an over-certain sub-claim, not to the facts, and the "default to refuted if not positively confirmed" instruction collapsed *incomplete* into *false*. A refute-vote count is not a truth oracle; read the reasoning before acting on the tally. → .claude/model-policy.json, AI_OPERATIONS_PLAN.md §2.2, this entry.
- **2026-07-16** · **Frontier ledger created (operator /goal): `docs/FRONTIER.md` — the session-persistent verified-facts store; freshness rule §5 now reads ledger-first.** The operator's problem: every session re-buys the same research (~10M tokens on the last two passes). The mechanism: /research now writes durable verified findings (model catalog, cutoffs, pricing, platform capabilities) into FRONTIER.md with `last_verified` + source; CLAUDE.md §5 gains one clause — a fact stamped <3 months may be relied on without re-research. The file also carries the "why we built it this way" context for LLMs whose training predates 2026 (harness-dominates ~9.5pt; agents over-report success by up to 55pt; reward-hacking 49–54%; first-party convergence on this state pattern) — build context, not runtime instruction, per the operator's framing. **Cross-vendor catalog verified live before cataloging (66-agent Sonnet pass, per-lane sampled — the lane-order truncation flaw from the morning pass is fixed): the operator's named frontier list checks out** — Gemini 3.5 Flash GA 2026-05-19 (cutoff Jan 2025), Grok 4.5 GA ~2026-07-08 (`grok-4.5`, cutoff stated Feb 1 2026, 500k ctx, not in EU), GPT-5.6 GA 2026-07-09 (Sol/Terra/Luna are generation-5.6 capability TIERS, not one model; `gpt-5.6-sol`, cutoff Feb 16 2026, 1.05M ctx). Anthropic cutoffs: Fable 5 / Opus 4.8 / Sonnet 5 all Jan 2026 (reliable = training); Haiku 4.5 Feb 2025 / Jul 2025. Gemini 3.5 **Pro is NOT GA** (only "next month" said May 19) — rumored dates/specs carried UNVERIFIED. **Model roles recorded** (FRONTIER §3): Fable 5 = planner/auditor at session level when available (NOT flipped as the `reasoning` tier default — 2× Opus cost with UNVERIFIED subscription terms is a cost decision reserved to the operator, §8 safer-shape rule); Opus 4.8 = implementer; Sonnet 5 = researcher; Gemini/GPT-5.6 = external second-opinion lanes, not integrated (plan §2.1 stands). Switching surface verified: /model, `claude --model`, ANTHROPIC_MODEL, settings `model` key; parallel terminals for parallel models. Also fixed: README said 6 months where CLAUDE.md §5 says 3 (3 is the rule); /research skill now weighs working public production repos over announcements/benchmarks (operator: production repos separate working practice from hype). CLAUDE.md edit walked the §9 checklist (53 lines post-edit, enforceable wording, logged here). Reversible. → docs/FRONTIER.md, CLAUDE.md §1+§5, .claude/skills/research/SKILL.md, README.md.
- **2026-07-17** · **Operator /goal: README must match repo reality; principles reworked 6 → 7.** The README set now leads with "Trust nothing without evidence" — plan P5, the engine's most-enforced rule, was absent from the README's public principles until now. Risk-tiered autonomy (previously body text) and "every line must earn its keep" (the operator's boil-it-down directive, grounded in the 2026 durable-core consensus: verification/state/guardrails endure, capability-compensating scaffolding rots) are promoted to principles; the two orchestrator-conduct principles merge into one; operator-as-business-client drops from the README per operator direction (it remains P3 in the plan — the plain-English operator surface is unchanged). Deliberately NOT renumbering plan P1–P6 across docs: the README is the public statement, plan §1 stays the enforcement mapping, and a cross-doc renumbering is blast radius without benefit (the plan's "These five rules" miscount for its six rows IS fixed). Grounded in a four-stream /research pass (instruction-file conventions; guardrail/evidence norms; template-repo landscape; simplicity-vs-scaffolding), durable findings written to FRONTIER §4 (4 rows). Reversible (docs). → README.md, AI_OPERATIONS_PLAN.md §1, docs/FRONTIER.md §4.
- **2026-07-17** · **Footprint honesty over flattery in the README truth-up.** Replaced the stale "~3,900 lines of scripts/hooks" with the measured 4,575 (scripts + hooks) and 5,987/52-file full-install figure, unified the internal 350+/464 test-count contradiction to the re-verified 464 (`hook contract tests: 464 passed, 0 failed`, 2026-07-17), and added an honest-limitations bullet naming the engine's own footprint growth as the number to watch for whether principle 6 is being honored. Chose stating the unflattering trend over silently updating the number. Reversible. → README.md.
