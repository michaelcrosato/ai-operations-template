# Ledger drift audit - 2026-07-18

Scope: all 27 done rows in roadmap/features.json at commit c7caa01, audited for F-0027-class drift (row records a design its own build pivoted away from). Method: one reader agent per row (checks row text vs DECISIONS/PROGRESS pivot history vs shipped artifacts); any DRIFTED verdict then faces 2 adversarial confirmation votes (majority required). Workflow: 29 agents, 0 errors.

## Tally: 27 MATCHES / 0 DRIFTED / 0 UNVERIFIABLE

| Feature | Verdict | Notes |
|---|---|---|
| F-0001 | MATCHES | Row describes building the full control plane (state files, agents, skills, hooks, gate scripts, update-state writer, CI). PROGRESS-archive (2026-06-09) confirms exactly this shipped: 5 sub-agents, 6 skills, 3 rules, 4 h |
| F-0002 | MATCHES | Row describes a trivial CLI/health-check feature to prove the loop end-to-end; shipped artifact is exactly that (src/health.js — zero-dep CLI printing a health JSON object, src/health.test.js). DECISIONS.md/PROGRESS-arch |
| F-0003 | MATCHES | Row claims fixture-based contract tests for guard-bash.sh, verify-gate.sh, commit-on-stop.sh, session-brief.sh, update-state.ts and assertion-shield, run in verify.sh and CI. roadmap/evidence/F-0003/contract-tests.log sh |
| F-0004 | MATCHES | Row is a human-only tracking entry (forbidden_paths: **) for off-repo operator setup: branch protection, claude-code-action secret, cloud environment. DECISIONS.md 2026-06-13 shows only a wording clarification (blocked_r |
| F-0005 | MATCHES | Row's acceptance criteria are verified against the feature's own commit (875ec3e, PR #2): six-principle Philosophy section landed in README, P6 + §5.5 Downtime protocol landed in AI_OPERATIONS_PLAN.md, /downtime skill cr |
| F-0006 | MATCHES | Row describes fixing 9 review defects incl. assertion-shield staged-diff/deleted-file detection and guard-bash refspec/flag bypasses. PROGRESS-archive.md (2026-06-10) confirms: security reviewer found the refspec (`+main |
| F-0007 | MATCHES | Row describes a PreToolUse hook enforcing authorized_paths/forbidden_paths with open design questions (active-feature resolution, glob engine, fail-open/closed). DECISIONS (2026-06-12) and PROGRESS-archive record it comp |
| F-0008 | MATCHES | Row claims shellcheck + actionlint + Biome as engine meta-gates in verify.sh. Confirmed in scripts/verify.sh lines 111-120 (biome lint, shellcheck.sh hard, actionlint CI-hard/local-soft) and biome.json + package.json dev |
| F-0009 | MATCHES | Row describes four tripwires: guard-bash exfil-shaped POST blocking, CI trust-widening/.mcp.json tripwire, assertion-shield .skip/.only/xit/xdescribe detection, and update-state --validate model-policy staleness warning. |
| F-0010 | MATCHES | Row describes metrics.jsonl append-at-RECORD, consumed by kaizen/status, with schema validation in verify.sh — all present. DECISIONS.md (line 30) records F-0010's one review cycle: evaluator NEEDS_WORK on mechanical fin |
| F-0011 | MATCHES | Row's description explicitly bakes in the AGENTS.md correction ("Claude Code keeps CLAUDE.md per official docs") — this was decided at grooming/verification time (DECISIONS.md 2026-06-10), not pivoted mid-build. Shipped  |
| F-0012 | MATCHES | Row describes a four-branch delegating shim (prod-refusal guard, delegate to package.json seed script propagating exit status, hard-fail in product mode with no seed script, template-mode stub) plus circular-delegation h |
| F-0013 | MATCHES | Row describes scripts/install-into.sh doing an adopter-clean copy (excluding src/, template package.json, roadmap state), a package.json transform (strip test script, lint scripts-only, placeholder/merged name), fresh ro |
| F-0014 | MATCHES | Row's title/description/acceptance (actionlint parity, fetch-depth 0, Node 24, paths filter, lane-parity contract tests) match the shipped diff in commit f6a1127 (PR #28) line-for-line: fetch-depth 0, node-version 24, id |
| F-0015 | MATCHES | Row describes ship.sh: bounded register-wait + fail-closed, gh pr checks watch-to-terminal, merge only on green, never merge master/main base, contract tests on stubbed gh, CLAUDE.md §6 reference. Spot-check confirms scr |
| F-0016 | MATCHES | Row title/description/acceptance describe three field-reported fixes: (a) template-neutral AI_OPERATIONS_PLAN doctrine wording, (b) install-into.sh seeding preamble-then-skeleton to avoid duplicate "## Now", (c) assertio |
| F-0022 | MATCHES | Row's acceptance names illustrative mechanisms ("settings.json env, a wrapper, or the spawn step") for arming CLAUDE_ACTIVE_FEATURE mechanically. What shipped (PR #46) instead derives the active feature from the single i |
| F-0025 | MATCHES | Row's title/description/acceptance describe: single-in_progress invariant in update-state.ts (validate + status-guard rejecting 2nd concurrent in_progress) and fail-closed hooks on multiple-in_progress. DECISIONS.md/PROG |
| F-0026 | MATCHES | SPLIT confirmation vote 1-1, downgraded to MATCHES: auditor read the judge-fixture lane as never-shipped; refuter cites the row's own "contract suite or a documented advisory lane" wording + the at-ship DECISIONS 2026-06-14 record of shipping the gate-able half |
| F-0027 | MATCHES | amended this same day via the F-0051 --amend verb; audit confirms the corrected row now matches |
| F-0030 | MATCHES | Row title/description/acceptance describe a generic goal (broaden deterministic guard patterns for cross-shell/indirection, document deterrent-not-boundary) with no specific rejected mechanism named. DECISIONS.md:68 reco |
| F-0040 | MATCHES | Row describes an admission gate (ADMIT/REJECT, single-command acceptance check, configurable token-budget heuristic, no new deps) that matches src/oneshot/admit.js exactly: CHAINING_RE rejects multi-command acceptance (" |
| F-0041 | MATCHES | Row (title/description/acceptance) matches shipped src/oneshot/verdict.js: runs descriptor.acceptanceCommand via spawnSync, captures real exit code to an evidence JSON file unconditionally, returns PASS only on exitCode= |
| F-0042 | MATCHES | Row claims verify-gate.sh's Windows abs-path bug (parent-prefixed instead of repo-relative) was fixed by canonicalizing via path.relative, mirroring path-guard.js. Verified in code: both .claude/hooks/verify-gate.sh (lin |
| F-0043 | MATCHES | Row describes: isolate 6 permissive-mode tests in test-hooks.sh from live features.json via a STATE_FILE fixture, fixing verify.sh failures while a feature is in_progress. PROGRESS.md and DECISIONS.md (2026-06-18) confir |
| F-0045 | MATCHES | Row describes trimming OPERATOR_GUIDE.md to a cheat sheet pointing to AI_OPERATIONS_PLAN.md §8, preserving all operator actions. DECISIONS.md (2026-07-01) and PROGRESS.md confirm exactly this: 79→40 lines, evaluator PASS |
| F-0047 | MATCHES | Row describes tightening CHAINING_RE to reject a single ampersand plus 5 deterministic verdict.js failure-path tests. Code confirms CHAINING_RE = /[\n;&/]/ at admit.js:58, matching the description exactly. PROGRESS.md (2 |

## Full per-row rationales (verbatim from the audit agents)

### F-0001 - MATCHES

Row describes building the full control plane (state files, agents, skills, hooks, gate scripts, update-state writer, CI). PROGRESS-archive (2026-06-09) confirms exactly this shipped: 5 sub-agents, 6 skills, 3 rules, 4 hooks, init/verify/update-state/seed scripts, CI workflows, evidence at roadmap/evidence/F-0001/verify.log. Pre-merge review findings (gitignore swallowing evidence, missing exec bits, permission-rule format, Stop-hook registration) were bugs fixed within the same build cycle, not a rejected design/mechanism — no "instead"/"corrected design" language tied to F-0001 in DECISIONS.md. The one DECISIONS.md mention of F-0001 (2026-06-11) concerns test-fixture ID collision contaminating features.json, unrelated to F-0001's own acceptance claims. No evidence of a pivot away from the described mechanism.

### F-0002 - MATCHES

Row describes a trivial CLI/health-check feature to prove the loop end-to-end; shipped artifact is exactly that (src/health.js — zero-dep CLI printing a health JSON object, src/health.test.js). DECISIONS.md/PROGRESS-archive show two build-time issues (Windows/Node test-glob portability fix, a --paths widening for package.json) both resolved within the same single attempt — not a rejected design left uncorrected. No evaluator NEEDS_WORK or security BLOCK recorded. PR merged to "develop" per archive, matching the acceptance text as written (branch model has since evolved to main, but that's post-hoc repo evolution, not F-0002 drift).

### F-0003 - MATCHES

Row claims fixture-based contract tests for guard-bash.sh, verify-gate.sh, commit-on-stop.sh, session-brief.sh, update-state.ts and assertion-shield, run in verify.sh and CI. roadmap/evidence/F-0003/contract-tests.log shows 45/45 tests passing, one section per named hook, including update-state.ts schema rejection, evidence-file requirement, and dependency validation checks — exactly matching the acceptance bullets. scripts/verify.sh line 122 invokes scripts/test-hooks.sh, and .github/workflows/ci.yml runs scripts/verify.sh, satisfying the CI acceptance clause. PROGRESS-archive.md (2026-06-09, F-0001 entry) references F-0003's "45/45 hook contract tests" and cites the same evidence log with no mention of a pivot, rejected mechanism, or evaluator/security-forced redesign specific to F-0003 — only the review findings for the surrounding F-0001 merge, all unrelated to test mechanism.

### F-0004 - MATCHES

Row is a human-only tracking entry (forbidden_paths: **) for off-repo operator setup: branch protection, claude-code-action secret, cloud environment. DECISIONS.md 2026-06-13 shows only a wording clarification (blocked_reason renamed to cite CLAUDE_CODE_OAUTH_TOKEN) before completion — not a mechanism pivot or rejected design. PROGRESS-archive (line 605-609) confirms completion matches description: "Resolved one-time setup question Q-0001... Marked the feature as done." No NEEDS_WORK/BLOCK cycles or "instead/rejected/corrected design" language tied to F-0004 anywhere in DECISIONS or PROGRESS. The evidence/verify.log is a generic pipeline-green log (expected, since the actual acceptance items are off-repo human actions the agent cannot capture directly) — not evidence of drift, just of the checklist's inherently human nature.

### F-0005 - MATCHES

Row's acceptance criteria are verified against the feature's own commit (875ec3e, PR #2): six-principle Philosophy section landed in README, P6 + §5.5 Downtime protocol landed in AI_OPERATIONS_PLAN.md, /downtime skill created, CLAUDE.md updated and kept under 150 lines. PROGRESS-archive.md explicitly notes "Surprises: none" and DECISIONS.md records only a routine sensitivity-rule decision (security review skipped, no pivot). The README Philosophy section was later rewritten in a subsequent, unrelated docs commit (c0d81f3) — that's normal post-ship evolution, not a within-feature pivot, so it doesn't constitute drift.

### F-0006 - MATCHES

Row describes fixing 9 review defects incl. assertion-shield staged-diff/deleted-file detection and guard-bash refspec/flag bypasses. PROGRESS-archive.md (2026-06-10) confirms: security reviewer found the refspec (`+main`) and flag-separated (`--work-tree`) push bypasses DURING F-0006's own build (attempts:1, PR #3), it was fixed and re-reviewed APPROVE before merge — same feature/PR, not a post-ship pivot left uncorrected. The row's acceptance criteria already state the corrected final behavior ("blocks refspec pushes... and flag-separated variants"), matching what shipped. Spot-checked scripts/assertion-shield.ts: uses execFileSync and `--cached` diff exactly as claimed. This is normal in-flight review remediation, not F-0027-class drift (no rejected/replaced mechanism left described in the row).

### F-0007 - MATCHES

Row describes a PreToolUse hook enforcing authorized_paths/forbidden_paths with open design questions (active-feature resolution, glob engine, fail-open/closed). DECISIONS (2026-06-12) and PROGRESS-archive record it completed as a shellcheck/bash-portability fix, not a redesign. Later entries (PR #91/#92, 2026-06-15) explicitly reaffirm F-0007 is done and path-guard.js is live, correcting an unrelated stale doc claim elsewhere — no NEEDS_WORK, security BLOCK, or mechanism swap is recorded against F-0007's own build. path-guard.js/.sh exist in .claude/hooks/ as claimed.

### F-0008 - MATCHES

Row claims shellcheck + actionlint + Biome as engine meta-gates in verify.sh. Confirmed in scripts/verify.sh lines 111-120 (biome lint, shellcheck.sh hard, actionlint CI-hard/local-soft) and biome.json + package.json devDependency present. DECISIONS.md entries for F-0008 describe dependency-choice rationale and same-PR bug fixes (tripwire self-trip, wget exfil-guard gap) made during the F-0008/9 review cycle — these are implementation details/fixes, not a pivot away from the row's described mechanism. No NEEDS_WORK/BLOCK cycle changed the design; PROGRESS-archive confirms PR #18 shipped exactly this feature set.

### F-0009 - MATCHES

Row describes four tripwires: guard-bash exfil-shaped POST blocking, CI trust-widening/.mcp.json tripwire, assertion-shield .skip/.only/xit/xdescribe detection, and update-state --validate model-policy staleness warning. All four verified present in shipped code (guard-bash.sh line 133-136, ci.yml/claude.yml reference allowed_non_write_users/.mcp.json, assertion-shield.ts line 137/181 skipPatterns, update-state.ts line 222-235 30-day staleness check). DECISIONS.md 2026-06-10 entry documents the one design choice made during this feature (cron auto-PR rejected in favor of zero-token check in update-state --validate) — this rejected alternative was never what the row claimed; the row already describes the adopted design, so no drift. No NEWS_WORK/BLOCK/pivot language tied to F-0009 found in DECISIONS or PROGRESS.

### F-0010 - MATCHES

Row describes metrics.jsonl append-at-RECORD, consumed by kaizen/status, with schema validation in verify.sh — all present. DECISIONS.md (line 30) records F-0010's one review cycle: evaluator NEEDS_WORK on mechanical findings (missing evidence dirs, an over-claimed seeded record) plus security hardenings (500-char cap per record, a documented *_FILE test-seam exemption). These are fixes/hardenings to the same mechanism, not a pivot to a different design — no "instead"/rejected-design language found. No F-0027-class drift.

### F-0011 - MATCHES

Row's description explicitly bakes in the AGENTS.md correction ("Claude Code keeps CLAUDE.md per official docs") — this was decided at grooming/verification time (DECISIONS.md 2026-06-10), not pivoted mid-build. Shipped artifacts (AGENTS.md stub, PR template, briefs/TEMPLATE.md, PRODUCT_MODE-gated placeholder check in verify.sh, CLAUDE.md at 53 lines) match every acceptance criterion. The one recorded evaluator NEEDS_WORK cycle (DECISIONS.md line 30) concerned mechanical issues — missing evidence dirs, an out-of-scope README edit, an over-claimed metrics record — not a design/mechanism pivot on F-0011's own deliverables. No F-0027-class drift found.

### F-0012 - MATCHES

Row describes a four-branch delegating shim (prod-refusal guard, delegate to package.json seed script propagating exit status, hard-fail in product mode with no seed script, template-mode stub) plus circular-delegation handling. scripts/seed.ts implements exactly these four branches in order, including the SEED_SHIM_ACTIVE recursion sentinel called out in acceptance criterion 5. DECISIONS.md (2026-06-11) and PROGRESS-archive.md confirm this design was the one built and shipped in PR #24, security-reviewed and accepted (sentinel ruled not a DANGEROUSLY_ override) — no evaluator NEEDS_WORK cycle or redesign is recorded for this feature; the only incidents logged (hand-transcribed evidence log, features.json fixture-ID corruption) were process/evidence issues, not mechanism pivots, and were resolved before judging/merge.

### F-0013 - MATCHES

Row describes scripts/install-into.sh doing an adopter-clean copy (excluding src/, template package.json, roadmap state), a package.json transform (strip test script, lint scripts-only, placeholder/merged name), fresh roadmap seeding, idempotent re-run, and a src/ product-mode warning. scripts/install-into.sh (376 lines) implements exactly this: package.json transform block, merge-vs-create name handling, placeholder-name warning, and a loud warning block for existing src/. DECISIONS.md/PROGRESS-archive entries mentioning F-0013 (2026-06-11 post-hoc record, 2026-06-12 rule) concern a stranded CI-watcher causing a missed RECORD step, not a design pivot on the installer's mechanism — no NEEDS_WORK/BLOCK/'corrected design' language tied to F-0013's actual build was found.

### F-0014 - MATCHES

Row's title/description/acceptance (actionlint parity, fetch-depth 0, Node 24, paths filter, lane-parity contract tests) match the shipped diff in commit f6a1127 (PR #28) line-for-line: fetch-depth 0, node-version 24, identical pinned actionlint step, expanded paths filter including e2e.yml/tests/package files. DECISIONS.md and PROGRESS-archive record a clean first-attempt PASS with zero evaluator/security findings — no pivot or redesign during the build. scripts/test-hooks.sh still contains the 4 lane-parity tests (line 1333+). e2e.yml itself was later deleted entirely by an unrelated commit (babbead, "purge the ForgeOps demo — engine-only template"), which is post-feature product evolution, not drift within F-0014's own history.

### F-0015 - MATCHES

Row describes ship.sh: bounded register-wait + fail-closed, gh pr checks watch-to-terminal, merge only on green, never merge master/main base, contract tests on stubbed gh, CLAUDE.md §6 reference. Spot-check confirms scripts/ship.sh implements exactly this (SHIP_REGISTER_TIMEOUT knob, base-branch refusal logic) and CLAUDE.md §6 names it as preferred mechanism. DECISIONS.md records only pre-merge hardening (two low-sev fixes adopted before merge: stricter base-branch guard on undetermined view, integer-sanitized/clamped timing knobs) — normal review-driven refinement of the same mechanism, not a rejected/pivoted design. No NEEDS_WORK cycle, no BLOCK, no "instead"/"rejected" language found for this feature in DECISIONS.md or PROGRESS archive.

### F-0016 - MATCHES

Row title/description/acceptance describe three field-reported fixes: (a) template-neutral AI_OPERATIONS_PLAN doctrine wording, (b) install-into.sh seeding preamble-then-skeleton to avoid duplicate "## Now", (c) assertion-shield detecting missing origin/develop upstream and printing a calm line instead of git fatal. DECISIONS.md (2026-06-12 entries) and PROGRESS-archive.md's F-0015+F-0016 RECORD entry both describe exactly this mechanism, with evaluator PASS (4 criteria) and mandatory security-reviewer APPROVE confirming shield scan coverage unchanged — no NEEDS_WORK cycle, no rejected/pivoted design, no "instead"/"corrected design" language found for F-0016 specifically. The batching of three defects into one PR was a deliberate documented judgment call, not a mechanism pivot.

### F-0022 - MATCHES

Row's acceptance names illustrative mechanisms ("settings.json env, a wrapper, or the spawn step") for arming CLAUDE_ACTIVE_FEATURE mechanically. What shipped (PR #46) instead derives the active feature from the single in_progress row in features.json, with explicit env var only as an override, per PROGRESS-archive.md:61 and path-guard.js:97-114 (confirmed by 11 new contract tests in verify.log). This satisfies the row's actual goal — the guard no longer fails open / no-ops without manual export — and was reached directly, not via a rejected first attempt later hidden. Evaluator PASS and security APPROVE on this exact design (one HIGH finding spawned a separate follow-up feature F-0025, not a redesign of F-0022). No DECISIONS/PROGRESS entry records a pivot away from a settings.json approach during F-0022's own build. The parenthetical mechanism examples are illustrative, not a locked spec, so this is engineering latitude, not the F-0027-class drift pattern.

### F-0025 - MATCHES

Row's title/description/acceptance describe: single-in_progress invariant in update-state.ts (validate + status-guard rejecting 2nd concurrent in_progress) and fail-closed hooks on multiple-in_progress. DECISIONS.md/PROGRESS.md/archive show F-0025 (PR #50) merged with evaluator PASS, security-reviewer APPROVE, "no findings" — no NEEDS_WORK cycle, no BLOCK, no recorded pivot/redesign for this feature's own build. Code spot-check confirms exact mechanism: update-state.ts lines ~136-142 (validate: `wip.length` check) and ~324-329 (writer: rejects setting in_progress when another feature already is), both explicitly commented "F-0025". Shipped artifact matches row's claims verbatim; no drift.

### F-0026 - MATCHES

Row title/description name the mechanism as testing "the evaluator and security-reviewer" (the LLM judges). What actually shipped and is gated is a deterministic corpus for four MECHANICAL guards (assertion-shield, path-guard, update-state, guard-bash) in scripts/test-hooks.sh — none of which are the evaluator/security-reviewer. The LLM-judge fixtures (seeded-idor→security-reviewer, irrelevant-evidence→evaluator) exist only under tests/judges/ as an explicitly DEFERRED, unwired advisory lane (no promptfoo dep, never executed). DECISIONS.md itself narrates this scope reduction (line 75: "Delivered the gate-able half... non-deterministic LLM-judge eval is documented as a deferred advisory... supersedes the earlier DEFERRED stance"), but the features.json row's title/description/acceptance were never amended to reflect that the actual enforced tests cover mechanical guards, not the evaluator/security-reviewer named in the title.

- confirmation vote: confirmed=False - Refuted. F-0026's description explicitly says the testing is "advisory-first," and its acceptance criteria allow fixtures to "run in the contract suite or a documented advisory lane" — wording broad enough to cover exactly what shipped. DECISIONS.md (2026-06-14, PR #60) records at ship time that the deterministic mechanical-guard corpus (test-hooks.sh:1765-1858, explicitly labeled "mechanical judges") was built and gated, while true evaluator/security-reviewer LLM-judge behavioral testing was deliberately deferred to the documented advisory promptfoo lane (tests/judges/README.md), pending an eval-budget trigger in optional-modules.md. This is a disclosed, pre-planned scope split baked into the row's own acceptance wording, not silent F-0027-class drift where a shipped build contradicts its own described design.
- confirmation vote: confirmed=True - DECISIONS.md (2026-06-14, PR #60) confirms this is a conscious pivot, not later evolution: it explicitly states F-0026 shipped only "the gate-able half" — mechanical-guard checks (assertion-shield/path-guard/update-state/guard-bash) in scripts/test-hooks.sh:1765-1858, labeled there as "MECHANICAL judges" — while "the non-deterministic LLM-judge eval is documented as a deferred advisory promptfoo lane," never wired into any running/passing suite. The row's title/description name evaluator+security-reviewer specifically (seeded access-control hole blocked, irrelevant evidence flagged NEEDS_WORK) — those are the LLM judges, not the mechanical guards that actually run. tests/judges/README.md confirms "Status: DEFERRED... NOT active in verify.sh or any CI gate." Row is marked done/passes:true describing a test of "the evaluator and security-reviewer," but what ships and executes is a different thing (mechanical guard tests) with the actual described behavior left as inert, unexecuted fixture files. This is genuine ledger drift, matching the F-0027 pattern, and DECISIONS.md itself frames it as a deliberate scope-reduction at ship time.

### F-0027 - MATCHES

F-0027's row title/description/acceptance already state the shipped design (branch-new-file suppression regex scan, AST-count redesign rejected by security review) and match DECISIONS 2026-06-14/PR #54 and the shipped scripts/assertion-shield.ts. The row DID drift for a period (DECISIONS 2026-07-18 review-wave found the row still narrating the rejected AST design) but was corrected via F-0051's new --amend verb, which is done/awaiting_approval with evidence and its own DECISIONS citation. Present-tense: no drift outstanding.

### F-0030 - MATCHES

Row title/description/acceptance describe a generic goal (broaden deterministic guard patterns for cross-shell/indirection, document deterrent-not-boundary) with no specific rejected mechanism named. DECISIONS.md:68 records attempt 1 was security-reviewer BLOCKed (over-blocked nested relative cleanup paths) and attempt 2 fixed it by anchoring the target to a path boundary — but this is an implementation-detail iteration within the build, not a pivot away from a design the row claims. The shipped guard-bash.sh (lines 78-99) implements exactly what the row promises: PowerShell Get-Content/gc, .NET File.ReadAll*, binary/dump utilities, dd, and redirection blocks. No F-0027-class drift (no false mechanism claim survives uncorrected).

### F-0040 - MATCHES

Row describes an admission gate (ADMIT/REJECT, single-command acceptance check, configurable token-budget heuristic, no new deps) that matches src/oneshot/admit.js exactly: CHAINING_RE rejects multi-command acceptance ("no-verifiable-criterion"), resolveBudget layers options/env/default (8000), charsToTokens is the deterministic chars/4 heuristic. DECISIONS.md shows F-0040 passed the fresh-context evaluator on its first cycle (5/5, 16/16 tests) — no NEEDS_WORK, no security BLOCK, no recorded pivot for this feature specifically (the pre-add NEEDS_WORK cited in DECISIONS.md was against F-0041, fixed before either feature was built, not a post-ship correction of F-0040).

### F-0041 - MATCHES

Row (title/description/acceptance) matches shipped src/oneshot/verdict.js: runs descriptor.acceptanceCommand via spawnSync, captures real exit code to an evidence JSON file unconditionally, returns PASS only on exitCode===0, ignores any 'claimed' field, consumes F-0040's TaskDescriptor shape, no new deps. DECISIONS/PROGRESS show only a pre-add groom-time NEEDS_WORK (tightened acceptance wording before the build started, per 2026-06-18 entries) — not a post-ship pivot. The build itself was evaluator PASS on attempt 0, no security-reviewer trigger (Tier B, correctly), no BLOCK. No mechanism substitution found.

### F-0042 - MATCHES

Row claims verify-gate.sh's Windows abs-path bug (parent-prefixed instead of repo-relative) was fixed by canonicalizing via path.relative, mirroring path-guard.js. Verified in code: both .claude/hooks/verify-gate.sh (lines 31-38, 148) and path-guard.js (lines 45-47) use path.relative(root, ...) with CLAUDE_PROJECT_DIR||cwd root, exactly as described. PROGRESS.md/DECISIONS.md show a clean build (evaluator PASS, security-reviewer APPROVE, PR #133) — no NEEDS_WORK cycle, BLOCK, or design pivot for F-0042 itself. A related LOW finding was folded into a separate feature (F-0043), not a redesign of F-0042.

### F-0043 - MATCHES

Row describes: isolate 6 permissive-mode tests in test-hooks.sh from live features.json via a STATE_FILE fixture, fixing verify.sh failures while a feature is in_progress. PROGRESS.md and DECISIONS.md (2026-06-18) confirm this shipped as designed via PR #134, Tier C, evaluator PASS + security-reviewer APPROVE, no coverage hole. Spot-checked scripts/test-hooks.sh: it literally pins STATE_FILE fixtures (zero-in_progress and one-in_progress) exactly as described, with explicit "F-0043" comments proving both the permissive and fail-closed contracts. No NEEDS_WORK cycles, BLOCKs, or "instead/rejected/corrected design" language found for F-0043 — it was built straight through on the first attempt (attempts:0). This is a genuine build-then-ship, not a drifted row.

### F-0045 - MATCHES

Row describes trimming OPERATOR_GUIDE.md to a cheat sheet pointing to AI_OPERATIONS_PLAN.md §8, preserving all operator actions. DECISIONS.md (2026-07-01) and PROGRESS.md confirm exactly this: 79→40 lines, evaluator PASS 4/4, PR #143 merged, no pivot/rejection recorded. Shipped OPERATOR_GUIDE.md is 40 lines and ends with a line pointing to §8, confirming the mechanism claim. No evaluator NEEDS_WORK cycle or redesign found for this feature.

### F-0047 - MATCHES

Row describes tightening CHAINING_RE to reject a single ampersand plus 5 deterministic verdict.js failure-path tests. Code confirms CHAINING_RE = /[\n;&|]/ at admit.js:58, matching the description exactly. PROGRESS.md (2026-07-02, PR #150) confirms the same regex and the same test set (spawn-error→127, evidence-write throw, stdout/stderr capture, budget boundary) shipped with evaluator PASS 6/6. DECISIONS.md notes one deliberate scope choice (quoted-ampersand false positive accepted by design) and a CRLF-test-scope trim — both scope refinements decided during grooming/build, not a rejected mechanism the row still misdescribes. No evaluator NEEDS_WORK or security BLOCK cycle found for F-0047 itself.

