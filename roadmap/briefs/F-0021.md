# Builder Brief — F-0021: ForgeOps F-0017 MVP Core close + ghost remediation (promptToGraph + A/B logs + RBAC + export unified; current slices integrated)

> Immutable. Build exactly this; report back; nothing more. (Orchestrator pre-brief from downtime: explorer not re-fanout needed — current src/forge + ev F-0019/20 + F-0017 log inspected via Grep/list/read; full delivery now possible.)

## Feature

Close the original F-0017 ForgeOps MVP Core (prompt-to-graph, A/B simulation with real-time structured logs, owner/viewer RBAC, portable JSON + Docker exports) now that remediation slices F-0019 (promptToGraph) + F-0020 (RBAC + export) have landed real src/forge/ code and artifacts under F-0007 guard. Clear the ghost (F-0017 ev was old template-only verify.log; now unify with delivered + add missing A/B logs + full integration/CLI demo). Enables sellable product experience + canvas. 1 builder session. Spec ref: roadmap/briefs/F-0017.md + AI_OPERATIONS_PLAN product vision (auth/RBAC/deploy).

## Spec

- All 5 original F-0017 ACs now exercisable with real code: promptToGraph (from F-0019), A/B sim emitting real-time structured logs (ts/nodeId/message/level/costDelta etc viewable CLI), RBAC check (from F-0020, owner full / viewer 403/404), export (graph + Dockerfile + docker-compose from F-0020 + health/DEMO_MODE).
- Unified demo/CLI: node src/forge/ or npm that runs promptToGraph -> graph -> A/B logs sample -> RBAC check demo -> exportArtifacts to ev dir.
- Update or add units covering all (deterministic, 2-principal RBAC per security.md, logs structure, export strings).
- Clear ghost: F-0021 ev/ includes unified sample logs + current artifacts + fresh verify.log with "VERIFY: PASS (exit 0)" + exact counts (units + product).
- Integrates cleanly (no engine src changes beyond src/forge); uses existing patterns (health.js CJS, minimalGraph, etc).
- Evidence: sample-*.json/logs + full artifacts + verify with marker.

## Acceptance criteria (verbatim from roadmap/features.json F-0017)

- promptToGraph(prompt: string) or equivalent core produces valid executable agent graph JSON (nodes + edges) consumable by the engine
- A/B simulator executes two graph variants over synthetic inputs and emits real-time structured logs viewable via CLI/UI hook
- RBAC layer enforces owner (full edit/run) vs non-owner (read-only 403/404 on resources)
- Export produces portable JSON + minimal Dockerfile/docker-compose scaffold for the graph; integrates cleanly with existing engine
- bash scripts/verify.sh exits 0 (units + product-mode checks; --e2e if UI surface added)

## Your scope & rules
- Touch ONLY authorized: ["src/**", "package.json"] (F-0007 guard live; no .claude/** .github/** scripts/** roadmap/**).
- Authorized paths from F-0017: src/** app/** components/** package.json; forbidden .claude/** .github/** scripts/** roadmap/**.
- **Grep/Glob + targeted Read before every search_replace/Edit/Write.** Initial Grep for "promptToGraph|abSim|rbac|check|owner|viewer|exportArtifacts|docker|DEMO_MODE|log|structured" + targeted reads on src/forge/*.js + tests + ev/F-0019/20. Report paths/lns only.
- Report **TOOL_CALLS_APPROX: N + errors** in final handoff.
- Work silently (no narration); conclusions + paths/lns only. Kaizen discipline: Grep/Glob + targeted before edits; precise contract; call count + evidence paths only (no dumps); 1-2 full verify max at end.
- Early stop if ACs + tests green.
- Final verify only with Git bash on Windows: "C:\Program Files\Git\bin\bash.exe" -c "bash scripts/verify.sh". Capture to roadmap/evidence/F-0021/verify.log containing "VERIFY: PASS (exit 0)" + counts (e.g. X/Y units + contracts).
- CLAUDE_ACTIVE_FEATURE=F-0021.
- Evidence under F-0021/: unified samples (graph, logs, exports, Dockerfile/compose), units output, verify.log + marker.
- Security.md: >=2 principals for RBAC tests (owner allowed, non-owner 403/404); no client-supplied ID trust.
- Frontend.md if any UI hook, but core non-UI.
- Align CLAUDE (explorer-first prior, drift --validate after top PROGRESS + full features + report mismatch + git hygiene + AGENT_STOP on drift), 4/16 xAI (4 quick/explorer + 16 heavy/JUDGE parallel on shared + debate/synth), web P1 fresh.
- Depends on F-0007 + F-0019/F-0020 delivery (use current graph shape, rbac, export).

## Explorer notes (pre-brief from downtime)
- Current on-disk: src/forge/ has promptToGraph.js + .test + rbac.js + .test + exportArtifacts.js + .test (slices delivered); ev/F-0019 has sample-graph + verify; F-0020 has graph/Dockerfile/compose + verify; F-0017 ev is ghost (old verify.log only, no current forge terms).
- A/B logs: extend from prior sketches (generateGraphAwareLog style or simple in-memory exec of 2 variants over synthetic; emit array of structured entries).
- CLI: extend existing or add unified smoke that chains prompt -> graph -> logs sample -> rbac demo (owner/viewer) -> export.
- Risks: Windows path/LF in verify (use Git bash); keep deterministic; exact 2-prin tests; no new deps.
- Pre-brief: Grep-first + targeted done by orch (src/forge ls, ev contents, brief reads); full immutable for instant builder start.
- 4/16 align + sub-agent bloat reduction (Grep/targeted + clean windows) verified live 2026-06-13 via web (xAI docs May 2026: 4/16 on shared backbone parallel debate/synth; Anthropic context eng: sub-agents + compaction for pollution; our Grep-first + explorer-first matches exactly — no prompt change).

BUILD COMPLETE when: 5 ACs exact (promptToGraph valid JSON + det; A/B logs real-time structured sample; RBAC 2-prin owner/403-404; export artifacts with DEMO/health strings; verify green marker + counts + unified ev; only src edits, 0 guard violations, TOOL_CALLS_APPROX + 0e reported). Early stop ok. Orch then --evidence --passes --status --SHIP/RECORD.

Grep first, precise, low bloat, velocity. Full ForgeOps core un-ghosted.