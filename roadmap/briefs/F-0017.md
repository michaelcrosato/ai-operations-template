# Builder Brief — F-0017: ForgeOps product MVP shell and demo experience

> Immutable. Build exactly this; report back; nothing more. (Orchestrator: note here whether explorer fan-out ran and what it found, or why it was skipped.)

## Feature
ForgeOps product MVP: Next.js 15 App Router shell with stunning marketing landing at / and fully interactive /demo powered by the rich seed data in lib/seed.ts. Includes premium UI (components/ui from polish), command palette, responsive dark/light, toasts, basic seed-driven marketplace, monitoring logs/metrics/interventions, and graph viz (now real @xyflow/react). No engine changes.

## Spec
- Use existing package deps (next, framer-motion, @xyflow/react, recharts, lucide, sonner, cmdk).
- Landing (app/page.tsx): Hero, features, interactive teaser using seed, pricing tiers, marketplace teaser.
- Demo (app/demo/page.tsx): Sidebar workspaces, live ticker advancing seed executions, GraphViz now real ReactFlow using seed graphs (nodes/edges, running highlights, click to inspect/log), marketplace grid from templates (start run from template), logs with interventions (pause, approve, inject), metrics from seed, RBAC persona switcher (gates some actions), export buttons (JSON, trace, self-hosted stub), command palette with real actions.
- Integrate real ReactFlow: convert seed WorkflowGraph to nodes/edges, support drag (local), animated for running, onNodeClick to append log.
- For prompt-to-agent: simple input + button in canvas area that creates a new agent node from text (label from prompt, random position, add to local or demo state).
- All data from cloneSeed(DEMO_SEED), mutations update the demo state.
- Polish: animations, empty states, responsive, a11y basics, loading for ticker.
- Authorized only product files; run npm run build (no verify needed beyond build for this feature as per carve-out in authorized).
- Evidence: build log.

## Acceptance criteria (verbatim from features.json for F-0017)
1. npm run build succeeds with / and /demo routes
2. Demo shows seed-driven templates, logs, metrics, interventions that update state
3. Basic graph viz (seed graphs) with click to inspect
4. Responsive, dark mode, toasts, command palette
5. No changes to engine files (.claude/, scripts/, roadmap/)

## Your scope & rules
- Touch ONLY: app/**, components/**, lib/**, package.json (scripts/build if needed), next.config.ts, postcss.config.mjs, tsconfig.json (for paths if needed).
- Authorized paths: from features.json for F-0017. Forbidden: .claude/**, scripts/**, roadmap/**, .github/workflows/**, src/**.
- Run `npm run build` and save the full output to roadmap/evidence/F-0017/build.log (as proxy for verify since product carve-out; the engine verify remains green for meta).
- Exit code is the only truth: report success only on successful build.
- Never delete, weaken, or skip any assertion (none in product yet, but if adding tests later).
- Work silently; final report = files changed, evidence paths, build exit code, anything surprising.
- Applicable .claude/rules/: frontend.md (loaded for app/**).
- If unbuildable, stop and report exactly why.

## Notes from orchestrator
Explorer fan-out: confirmed @xyflow/react in deps, seed has full graphs with source/target, demo page had SVG stand-in ready for replacement, previous polish added UI primitives and command palette skeleton. No conflicts with engine (forbidden paths respected). The subagent refusal was protocol correct (no brief at time), so as orchestrator I groomed F-0017 and provided this brief. Proceed to make the canvas real with ReactFlow + prompt input for agent node creation. Integrate with existing ticker for "running" highlights. Keep demo delightful and immediately runnable with `npm run dev`. 

This unlocks the full vision in the approved plan. After this, other F- will be groomed for monitoring full, rbac, exports, tests, docs.