# Builder Brief — F-0002: Demo feature through the full loop

> Immutable. Build exactly this; report back; nothing more. (Orchestrator: explorer fan-out skipped — the codebase has no product code yet; there is nothing to explore.)

## Feature
A zero-dependency **health-check CLI** — the deliberately trivial feature that proves the engine's loop end-to-end (AI_OPERATIONS_PLAN.md §10, Phase 0 exit criterion).

## Spec
Create exactly two files, plain CommonJS JavaScript, **no new dependencies**:

1. **`src/health.js`**
   - Exports `getHealth()` returning `{ status: 'ok', service: 'ai-operations-template', uptime_s: <non-negative number>, timestamp: <ISO-8601 string> }`.
   - When run directly (`node src/health.js`): prints that object as JSON to stdout and exits 0.
2. **`src/health.test.js`** — `node:test` + `node:assert` suite, ≥4 real assertions:
   - `getHealth().status === 'ok'` and `service === 'ai-operations-template'`
   - `timestamp` parses to a valid Date (ISO-8601)
   - `uptime_s` is a number ≥ 0
   - CLI behavior: spawning `node src/health.js` (execFileSync) exits 0 and stdout parses as JSON with `status === 'ok'`

## Acceptance criteria (verbatim from roadmap/features.json)
1. Feature implemented by the builder sub-agent from a written brief
2. Evidence saved under roadmap/evidence/F-0002/ including a green verify log
3. Evaluator sub-agent issued PASS from a fresh context
4. PR merged to develop with green CI and the operator PR template

## Your scope & rules
- Touch ONLY `src/health.js`, `src/health.test.js`, and files under `roadmap/evidence/F-0002/`. `package.json` test/lint scripts are already prepared — do not modify them or anything else.
- Authorized paths: `src/**`, `roadmap/**`, `package.json` (read-only for you). Forbidden: `.claude/**`, `.github/**`, `scripts/**`.
- Run `bash scripts/verify.sh` and save the full output to `roadmap/evidence/F-0002/verify.log`. Also save `roadmap/evidence/F-0002/test-output.log` (the node --test output) and `roadmap/evidence/F-0002/cli-run.log` (one `node src/health.js` invocation's stdout).
- Exit code is the only truth: report success only on `VERIFY: PASS (exit 0)`.
- Never delete or weaken any assertion. Work silently; final report = files created, evidence paths, verify exit code, anything surprising.
- Rules files: none apply (`src/**` UI/db/api rules don't match a CLI healthcheck).
