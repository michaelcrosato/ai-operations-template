# Builder Brief — F-0019: Production-grade tests, E2E, CI for ForgeOps app

> Immutable. Build exactly this; report back; nothing more.

## Feature
Harden the ForgeOps product with proper tests and CI so the engine gate passes in PRODUCT_MODE. Add Vitest for unit/integration (seed, graph utils, costs, RBAC sim), Playwright E2E for critical user journeys (landing to demo, canvas creation/run, marketplace import, monitoring intervention, export, billing upgrade). Update package.json scripts (test, e2e, build) to satisfy verify.sh. Add basic CI parity if safe. Dockerfile/compose already present.

## Spec
- Vitest: tests for lib/seed.ts (workflows, clones, costs), graph sim logic, demo state mutations, RBAC gating.
- Playwright: e2e/ flows covering: visit / → open demo, switch persona/ws, start from marketplace, interact canvas (create node, run), open monitoring, intervene, export script, open billing, upgrade.
- package.json: ensure "test", "e2e", "build" exist and work; "typecheck" via tsc or next.
- verify.sh compatibility: product mode happy (no missing script errors); run with --e2e if possible (even if skips gracefully).
- No new secrets; use demo mode.
- Evidence: build + test logs under roadmap/evidence/F-0019/.
- Keep product surface only; no engine files.

## Acceptance criteria (verbatim from features.json F-0019)
- Vitest tests for key product logic (seed, graph, costs)
- Playwright E2E covering main demo user journeys
- package.json has test, e2e, build scripts that satisfy verify
- npm run build and test pass
- No engine breakage

## Your scope & rules
- Touch ONLY: app/** (for e2e selectors if needed), lib/seed.ts (if test helpers), components/**, package.json, vitest.config.ts, playwright.config.ts, tests/**, e2e/**, Dockerfile, docker-compose.yml.
- Authorized: product paths above. Forbidden: .claude/**, scripts/verify.sh (read only), roadmap/features.json (use update-state), .github/workflows (read only, no edits per prior).
- Run `npm run build`, `npm run typecheck`, `npm test:product` (or equivalent), and `bash scripts/verify.sh --e2e` at end. Save logs to roadmap/evidence/F-0019/ (build.log, test.log, verify.log).
- Every acceptance has a test.
- Use existing patterns; add minimal config for vitest/playwright.
- Delight not primary here — focus on green gates and coverage.
- Report: test counts, coverage if easy, full gate outputs, demo E2E steps.

## Explorer notes
- Current package has "build" (Next), "test" (engine health + echo), no vitest/playwright yet (sub-agent attempts were blocked).
- Dockerfile/compose exist from prior.
- demo/page.tsx is rich; E2E can target data-testid or roles/text.
- Build is green; keep it.
- verify.sh will complain about missing e2e script or product test/lint — add them so it passes or notes cleanly.
- No .claude or core scripts changes.

This makes the ForgeOps product "real" in the eyes of the engine gate. Once green, the factory can treat product work like any other feature.

## Notes from orchestrator
This F- follows the approved plan's Phase 8. Previous sub-agent for tests refused due to no brief at time; this brief fixes that. Prioritize green build + basic coverage over 100% E2E (add more later via /kaizen). Use Playwright for browser flows like a human user, per the template's own E2E philosophy.