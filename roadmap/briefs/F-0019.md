# Builder Brief — F-0019: ForgeOps promptToGraph core — natural language to executable agent graph JSON (F-0017 ghost remediation slice 1)

> Immutable. Build exactly this; report back; nothing more.

## Feature

Minimal product-mode core delivering the first AC of F-0017: promptToGraph(prompt) or equivalent produces valid executable agent graph JSON (nodes + edges). Deterministic foundation for canvas/sim/export/RBAC later slices. 1 builder session. Un-ghosts the product by landing real src/ code under F-0007 guard. Depends on F-0007 (per-feature authz now passes true + live).

## Spec

- promptToGraph(prompt: string) or equiv pure function/CLI in src/forge or src/lib: takes natural language (or structured prompt) and produces valid JSON with >=1 node + edges; required keys on nodes: id, type, label; on edges: source, target. Additional encouraged for downstream (position/model/prompt/estimatedCost) but minimal to pass ACs.
- Deterministic: same input → same shape (no randomness, no external calls).
- Unit test exercising it (parseable, keys present, deterministic).
- CLI smoke/integration: node invocation or npm script that runs without error and emits e.g. sample-graph.json to evidence or stdout.
- All changes confined to authorized; F-0007 PreToolUse guard enforces (no .claude, scripts, roadmap, .github edits).
- Evidence artifacts: sample-graph.json + exercising unit + green verify.log with exact "VERIFY: PASS (exit 0)" marker.
- Integrates with existing engine (health-style JS, no heavy new deps if possible; update package.json only if runtime needed and documented).

## Acceptance criteria (verbatim from roadmap/features.json F-0019)

- promptToGraph(prompt) or equiv in src/forge|src/lib produces valid JSON (>=1 node + edges); keys include nodes[].id/type/label + edges[].source/target; no touches outside authorized
- Unit test asserts output JSON parseable, has required keys, deterministic (same input same shape)
- CLI smoke or integration (product src/ present) runs without error and can emit sample-graph.json
- bash scripts/verify.sh exits 0 (units + product-mode); evidence/F-0019/ includes sample-graph.json + unit exercising the fn
- Edits confined to authorized (src/**, package.json if dep); F-0007 guard not violated

## Your scope & rules

- Touch ONLY authorized: ["src/**", "package.json"]
- Forbidden for this feature: [".claude/**", ".github/workflows/**", "scripts/**", "roadmap/**"] (F-0007 guard is live and mechanical — blocks violations; orchestrator/maintenance sessions without active feature env are safe).
- **Grep/Glob + targeted Read before every search_replace / Edit / Write.** Do initial broad + targeted Grep/Glob (e.g. for "promptToGraph|graph|node|edge|forge|sample-graph|health") + limited reads (offset/limit) on src/health.js, package.json, tsconfig, any tmp remnants. Report locations/paths/lns only in reasoning. Never edit without prior Grep confirmation of target.
- Report **TOOL_CALLS_APPROX: N (exact if tracked) + errors** at final handoff.
- Work silently: no narration in changes; conclusions + paths/lns + evidence only. Cache-aware: stable rules first.
- Kaizen discipline (verbatim in every sub): "Apply kaizen/guard: Grep/Glob + targeted before edits; precise contract only; report call count + evidence paths/refs only (no dumps); read-only for judges; 1-2 full verify max at end; escalate on 2nd strike."
- Early stop: if ACs met + tests green + sample emitted + marker, stop — no gold-plating.
- Final verify only (1-2 runs max): use Git bash on this Windows host for all sh: full path `"C:\Program Files\Git\bin\bash.exe" -c "bash scripts/verify.sh"`. Save full output + artifacts to roadmap/evidence/F-0019/ (create dir if needed). Poll for "VERIFY: PASS (exit 0)" marker.
- Set CLAUDE_ACTIVE_FEATURE=F-0019 for the build context (in any sub spawns or env for hook).
- Branch: this orch session will fetch + checkout -b feat/F-0019 from origin/develop first. Builder works there. Commit cleanly on that branch.
- Evidence: roadmap/evidence/F-0019/{sample-graph.json, promptToGraph.test.js or equiv, verify.log with marker and exact pass/fail counts e.g. "X passed, 0 failed"}.
- Align with CLAUDE.md (explorer first for research/BRIEF, drift guard after top PROGRESS + features + --validate), .claude/rules (security.md for any RBAC hints but this slice minimal), frontend not applicable (no UI).
- No .env/secrets/production DB, no curl|sh in code, no rm -rf, no assertion weaken.

## Explorer notes (pre-brief context)

- Current tree (confirmed): src/ only contains health.js + health.test.js (F-0002 trivial demo); app/ absent; no prior forge/ promptToGraph on disk (F-0017 ghost confirmed in verify.log snapshots + ls; samples absent). Product vision lives in F-0017/F-0018 history + briefs + groom-adds.ps1 slices + PROGRESS notes post F-0007.
- F-0007 guard now passes + active: any builder must honor authorized src/** + package only or hook fails the edit.
- Determinism without LLM: use keyword/rule-based parser on prompt text (split sentences/phrases → nodes; link sequential or "then"/"and" → edges). Or fixed canonical small graph + light variation from prompt keywords (e.g. "research" adds model/research node). Goal: valid JSON satisfying AC keys + deterministic (hash or stringify compare). Make extensible for later F-0020/RBAC/export/canvas.
- Leverage patterns: src/health.js style (module.exports fn + CLI if (require.main)), simple JSON emit, tests using node:test or existing.
- Prior F-0017 ev had sample-graph.json shape expectations (from history: nodes+edges).
- 4/16 xAI align (web-fresh prior): explorer-first used here; builder focused 1 feat; Heavy-style fresh JUDGE later; kaizen/precise contracts/TELEMETRY + call counts.
- Risks: Windows path/glob in verify (use Git bash + norm /); env for CLAUDE_ACTIVE_FEATURE in node spawns (document/inject in brief); keep no new prod deps.
- This is slice 1: only promptToGraph + test + smoke + verify. F-0020 handles RBAC+export. Do not implement full F-0017.

BUILD COMPLETE when: all 5 ACs delivered exactly (promptToGraph fn + valid output + unit passing deterministic + CLI/smoke emits sample-graph.json + scripts/verify.sh green with "VERIFY: PASS (exit 0)" marker + exact pass/fail counts surfaced + evidence files + call count reported + 0 errors + only edits under src/** (package if needed) + F-0007 guard not triggered/violation in logs). Report TOOL_CALLS_APPROX + evidence paths. Then orch does --passes + SHIP/RECORD.

Explorer first, Grep before edits, precise, low-context, velocity-focused. Make the foundation real.
