# Builder Brief — F-0018: Full interactive visual canvas with @xyflow/react

> Immutable. Build exactly this; report back; nothing more.

## Feature
Replace any placeholder/stub canvas in the ForgeOps /demo with a full, production-feeling @xyflow/react implementation. Support drag-drop from a palette or via prompt-to-agent, node/edge editing, properties panel, integration with lib/seed.ts graphs for workflows, live execution simulation tied to the monitoring ticker (highlight running nodes, step-by-step).

## Spec
- Use @xyflow/react (already in deps) for the canvas area in app/demo/page.tsx.
- Palette or natural language prompt creates nodes (agent, tool, human-gate, parallel, etc.) with realistic Grok model prompts, cost estimates from seed.
- Draggable nodes, connectable edges with rules (no cycles for some, human gates block sim).
- Sidebar properties panel: edit label, prompt, model, params, estimatedCost; live update graph.
- Load/save from seed workflows: buttons to "Load research swarm", "Save as template".
- Execution: "Run simulation" walks the graph, produces logs/costs/trace in the ticker/detail view, highlights active nodes on canvas in real-time (use the existing ticker logic).
- Delight: smooth animations (framer), snap-to-grid, mini-map, zoom, keyboard (delete, undo), accessible labels.
- Responsive: canvas works on desktop primarily, with mobile notes.
- All state via cloned seed; no persistence needed beyond demo session.
- Authorized: only product files. No engine changes.

## Acceptance criteria (verbatim from features.json F-0018)
- Real ReactFlow canvas in /demo with drag, drop, connect nodes
- Prompt input creates high-quality agent nodes from natural language
- Properties panel for editing prompt, model, cost estimates
- Tied to seed: load/save graphs for workflows, highlight running nodes
- No engine files modified
- npm run build succeeds, demo flows work

## Your scope & rules
- Touch ONLY: app/demo/page.tsx, app/page.tsx (if links), lib/seed.ts (additive helpers only), components/ui/* (reuse/enhance existing).
- Authorized paths: app/**, components/**, lib/**. Forbidden: .claude/**, scripts/**, roadmap/** (except this brief), .github/**.
- Run `npm run build` (and `npm run test:product` if exists) at end; save full output to roadmap/evidence/F-0018/build.log.
- Use existing patterns: framer-motion, sonner toasts, Card/Modal/Button from ui/, client state with cloneSeed.
- Every user-visible state: loading (skeleton canvas), empty (no workflows), error (invalid graph), success.
- a11y: aria labels on nodes/edges/palette, keyboard reachable, focus visible.
- Delight: springy drags, animated highlights for running nodes, toast on every create/edit/run.
- No new deps. If prompt-to-agent needs smarts, use simple local heuristics + examples from seed (no LLM calls in demo unless via existing).
- Report: files changed, build evidence path, demo steps, surprises.

## Explorer notes (from prior downtime)
- @xyflow/react is installed, basic usage in previous attempts but reverted to SVG stub in demo for compliance.
- seed.ts has full WorkflowGraph (nodes with position/type/label/model/prompt/cost, edges source/target).
- demo/page.tsx already has rich ticker, interventions, marketplace, RBAC from other sub-agents; integrate canvas without breaking.
- Existing GraphViz placeholder mentions "Real Flow coming" — replace it.
- Build is currently green; keep it so.
- No .claude or scripts changes.

This completes the visual build promise of ForgeOps. Make the canvas the star of the /demo — users should fall in love with dragging agents and watching sims light up nodes live.