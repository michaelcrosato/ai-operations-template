# Builder Brief — F-0022: ForgeOps new auth/RBAC slice + Routine clean run support (extended owner/viewer, billing/marketplace stubs, scheduled Routine hygiene per AI_OPERATIONS_PLAN vision)

> Immutable. Build exactly this; report back; nothing more. (Orchestrator pre-brief from downtime post F-0020 RECORD: no groom --add for ROADMAP Next bullet as non-code; this pre-brief prepares next possible after F-0017/18 resolution. Grep/Glob + targeted reads on ROADMAP/AI_OPERATIONS_PLAN/features/briefs/src/forge/evidence first.)

## Feature

Next ForgeOps auth/RBAC extension slice (builds on F-0020 RBAC sim + F-0017 vision) + support for clean weekly autonomous Routine runs (operator/cloud scheduled /status + model-policy freshness + --validate drift guard + optional-modules trigger watch + hygiene). Per AI_OPERATIONS_PLAN product vision (ForgeOps auth/RBAC/deploy/billing/marketplace) and ROADMAP Next "Nightly autonomous run (Routine) once a week of supervised runs is clean." Small 1-session testable foundation. No UI. Depends on F-0007 + F-0019/20.

## Spec

- Extend RBAC (src/forge/rbac or new auth.js): additional checks or roles (e.g. billing viewer, marketplace admin) with owner full vs limited; unit tests >=2 principals (owner allowed all; non-owner 403/404 semantics) per security.md. Synthetic for demo.
- Billing/marketplace stubs: minimal functions or JSON schema stubs (e.g. canBill(principal), listMarketItems(viewer-safe)) that enforce RBAC; exportable in graph or separate; no real payments.
- Routine clean support: ensure /status skill + downtime sentinel + research + --validate + schedulers produce clean output in non-tty (no prompts, exact counts, plain English); optional-modules trigger checks (src/ exists, protection) fire correctly without side effects. Add or exercise a "Routine mode" seam if needed (e.g. env or flag for scheduled).
- CLI smoke: unified or new that demos extended RBAC + stubs + "Routine hygiene report" (e.g. validate + status summary).
- Units + integration cover RBAC extensions + stub behaviors + Routine hygiene paths (exit 0, no errors in headless).
- Evidence: F-0022/ with extended samples, RBAC logs, stub outputs, fresh verify.log "VERIFY: PASS (exit 0)" + counts.
- All in src/** + package if dep; integrates after F-0020.

## Acceptance criteria (verbatim; adapted for slice from F-0017 + ROADMAP/AI plan)

- Extended RBAC exports checks for new personas (e.g. billing/marketplace); owner allows, non-owner 403/404 on mut; >=2-principal tests per security.md
- Billing/marketplace stubs enforce RBAC (owner full, viewer read-only) and integrate with export/graph; artifacts valid
- Units cover extended deny + stubs + Routine/headless hygiene (no interactive, clean --validate/--status paths); evidence includes samples
- bash scripts/verify.sh exits 0 (units + product); evidence/F-0022/ has RBAC logs + stub outputs + "Routine clean" marker + verify.log with marker
- Only src/** + package.json; forbidden .claude/** .github/workflows/** scripts/** roadmap/**; F-0007 guard honored; pre-brief Grep discipline + TOOL_CALLS_APPROX + 0e

## Your scope & rules
- Touch ONLY: ["src/**", "package.json"] .
- Forbidden: [".claude/**", ".github/workflows/**", "scripts/**", "roadmap/**"] (guard blocks).
- **Grep/Glob + targeted Read before every search_replace / Edit / Write.** Initial Grep/Glob "rbac|check|owner|viewer|billing|marketplace|Routine|status|validate|optional-modules|DEMO|health" + targeted src/forge + roadmap/ (ROADMAP/AI_OPERATIONS_PLAN/PROGRESS/features/briefs) + ev. Report paths/lns.
- Report **TOOL_CALLS_APPROX: N + errors** final.
- Work silently; conclusions + paths/lns only. Kaizen: Grep/Glob + targeted before; precise only; report count + ev paths; 1-2 verify max.
- Early stop if ACs green.
- Final verify: Git bash wrapper "C:\Program Files\Git\bin\bash.exe" -c "bash scripts/verify.sh"; save full to ev/F-0022/verify.log with "VERIFY: PASS (exit 0)".
- CLAUDE_ACTIVE_FEATURE=F-0022 .
- Evidence: F-0022/ RBAC extended logs, stub files/outputs, verify + marker + counts.
- Security.md: 2+ principals min, no ID trust.
- Align: CLAUDE protocol (top~50 PROGRESS + full features + --validate drift guard + git hygiene + AGENT_STOP on mismatch; explorer-first for BRIEF; 4/16 xAI); web P1 (xAI 4/16 May 2026 + Anthropic sub-agent compaction for bloat 2026-06-13 fresh).
- Windows/pwsh: explorer first for cross-plat (git-bash vs pwsh, / vs \, LF .gitattributes, shebangs); explicit checks in ev.

## Explorer notes (pre-brief)
- Current: F-0020 rbac/export live in src/forge + ev; F-0017 ghost (old ev only); ROADMAP has unshipped Next "Nightly...clean" (groom read: no --add, infra ready via skills/schedulers/validate); AI_OPERATIONS_PLAN vision explicitly lists billing/marketplace after auth/RBAC/deploy.
- Stubs minimal (pure fns or consts returning RBAC-decorated results; no external calls).
- Routine: exercise existing /status (plain English regen), downtime sentinel (ls ev/briefs, gh runs, --validate, model last_verified <30d, optional triggers), research; confirm headless clean (no tty prompts).
- Pre-brief immutable for instant resume (F-0017 retry or this as next post ghost clear).
- Risks: scope creep to real billing (forbid; stubs only); Windows quoting in verify (Git bash); exact 2-prin + marker.
- 4/16 + prompt bloat: web fresh confirms (xAI: 4 quick/16 Heavy shared parallel debate/synth for research; Anthropic: sub-agents + compaction + structured notes reduce pollution; Grep/targeted + brief discipline + explorer first = our match; no changes).

BUILD COMPLETE when: ACs exact (extended RBAC 2-prin + stubs with owner/viewer; units + Routine hygiene clean; verify green marker + counts + ev artifacts; src/** only, 0 violations, TOOL_CALLS_APPROX + 0e). Orch --passes etc.

Grep first, precise contract, velocity. Prepares billing/marketplace + clean autonomous cadence per operator vision.