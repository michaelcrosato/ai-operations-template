# Brief: F-0053 — evidence provenance: capture pair + VERIFY-COMMIT ancestry (Tier C)

Immutable brief. Builder: `builder-strong`. Branch: `feat/F-0053` (already checked out). Do not rescope; escalate blockers in your report.

## Why (context)
The 2026-07-18 review wave (DECISIONS 2026-07-18) verified two provenance gaps in `scripts/update-state.ts` `collectEvidenceErrors` (~lines 189-205): (a) a hand-authored `verify*.log` containing only the bare line `VERIFY: PASS (exit 0)` satisfies the validator whenever no `CAPTURED-BY:` header is present — the tamper-evident capture path is optional; (b) `VERIFY-COMMIT` is written by verify.sh (line ~146) but never read by anything. CI re-running the gate remains the backstop for "broken code can't merge"; this feature makes the *stored evidence artifacts* trustworthy going forward without breaking the 27 grandfathered legacy rows.

## Acceptance (verbatim from the feature row)
1. `--passes` requires the green verify log to carry both `CAPTURED-BY: scripts/capture.mjs` and `CAPTURE-EXIT: 0`; a bare hand-written marker line no longer flips - contract test
2. `--passes`, and the `--validate` deep audit for features above the cutoff, parse `VERIFY-COMMIT` and require the SHA to be an ancestor of HEAD via git merge-base; a fabricated or foreign SHA, a no-git value, or a missing line is rejected - contract test; code comments record that this depends on merge commits (ship.sh) and full-history CI checkout and must be revisited if the merge strategy ever changes
3. a named `PROVENANCE_CUTOFF` constant set to 50 grandfathers the existing backlog: `--validate` stays green on a clean tree for all currently-done rows while flip-time strictness is unconditional; any F-9xxx contract-test fixtures that the new rules would catch are updated to captured-style logs
4. `save()` writes to a unique temp file (process id plus randomness) before rename - contract test or assertion covers it
5. `bash scripts/verify.sh` exits 0 with evidence under `roadmap/evidence/F-0053/` and mutation smoke still kills all known mutants

## Design contract
- **Flip-time (unconditional, inside the `--passes` evidence check):** the green verify log must contain, each as an exact trimmed line: `VERIFY: PASS (exit 0)` (existing), `CAPTURED-BY: scripts/capture.mjs`, `CAPTURE-EXIT: 0`, and a `VERIFY-COMMIT: <sha> @ <ts>` line whose `<sha>` satisfies `git merge-base --is-ancestor <sha> HEAD` (spawn git via execFileSync with an args array — no shell interpolation of log content; a sha failing `/^[0-9a-f]{7,40}$/i` — including the literal `no-git` — is rejected BEFORE any git call).
- **Audit-time (`--validate` deep evidence audit): apply the same two requirements ONLY to features whose numeric id part is > `PROVENANCE_CUTOFF` (= 50)**. Constant near `SAFETY_FORBIDDEN` with a comment citing DECISIONS 2026-07-18 (grandfather: 27 legacy rows predate mandatory capture; flip-time strictness is unconditional so pending sub-cutoff rows F-0046/F-0048 are still caught at their future flip).
- **Squash-merge caveat comment** (AC2's wording) sits next to the ancestry check.
- **`save()` rider:** temp path becomes `${FILE}.${process.pid}.${<random hex>}.tmp` (rename semantics unchanged).
- The existing F-0051 row (id 51 > cutoff) is already passing with captured evidence — after your change, `--validate` on the REAL backlog must stay green (its log carries the capture pair and `VERIFY-COMMIT: a001d4e…`, an ancestor of main via PR #171). If it does not, STOP and report — do not weaken the check and do not touch the real evidence.

## Contract tests (scripts/test-hooks.sh, STATE_FILE-fixture pattern, F-9xxx ids)
Prove at minimum: bare-marker log (no capture header) REJECTED at `--passes`; log with capture pair but fabricated/foreign SHA REJECTED; missing `VERIFY-COMMIT` line REJECTED; the literal value `no-git` REJECTED; a fully-valid captured log with a real ancestor SHA (use `git rev-parse HEAD` in the test) ACCEPTED; a grandfathered fixture (id ≤ 50) with a legacy bare-marker log still passes `--validate`'s deep audit; unique-tmp behavior (e.g. two temp names differ, or the tmp filename matches the pid+random pattern).
**Fixture trap (expected work):** existing F-9xxx fixtures in test-hooks.sh that flip `--passes` with plain marker logs will now fail — update those fixtures to captured-style logs with a real HEAD sha (helper function encouraged). Do NOT weaken or delete any existing assertion (assertion-shield + evaluator will check).
Do NOT touch `scripts/mutation-smoke.sh` (not in authorized paths); mutation smoke must still pass all existing mutants.

## Evidence (AC5)
    bash scripts/capture.sh F-0053 verify -- bash scripts/verify.sh
Never tee/redirect into roadmap/evidence; never prefix verify.sh with CLAUDE_ACTIVE_FEATURE.

## Paths
- Authorized: `scripts/update-state.ts`, `scripts/test-hooks.sh`, `roadmap/evidence/F-0053/**` (+ features.json diffs via the sanctioned writer only).
- Forbidden: everything else; explicitly `.claude/**`, `.github/**`, `scripts/mutation-smoke.sh`, `scripts/verify.sh`, hand-edits to `roadmap/features.json`, and ALL existing `roadmap/evidence/F-*/` logs (grandfathered artifacts are historical record).
- Set `CLAUDE_ACTIVE_FEATURE=F-0053` for Edit/Write operations; not around verify.sh.

## BUILD-end git hygiene (mandatory)
`git add scripts/update-state.ts scripts/test-hooks.sh roadmap/evidence/F-0053 roadmap/features.json` → commit (`feat(F-0053): ...` + Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>) → push `feat/F-0053` → report SHA, test counts, surprises. Tier-C reminder: after judging, this feature PARKS at awaiting_approval and its PR stays OPEN for the operator — do not merge anything yourself.
