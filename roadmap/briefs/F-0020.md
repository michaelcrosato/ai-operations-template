# Builder Brief — F-0020: ForgeOps RBAC simulation + deploy/export scaffold foundations (owner/viewer 403/404, portable JSON + minimal Dockerfile/docker-compose)

> Immutable. Build exactly this; report back; nothing more.

## Feature

RBAC sim + export/deploy scaffold slice of F-0017 (owner/viewer 403/404 per security.md, portable graph JSON + minimal Dockerfile + docker-compose with DEMO_MODE/healthcheck). Small testable foundation after F-0019 promptToGraph core lands. Un-ghosts more of product; engine guard F-0007 stable.

## Spec

- RBAC module (e.g. src/forge/rbac.js or lib): exports check(principal, resource, action): 'allow' | 'deny'. Owner principal allows all; viewer denies mutations (return 403/404 semantics in usage, or explicit codes). Unit tests use >=2 principals (owner allowed; non-owner 403/404) per security.md rule.
- Export helper: writes artifacts (graph.json + minimal Dockerfile (node base, COPY graph, healthcheck) + docker-compose.yml (DEMO_MODE, health)). No changes to engine src. Artifacts valid, contain expected strings.
- Units cover RBAC deny + export contents.
- CLI or integration smoke to produce the artifacts (similar to F-0019 sample emit).
- Evidence under F-0020/: RBAC logs or test output, Dockerfile/compose snippets, graph.json sample.
- All under authorized only; integrates after F-0019 graph shape.
- verify green (units + product).

## Acceptance criteria (verbatim from roadmap/features.json F-0020)

- RBAC module exports check(principal, res, act): allow/deny; owner allows all, viewer denies mutations (403/404); unit tests use two principals (owner allowed, non-owner 403/404) per security.md
- Export helper writes graph.json + minimal Dockerfile (node base, COPY graph, healthcheck) + docker-compose.yml (DEMO_MODE, health); artifacts valid, no engine src changes
- Units cover RBAC deny for viewer + export file contents contain DEMO_MODE/healthcheck strings; evidence includes sample artifacts
- bash scripts/verify.sh exits 0 (units + product); evidence/F-0020/ has RBAC logs + Dockerfile/compose snippets + graph.json
- Only src/** + package.json; forbidden .claude/** .github/workflows/** scripts/** roadmap/**; F-0007 guard honored

## Your scope & rules

- Touch ONLY authorized: ["src/**", "package.json"]
- Forbidden for this feature: [".claude/**", ".github/workflows/**", "scripts/**", "roadmap/**"] (F-0007 live guard blocks; orch/maintenance safe without active).
- **Grep/Glob + targeted Read before every search_replace / Edit / Write.** Initial Grep/Glob for "rbac|check|owner|viewer|403|404|Dockerfile|docker-compose|DEMO_MODE|export|graph" + targeted reads on src/ after F-0019 lands (or health patterns). Report paths/lns.
- Report **TOOL_CALLS_APPROX: N + errors** final.
- Work silently; conclusions + paths/lns only. Kaizen discipline verbatim: "Apply kaizen/guard: Grep/Glob + targeted before edits; precise contract only; report call count + evidence paths/refs only (no dumps); read-only for judges; 1-2 full verify max at end; escalate on 2nd strike."
- Early stop if ACs + tests green.
- Final 1-2 verify only with Git bash on Windows: `"C:\Program Files\Git\bin\bash.exe" -c "bash scripts/verify.sh"`. Capture full to evidence/F-0020/verify.log containing "VERIFY: PASS (exit 0)".
- CLAUDE_ACTIVE_FEATURE=F-0020 .
- Branch: feat/F-0020 (after F-0019 lands; orch manages checkout from origin/develop).
- Evidence: roadmap/evidence/F-0020/ with RBAC test output, Dockerfile, docker-compose.yml, graph.json, verify.log + exact counts.
- Security.md: two principals min for authz tests (owner/non-owner); no client ID trust.
- Align CLAUDE (explorer first, drift --validate after reads, 4/16 patterns), no secrets.
- Depends on F-0019 graph shape for export sample (use compatible minimal JSON).

## Explorer notes (pre-brief)

- Builds on F-0019 promptToGraph/graph.json foundation (sequential slice).
- Current: no RBAC/export code (ghost); use synthetic principals (strings 'owner'/'viewer' or objects) for demo.
- Export: keep minimal (no real docker build here, just scaffold files with strings).
- F-0007 guard: src/** only.
- Patterns from prior ev (sample-ab-logs, exports) if in history; otherwise pure new under src/forge/.
- 4/16: focused builder + future Heavy JUDGE; kaizen + telemetry (calls/min, 0e).
- Risks: path norm Windows for verify; keep deterministic; exact security AC test shape (owner allowed, viewer 403/404 on mut).
- Slice: RBAC + export only. Do not redo promptToGraph or add UI.

BUILD COMPLETE when: 5 ACs exact (RBAC check with 2-principal tests per security, export writes 3 artifacts with strings, units pass, verify green with marker + counts, evidence files, only src edits, 0 guard violations, call count + TOOL_CALLS_APPROX reported). Early stop ok. Then orch --passes/SHIP/RECORD.

Grep first, precise directive, low bloat, velocity. Foundation for sellable ForgeOps.
