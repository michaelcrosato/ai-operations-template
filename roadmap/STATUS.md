# Status Report — 2026-06-14 — External review verified; safe fixes shipped, the rest honestly queued

> This week an outside review (five independent expert reports) was checked point-by-point against what the system actually does. Most of it was fair; some of it was already handled; a few claims were about things that are intentional. The safe, clear fixes are going out now. The bigger fixes are written up as planned work so nothing is lost — and the "everything is done" claim has been corrected to the honest picture.

> Auto-generated by `/status`. Written for a business reader; no jargon.

## Shipped (recently merged)
- **The visual workflow canvas** is live in the demo: build agent workflows by dragging boxes or describing them in plain language.
- **The demo is more reliable**: a flaky internal test that occasionally failed for no real reason was fixed at the root, so the automated checks are trustworthy again.
- **Windows friendliness**: fixed how the safety checks run on Windows so they stop failing for the wrong reasons.

## Ready for your QA
- A **clean-up update** is going out for review now (one bundle). It does not add product features — it removes overstated marketing claims, adds the standard public-project safety/contribution files, patches a known third-party security advisory, and makes the Windows setup smoother. Plain-English review notes and a click-by-click check list are in the update itself.

## In progress / queued next
Three pieces of planned work were opened to finish things honestly:
- **Finish the canvas access control.** The visual canvas shipped, but the "viewer can look but not change" rule is not yet fully enforced on every edit button, and that needs a real browser test to prove it. This is now the top queued item.
- **Make a safety guard actually switch on.** One internal guard that is supposed to keep each task inside its assigned files only turns on when a setting is present that, today, nothing sets — so it currently does nothing. Queued to wire it up properly (a second safety net already covers this in the meantime).
- **Refresh the AI model names.** The demo refers to an older AI model name that has since been superseded. Queued to point everything at one up-to-date setting so this never drifts again.

## Blocked / needs you
- **Nothing blocking.** Three light, optional questions are waiting whenever you have a moment (see the questions list): the tone of the demo's marketing copy, the order to tackle the queued work, and when to take on the larger software-version upgrades. Sensible defaults are already applied.

## Health
- ✅ **All automated checks passing** (206 checks total: 16 product checks + 190 safety-guard checks, 0 failing).
- ✅ **Security settings turned on** for this public project: vulnerability alerts, automatic security updates, secret scanning, and private vulnerability reporting were all off and are now on. A known third-party advisory was patched in the update going out for review.
- ✅ **Honest progress count**: 20 of 23 planned items are complete; the 3 new ones above are the honest "not done yet" list (previously the report implied everything was finished).
- ⚠️ One automated security-update notice (about the third-party component just mentioned) shows as failed — it is redundant because the fix is already in the update awaiting your review; it clears once that merges.
- Cost note: running on subscription usage. Reminder: automated background runs draw from a separate monthly pool, and the newest top-tier model moves to usage-credit billing around June 22 — worth re-checking that date before relying on it.
