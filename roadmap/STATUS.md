# Status Report — 2026-06-12 — A RELIABLE "MERGE ONLY WHEN GREEN" TOOL + FIRST-INSTALL POLISH

> Today the factory mechanized its most-broken habit — finishing a change by waiting for its automated checks and only merging when they all pass — into a single reliable tool, so work can't get stranded by a forgotten watch again. It also smoothed three small first-time-install snags that nine separate trial setups hit on the same day. Still waiting on your three setup clicks.

> Auto-generated by `/status`. Written for a business reader; no jargon.

## Shipped (since yesterday's report)
- **A dependable "wait, then merge only if everything's green" tool** (PR #30): the single most common way work got stranded was a session opening a change, starting to watch its checks, then ending — so nobody saw them turn green and the finished change sat unmerged. That has happened three times across your projects. There is now one reliable command that does the whole thing: it waits for the checks to actually appear, refuses to merge if they never do, watches them to the end, and merges only when every check is green (and never into your protected release line). It will not guess.
- **Three first-install rough edges smoothed** (PR #31): brand-new projects copying this system hit three bits of harmless-but-confusing noise — a scary-looking (but always-passing) red error on the very first save, a section heading printed twice in the starter plan file, and one line of internal documentation that only made sense for this original project. All three are now clean. Nine separate trial installs ran into these on the same day; none changed how anything actually works.

## Quality notes you should like
- Both changes passed inspection on the first attempt: independent AI inspection PASS on each, and security review APPROVE with zero blocking findings on each.
- The new merge tool was deliberately *not* allowed to merge its own introducing change — a brand-new safety tool earns its first real use on the next change, not itself. Its first live merge was the install-polish change, and it worked.
- The system now runs 164 self-tests on every change (up from 139 yesterday).
- A working-habit gap was closed: the prior session merged both of these changes but ended before writing them into the project's records. The rulebook now requires every session that merges a change to finish recording it the same session, or leave a clear written hand-off note. Today's session completed those missing records.

## Ready for your QA
- Nothing needing action — both items were internal machinery (a merge tool) and first-install polish. Adopting projects pick them up when the department syncs them.

## In progress
- Nothing — every buildable item is shipped (14 of 16 complete with verified proof; the two open items are your setup task and one deliberately deferred design).

## Blocked / needs you (~15 minutes)
- Same three clicks-only steps in Q-0001: install the Claude GitHub App, add your **subscription token** as a repo secret (no API key — runs on your Max plan; turns on "@claude fix this" comments), and create the cloud workspace.

## Health
- ✅ All automated safety checks passing — 164 self-tests, every completed work item has verified proof on file.
- ✅ No open changes waiting on checks; both of today's merged changes are now fully recorded before this session ends (per the recording rule above).
- ✅ Field feedback loop healthy — three first-install snags reported across nine trial setups were fixed within a day.
- Cost note: running on subscription usage, session-by-session. Reminder: from June 15 automated background runs draw from a separate monthly credit pool, and from June 23 the newest top-tier model bills as usage credits.
