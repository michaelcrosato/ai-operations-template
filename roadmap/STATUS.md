# Status Report — 2026-06-14 — Review fixes shipped: real access-control, guard, and model fixes now live

> An outside expert review was checked point-by-point against what the system actually does, and the genuine, high-value fixes have now been built and merged — not just listed. The four most important ones are done; the bigger remaining items are written up as planned work so nothing is lost. The progress count is honest: 24 of 31 done.

> Auto-generated by `/status`. Written for a business reader; no jargon.

## Shipped (this session)
- **Viewers can no longer secretly edit the visual canvas.** The demo had a "viewer" role that looked read-only but didn't actually stop edits. Now every edit control is disabled for viewers with a clear "read-only" message, and the underlying access-control rules were rebuilt and tested (an independent security check caught and we fixed two real loopholes where a lower role could change billing/org data).
- **A safety guard that was silently switched off now works.** A guard meant to keep each task editing only its assigned files was effectively doing nothing; it now activates automatically, and a follow-up fix closed a way it could have been bypassed.
- **The demo's AI model name is current again.** It referred to an older model everywhere; that's now pulled from a single setting and updated, so it can't drift out of date across files again.
- Plus the earlier clean-up wave: removed overstated marketing claims, added the standard public-project safety/contribution files, patched a known third-party security advisory, and turned on the repository's security alerts.

## Ready for your QA
- Nothing requires action right now. All of the above merged on green automated checks. When you next promote a release, the QA pack will walk you through trying the viewer-vs-owner canvas behavior click-by-click.

## In progress / planned next
Seven larger improvements are written up as planned work, in priority order. Each is a substantial, focused piece (and a couple are deliberately risky), so they're queued rather than rushed:
- Finish the automated browser-test program for the UI.
- Add tests for the AI "reviewer" steps themselves (so the graders are graded).
- Make the test-protection guard smarter (fewer false alarms) — it currently nags on harmless test renames.
- Make the safety guards understand more shell types (e.g. PowerShell) — today they mainly cover one.
- Modernize the core code's type-safety and tidy up the largest UI file.
- Take on the big software-version upgrades (these are about staying current, not urgent security — the security-critical versions are already in place).

## Blocked / needs you
- **Nothing blocking.** Three light, optional questions are waiting whenever convenient (see the questions list): the demo's marketing tone, the order to tackle the planned work, and when to schedule the big version upgrades. Sensible defaults are already applied.

## Health
- ✅ **All automated checks passing** (205 safety-guard checks + 29 product checks, 0 failing on every merge).
- ✅ **Security settings on** (vulnerability alerts, automatic security updates, secret scanning, private reporting); the previously-flagged third-party advisory is patched.
- ✅ **Honest progress count: 24 of 31 planned items done.** The 7 remaining are the larger planned items above, each with a written spec.
- Cost note: running on subscription usage. Reminder: automated background runs draw from a separate monthly pool, and the newest top-tier model moves to usage-credit billing around June 22 — worth re-checking that date before relying on it.
